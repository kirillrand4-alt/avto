# Гейт «нет опции отказа» отключён (02.08.2026)

Замер по `ai_letter_log` (452 письма): 656 срабатываний замечания «нет опции
отказа», 369 писем упёрлись в предел кругов переписывания — каждое отсутствие
шаблонной строки «извините за письмо, больше не побеспокою» стоило платного
круга перегенерации.

Решение владельца: письма из-за этого не перегенерировать и НЕ браковать.
Сама фраза при этом остаётся: правило 2 в промптах (`RULES_KC`, `RULES_MEYER`)
не тронуто, модель пишет её как раньше — почти всегда.

Правка в `C:\sender\sender\ai_letter.py`, только гейт:

    БРАКОВАТЬ_БЕЗ_ОТКАЗА = os.environ.get('AI_LETTER_BRAKOVAT_BEZ_OTKAZA', '') == '1'
    ...
    if БРАКОВАТЬ_БЕЗ_ОТКАЗА and not _has_optout(low):
        fails.append('нет опции отказа')

Возврат старого поведения — `AI_LETTER_BRAKOVAT_BEZ_OTKAZA=1`, без отката файла.
Резервная копия исходника: `C:\sender\_ops\ai_letter.py.bak-3s-20260802-201821`.

Юридическая отписка не зависит от текста вовсе: `List-Unsubscribe` +
`List-Unsubscribe-Post: One-Click` (RFC 8058) ставятся в заголовки каждого
письма всегда (`sender.build_headers`, «юр-гейт»).

Проверка на живом коде после замены: письмо без фразы — gate вернул `[]`,
письмо с фразой — `[]`, правило в промпте на месте.

Попутно снят мой же ложный вывод из OTCHET-pravki-pisem-3-sessii.md §3:
«адрес замораживается при постановке в очередь» — НЕВЕРНО. `Sender.send`
перечитывает получателя из базы в момент отправки, а 19 писем в общие ящики —
ручные правки оператора (журнал `recipient_changed`: 17 смен «личный → общий»
руками, автоподбор auto-69 двигал в обратную сторону и был откачен). Чинить
там нечего; править код значило бы затирать решения владельца.
