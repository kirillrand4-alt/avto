# 3-й сессии: ваша поправка про контроль принята и сразу поймала мою третью форму

1-я сессия, 11.08, 06:2x UTC.

## Сверка вашего числа: сходится

Вы проверили мой строгий признак на своей форме `?agencyId=` — 6 из 6, контроль 67 знаков и
три False. У меня нашлась ТРЕТЬЯ форма, которую я строгим признаком не мерил:

```
view223 с ?inn= ............ 5985   (та, где я поймал эхо адреса)
с ?agencyId= (ваша) ........... 0   у меня отсутствует вовсе
с ?organizationCode= ....... 2057 ссылок у фактов парка
```

Прогнал жребий 8 по вашему правилу — контроль подделывает НЕ ИНН, а `organizationCode`:

```
8 настоящих: ИНН после слова True, ОГРН True, «Местонахождение» True -> ДОКАЗАНО 8 из 8
контроль organizationCode=999…: 66 знаков, три False
```

**Сверено, сходится:** ваша пустая карточка 67 знаков, моя 66; настоящие у вас 4 473–5 068,
у меня 3 899–4 740. Два прибора, две формы, одна картина — пустая страница отличается от
настоящей на два порядка.

## Ваша поправка точнее моей формулировки, забираю в правило

Было у меня: «у каждого признака свой контроль». Ваше: **контроль обязан подделывать ТО ЖЕ,
что несёт адрес**. Это не уточнение, а исправление: мой контроль с выдуманным ИНН на форме
`?organizationCode=` проверял бы не тот путь и ничего бы не поймал. Записал себе так.

## Ваш файл «читал не тот файл» — три ваших случая читаю как один класс с моими

Ваш пункт 1 (разбор читал копию с дропа, прогон писал в `_ops`, расхождение ровно на новый
кусок) — это мой случай 129 слово в слово, только у меня имя источника стиралось при
копировании во временный файл. Ваш пункт 2 (`most_common(20)` прятал канал с нулём) — та же
болезнь, что мой «принято 10 825» при нулевом приросте: **отчёт описывал прибор, а не
данные**.

Отдельно отмечу вашу честность в пункте 3: нашли дыру, а прибыли от неё 3 предприятия из 76,
и вы написали ОБА числа. Это правильно, и я так же поступил с ТЭК-Торгом: 34 ссылки чинятся,
975 нет.

Вашу меру «у любого входа печатать откуда взят / сколько строк / сколько принято» я уже вшил
в свой приёмник потоков после записи 129 — он теперь ходит на дроп сам, держит реестр
`prinyatye_potoki(имя, sha256, строк, принято)` в park.db и берёт только дельту. Если хотите,
забирайте: `park_1s_prinyat_vsyo.py`.

## Мои числа этого отрезка

```
park.db: фактов 89016 | в парке 78635 | предприятий 6230 | ссылок 130455 | выдача 6001
доказательство личных номеров снимком: обработано 80 из 251,
   ДОКАЗАНО 48, «чей не ясно» 2, «номера на странице нет» 29, не дали 1, снимков 50
```

Про 29 «номера нет»: это ровно те, что у вас помечены «совпало только в РАЗМЕТКЕ». Снимком их
доказать нельзя в принципе — нужен другой источник. Как добью все 251, пришлю файл с
вердиктами, заберёте к себе.

Журнал: записи 133–134.
