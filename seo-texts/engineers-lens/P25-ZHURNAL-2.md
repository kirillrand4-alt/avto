# P25 · журнал 2-й сессии. Площадки, сайты, первая двадцатка

Задание: `P25-TZ.md`. Старт: `P25-START-VSEM-SESSIYAM.md`. Общие правила: `PRAVILA-OBSHCHIE.md`.
Правило отчётности: закрытый пункт без записи с числами считается незакрытым.

---

## 04.08, старт смены

### Исходник проверен своими руками, а не принят на слово

`Company.xlsx` скачан с дропа, пересчитан. Совпало с ТЗ: **2 045 операций, 491 ИНН,
2 686 млн ₽**. Разложен в `P25-PREDPRIYATIYA-po-summe.csv` и в рабочую очередь
`P25-OCHERED.csv` (формат прежней очереди: `;`, колонки `inn`, `predpriyatie`, плюс `mesto`,
`summa`, `operaciy`, `poslednyaya_pokupka`, `tip`).

Три вещи, которых не было в ТЗ, потому что файла тогда не было:

| находка | число | что меняет |
|---|---|---|
| топ-20 по сумме | **57 % всех денег** | «первую двадцатку руками» — теперь главный пункт, а не украшение |
| топ-50 | 76 % | хвост в 441 предприятие весит вчетверо меньше |
| ИНН из 12 цифр = ИП | 13, на 24,3 млн | оргструктуры нет, шесть методов сайта по ним бессмысленны |
| «Город» и «Сфера» на листе ИТОГ | **пусто целиком** | планировать на них было нельзя |
| купили ровно раз за год | 196 | |
| купили 5+ раз | 120 | |

### Инструменты: переиспользованы, не форкнуты

Шесть обходчиков площадок и оба разборщика лиц переключаются на P25 **переменными
окружения** (`P25_OCHERED`, `P25_VYHOD`, `P25_KARTA`, `P25_DIR`, `P25_PREFIKS`), а не копией
файла. Причина конкретная: в этих модулях половина ценности — записанные ловушки площадок
(пустое значение фильтра отдаёт весь реестр; несколько секций молча схлопываются в одну).
Копия разошлась бы с оригиналом на первой же правке ловушки, и разошлась бы молча.

**Порядок обхода пришлось чинить в двух местах** — это не мелочь, а подмена задачи:

* `gpb_po_ocheredi.py` шёл по порядку СПРАВОЧНИКА ПЛОЩАДКИ. Первым обошёлся бы тот, кто
  первым попался в чужом файле.
* `lica_s_kartochek.py` и `gpb_lica_s_kartochek.py` сортируют «сначала у кого нет
  технического человека». У P25 таких **все**, оба ключа у всех нули, и порядок задавал бы
  третий ключ — число карточек. Первым пошёл бы самый болтливый на площадке, а не самый
  дорогой покупатель.
* У `gpb_lica_s_kartochek.py` вдобавок есть список приоритетов соседей под центробежку.
  Предприятие, попавшее в оба списка, увело бы порядок P25 к чужой задаче. Проверка `mesto`
  поставлена ДО него.

Во всех трёх случаях правило владельца «единственная сортировка — по сумме покупок» молча не
выполнялось бы, а лог показывал бы честную работу.

### Замер входов до прогона — прибор способен увидеть?

| справочник | строк | наших ИНН нашлось |
|---|---:|---:|
| ЭТП ГПБ `etpgpb-zakazchiki-inn.csv` | 17 499 | **58 из 491** |
| Сбербанк-АСТ `sber-inn-kpp.csv` | 1 086 | **43 из 491** |

Оба справочника собирались под центробежку. Росэлторг, ТЭК-Торг, Портал и РТС ищут по ИНН
напрямую — они покрывают все 491. Значит по ГПБ и Сберу канал заведомо неполный, и это
надо чинить (расширять справочник), а не выдавать 58 за охват.

### Запущено

| прогон | охват | состояние |
|---|---|---|
| ЭТП ГПБ, списки закупок | 83 входа (58 ИНН) | идёт |
| Росэлторг, списки закупок | 488 предприятий | идёт |
| Поиск сайтов + судья «чей это сайт» | 491 | идёт |

**Беру канал поиска (xmlriver) примерно на 30 минут** — 491 предприятие по замеру 2,1
запроса на компанию ≈ 1 030 запросов. Пулы общие на три сессии, поэтому пишу здесь до
прогона, а не после.

Контроль сужения на ЭТП ГПБ пройден до сбора: несуществующая компания вернула пусто
(`kontrol_bred: 0`). Без него площадка молча отдаёт весь реестр — 1,7 млн процедур.

### Чего нет и что придётся строить

**Метод №1 из шести — «найти страницы по карте сайта, а не угадывать пути» — инструмента
нет ни одного.** Ни одного упоминания `robots.txt` или `sitemap` во всём репозитории.
Существующий разбор структурных подразделений ходит только по заданным путям. Это следующая
постройка.

---

## Вопросы соседям

**1-й сессии.** По `P25-TZ.md` за вами DaData → ОГРН → чеко. **Сайт предприятия** лежит в
карточке чеко. Я сейчас ищу сайты поиском (491 шт., ~30 мин канала) — если у вас они
появятся из чеко раньше, отдайте файлом, и я сниму свой прогон: это чистое дублирование.
Мой результат отдам в любом случае, с колонкой «чей сайт подтверждён» (по замеру на
водоканалах «найден сайт» и «найден ЕЁ сайт» расходятся втрое: 60 против 16 из 64).

**3-й сессии.** Канал взят на ~30 минут под поиск сайтов, не под обратный ход по ФИО.

---

## 04.08, первые результаты и один упор

### Люди пошли

ЭТП ГПБ, списки: **83 компании, 37 422 закупки, ноль сбоев**. Разбор карточек на 17 из 51:
**57 человек, 17 с личным мобильным, 17 предприятий**. Отдача по мобильным держится около
30 % — совпадает с прежним замером канала (15–16 %) и даже выше.
Росэлторг: 8 755 процедур, идёт.

### Метод №1 построен, и на нём поймано три ошибки прибора подряд

`p25_karta_sayta.py`. Все три — один класс: **сайт отдавал всё, а модуль писал «пусто»**.

1. **CORS по своему же сайту.** Открывали `kaustik.ru`, сайт уводил на `www.kaustik.ru`, а
   запросы уходили по нашей строке — то есть на ДРУГОЙ origin. Браузер резал их, `fetch` кидал
   TypeError. В журнал шло «robots нет, адресов в картах 0». Починка: origin берётся со
   страницы (`location.origin`), а не из нашей строки.
2. **Mixed content.** Карта Каустика перечисляет вложенные карты по `http://`, страница
   открыта по `https://`. Браузер режет как небезопасный запрос. Тот же ложный ноль.
   Починка: схема подтягивается до `https` для СВОЕГО хоста (чужие не трогаем — там это была
   бы догадка). **После починки: 0 адресов → 1 176.**
3. **Потолок резал случайное.** Подошёл 41 адрес при потолке 40, и выжила ли нужная страница,
   решал порядок меню. Починка: вес адреса, `struktur|podrazdelen` первыми. Плюс отсечены
   581 языковая копия (`/en/...` — то же содержимое вторым запросом) и ложное совпадение
   «apparatus-operator» со словом «аппарат управления».

### Сайты первой двадцатки терялись на ПУСТОМ значении

Поиск сайтов по 24 верхним: подтверждено 10, «чужой» 3, «ничего» 11. Но в «ничего» лежали
`alrosa.ru`, `uacrussia.ru`, `kurganpribor.ru`, `karmed.ru`, `kgs.kaluga.ru` — найдены, и
найдены верно. Судья сказал не «чужой», а «не знаю», и следующее звено прочитало это как
«ничего нет». Шесть сайтов из первой двадцатки — 57 % денег.

Отдельный исход нужен и для холдингов: `phosagro.ru` у АО «Апатит» и `knauf.ru` у
ООО «КНАУФ ГИПС БАЙКАЛ» помечены «чужой». Формально верно — юрлицо не то. Практически это
единственное место, где лежат структура и люди завода.

Написан `p25_sayt_podtverzhdenie.py`: ИНН ищется на страницах самого сайта (реквизиты,
контакты, раскрытие — на одну ссылку глубже главной), и найденная строка сразу становится
ПЕРВОИСТОЧНИКОМ со ссылкой и цитатой, как требует ТЗ.

### УПОР, который в одиночку не снять

**Целый класс сайтов предприятий недоступен нашему браузеру.** Проверено на трёх:

    https://alrosa.ru/       → Privacy error, location.origin = "null"
    https://uacrussia.ru/    → Privacy error
    https://kurganpribor.ru/ → Privacy error
    http:// вместо https     → то же самое (редирект на https)

Это не сломанные сайты. Российские предприятия массово перешли на сертификаты НУЦ Минцифры,
корня которого в Chromium нет. **Мы читаем «сайт не открылся» там, где сайт работает.**

Лечение известно и уже описано в нашем же репозитории: `server/patch_fetch_url_v2.py`, флаг
`insecure` — «нужен для госресурсов с российскими сертификатами». Но:

* `fetch_url` **не в allowlist раннера** — проверено запросом, ответ «task не в allowlist».
  Патч написан и не установлен (или снесён `pull`-ом).
* Установлены сейчас: `browser_probe`, `enrich_contacts`, `news_scan`, `verify_company`,
  `ping`, `pull`.
* Сам чинить не стал: `pull` затирает чужие патчи, а на раннере прямо сейчас работают три
  сессии. Это ровно тот случай, когда правило говорит спросить, а не пробовать.

**Нужно решение:** поставить `patch_fetch_url_v2.py` (или добавить `insecure` в
`browser_probe` — тогда чинится для всех модулей сразу). До этого предприятия с российским
сертификатом закрывать как «людей на сайте нет» НЕЛЬЗЯ: это ложный ноль.

---

## 04.08, 16:50 — ТРЕТИЙ ОТКАТ КОНТЕЙНЕРА ЗА СМЕНУ. Что уцелело и как это видно

Обнаружено не догадкой: сторож попросил числа, и **все файлы разом показали ноль**, а логи
прогонов оказались пусты. Проверка `git log -1` дала `e1d8fcd` — коммит ДО всей работы по P25.
Девять живых прогонов исчезли вместе с контейнером.

### Уцелело всё, что было закоммичено. Потеряно только несохранённое

    восстановлено `git checkout -B ... origin/claude/read-intro-section-hrstzj` → 88c8e3c
    файлов P25 в engineers-lens                 44
    модулей p25_*.py                             6

| файл | строк после восстановления |
|---|---:|
| `P25-etpgpb-po-kompaniyam.csv` | 37 683 |
| `P25-roseltorg-po-kompaniyam.csv` | 13 741 |
| `P25-sberast-po-kompaniyam.csv` | 7 449 |
| `P25-tektorg-po-kompaniyam.csv` | 2 541 |
| `P25-portal-po-kompaniyam-inn.csv` | 2 658 |
| `P25-etpgpb-lica-s-kartochek.csv` | 494 |
| `P25-LYUDI-2S-005.csv` | 490 |
| `P25-LYUDI-S-SAJTOV.csv` | 136 |

**Потеряно ровно одно:** `sajty_P25_OCHERED_csv.jsonl` — сырая выдача поиска сайтов по всем
491, лежала только в песочнице. Из неё уже извлечено главное (95 подтверждений в
`P25-SAJTY.csv` и 97 строк в `P25-SAYTY-2S-002.csv`), но исходные кандидаты для второго
прохода подтверждения пропали. Это цена того, что промежуточный файл жил в песочнице, а не в
репозитории.

**Правило себе:** результат прогона пишется в `engineers-lens/` и коммитится, а песочница —
только для логов. В ТЗ это записано («на сервер и в `.jsonl` с fsync, не только в песочницу»),
и я это нарушил ровно в том месте, где потом и потерял.

### Что перезапущено

    ЭТП ГПБ лица      50 из 51 → доработать
    ТЭК-Торг          171 из 491 → 320 осталось
    Портал Москвы     364 из 491
    Сбербанк-АСТ      516 из 1063 ключей → 787 осталось
    карточки Яндекса  38 из 491 → 453 осталось

Ни один журнал обхода не потерян, поэтому перезапуск не переспрашивает пройденное.

---

## 04.08, 17:30 — youtube.com в списке сайтов предприятий

Найдено не проверкой, а по дороге: в журнале обхода сайтов среди целей стоял
`https://www.youtube.com`. Он дошёл бы до `company.sayt` как сайт предприятия.

**Откуда взялся.** Из ссылок карточки организации Яндекса. Мой фильтр в ней отсекал ВК,
Одноклассники и Телеграм — и пропускал видеохостинги, соцсети и агрегаторы. Канал организации
на YouTube, страница в соцсети и карточка на checko выглядят с карточки одинаково: «внешняя
ссылка», а сайтом предприятия не являются ни в каком смысле.

**Починено и вычищено:**

    заслон чужих площадок расширен: youtube, rutube, facebook, instagram, twitter, linkedin,
      wikipedia, rusprofile, list-org, checko, zachestnyibiznes, sbis, audit-it, 2gis, zoon,
      flamp, hh, avito, wildberries, ozon
    P25-STRANICY-SAJTOV.csv    931 → 891   (40 страниц youtube.com)
    P25-YANDEX-KARTOCHKI.csv    62 → 52    (10 карточек с чужой ссылкой)
    P25-SAJTY-godnye.csv        50 → 49

**Людей с этих страниц не добыто ни одного** — проверено по обеим выкладкам: 0 строк с чужих
площадок. То есть мусор пойман раньше, чем дал результат, и в панель не уехал.

### Цитата стала дословной, старое пересобрано

136 строк с сайтов были разобраны до починки цитаты и содержали пересказ моих же полей.
Отложены в `P25-LYUDI-S-SAJTOV-bez-citat.csv`, обход запущен заново — теперь в каждой строке
кусок самой страницы вокруг человека.

Проверка «должность встречается в цитате» на старых строках давала 100 % и не значила
ничего: я сам составлял цитату из тех же разобранных полей, которые ею и проверял.

---

## 04.08, ночь. Два замера, один канал закрыт не начав

### Должности у площадок нет — проверено на живой карточке, а не по списку полей

Раньше я писал «у ЭТП ГПБ 92 поля, полей вида position нет». Это про API. Открыл саму
карточку глазами:

    https://etpgpb.ru/procedure/tender/price_request/349915-...
    длина текста 3 546 знаков, имя контактного лица на странице ЕСТЬ
    должностных слов на всей карточке — НОЛЬ

Вокруг имени стоит: «Адрес электронной почты | s.polikarpov@rt-eg.ru | Контактный телефон |
+7 495 600-32-00 | Контактное лицо | Поликарпов Станислав Олегович». Должности нет ни в
каком виде. Значит **должность приходит только с сайта предприятия**, и расширение сайтового
канала — единственный путь к мере ТЗ, а не один из нескольких.

### Сбербанк-АСТ: 40 281 процедура, разборщика нет и строить его сейчас не стоит

Самый крупный неразобранный источник. Проверил, есть ли на карточке контактное лицо:

    https://www.sberbank-ast.ru/OK/purchaseview.aspx?id=10977750
    http 200, заголовок «Информация об открытом конкурсе»
    текст документа            3 657 знаков — только шапка, меню и подвал
    фреймов                    1, и его src РАВЕН адресу самой страницы
    длина текста во фрейме     0
    «Контактное лицо» в тексте НЕТ

Страница вкладывает саму себя во фрейм, и фрейм приходит пустым. Телефоны, которые видны на
ней, — **линии поддержки самой площадки** (`8 800 302-29-99`, `+7 495 787-29-97`,
`info@sberbank-ast.ru`). Наивный разборщик собрал бы их как контакты предприятия: одинаковый
номер у сотен ИНН, и правило «общий номер» поймало бы это только после заливки.

**Решение: канал не строю.** Не потому что «источник плохой» — а потому что замер показал,
что цена входа высока (карточка не отдаёт содержимое обычным путём), а искомого на ней не
видно вовсе. Если понадобится — вход искать через POST-выдачу, как на других страницах этой
площадки; рецепт лежит в `patch_browser_probe_evaljs.py`.

### Ходок считал успехом список из одних ошибок

Разбор страниц возвращает по записи на каждый адрес, и у неудачи запись тоже есть — там
вместо текста лежит `err`. Список из двадцати ошибок непустой, `bool(res)` даёт True, ходок
решает «дошли» и второго захода с сертификатом НЕ делает.

    было:  uacrussia.ru  20 страниц запрошено, 0 ответили
           alrosa.ru     20 запрошено, 0 ответили
    стало: uacrussia.ru  20 → 20 ответили,  kak = без проверки сертификата
           alrosa.ru     20 → 19 ответили,  kak = без проверки сертификата

Это ОАК и АЛРОСА — 9-е и 12-е места по сумме покупок. Терялись целиком и молча.
Починка: список считается дошедшим, только если хоть у одной записи есть содержимое.

---

## 04.08 вечер — ПЯТЫЙ откат контейнера. Механика восстановления записана как рецепт

Откаты идут подряд: 16:50, 20:16, 21:15. Каждый раз одно и то же — прогоны исчезают, файлы
показывают вчерашние числа, `git log -1` отстаёт от origin.

**Признак, по которому виден откат, а не медленная работа:** числа по ВСЕМ каналам падают
разом. Один канал может встать, все сразу — не могут.

    было в 20:16   карта 107 сайтов, ЕИС 94, Яндекс 186, годных сайтов 105
    показало       карта  57 сайтов, ЕИС  0, Яндекс  70, годных сайтов  53

**Рецепт восстановления, третий раз одинаковый:**

    git stash push -u -m "откат <время>"        ← ОБЯЗАТЕЛЬНО: без него checkout отказывает
    git checkout -B <ветка> origin/<ветка>
    перезапустить прогоны — журналы обхода целы, пройденное не переспрашивается

Второй раз `checkout` ответил «Aborting» из-за незакоммиченных правок в четырёх файлах. Это
не ошибка git, это правильное поведение: он отказался затирать несохранённое. Stash решает.

**Что уцелело после пятого отката** (всё, что было запушено):

    карта сайтов     199 сайтов, 3 332 страницы
    люди с сайтов     90 сайтов, 263 человека
    ЕИС               94 карточки, 65 контактов
    карточки Яндекса 213 предприятий, 191 карточка
    годных сайтов    105 из 491
    выкладок людей    17, последняя 804 строки

**Вывод, который стоит дороже самих потерь:** дисциплина «пушить после каждого шага» —
единственное, что отделяет откат от потери смены. Всё, что не запушено, теряется целиком;
всё, что запушено, возвращается за одну команду.

---

## 05.08 ночь — аудит по ТЗ и канал 223-ФЗ

### Что не сделано, честным списком

Владелец спросил: всё ли, что умею, сделано. Проверил по пунктам ТЗ, а не по памяти.

| пункт ТЗ | состояние |
|---|---|
| 11. обход шести площадок | сделано: Портал и ТЭК-Торг 491/491, Росэлторг 488, Сбер 71 249 процедур |
| 12. разбор карточек | **четыре из шести**; РТС запущен только сейчас, Сбер разбирать нечего |
| 13.1 карта сайта | построено с нуля, 248 сайтов, 3 820 страниц |
| 13.2 разбор от ФИО | построено, соединено с картой |
| 13.3 запрос «предприятие + должность» | **НЕ НАЧАТО** — капча и неотвеченный вопрос про `panel_py` |
| 13.4 новости о назначениях | **НЕ НАЧАТО** |
| 13.5 раскрытие, годовые отчёты, состав комиссии | **НЕ НАЧАТО** |
| 13.6 разбор провайдером | **НЕ НАЧАТО**, хотя это прямое правило владельца |
| 14. первая двадцатка руками | **не сделана отдельным проходом** |

### Поправка к собственной цифре

Я сказал «доступно 39 105 номеров ЕИС». Пересчитал ПО ДЛИНЕ НОМЕРА, а не по уникальности:

    19 знаков (44-ФЗ)   8 442 номера на 20 предприятиях   ← канал работает, отдача 61 %
    11 знаков (223-ФЗ)  4 480 номеров на 18 предприятиях  ← канал не спрашивался вовсе
    8, 6, 7, 9 знаков   внутренние номера площадок, в ЕИС не ведут

То есть потолок ЕИС — не десятки тысяч, а два десятка предприятий по каждому закону.
Считал уникальные номера вместо годных: большое число оказалось поводом проверить прибор.

### Сбербанк-АСТ: не пропущенный канал, а вход для ЕИС

Живая карточка `sberbank-ast.ru/OK/purchaseview.aspx?id=...` людей не содержит вовсе — это
зеркало извещения 44-ФЗ. Зато в ней стоит **ссылка на извещение в ЕИС**, а ЕИС людей даёт.
Разбирать карточки Сбера незачем; его 71 249 процедур ценны номерами.

### 223-ФЗ подключён, но люди с него пока не пошли

`adres_po_nomeru()`: длина номера называет закон, 11 знаков → раздел 223 ЕИС. Признак дешевле
любой пометки площадки — у Росэлторга в одном файле лежат оба вида.

Первые шесть карточек дали НОЛЬ людей. Проверил страницу глазами: она открывается, ИНН и
заказчик на месте, но контактного блока в основной вкладке нет — текст всего 6 179 знаков.
Похоже, у 223-ФЗ контакт лежит в другом месте, и это надо искать отдельно, а не считать,
что данных нет.

### Ошибка в собственном пробнике, из-за которой чуть не обвинил ЕИС

Второй пробник ответил «сайт не открылся, оба захода не дошли». Причина не в сайте: я убрал
из возвращаемого объекта поле `origin`, а ходок именно по нему решает, дошли ли. Без поля
любой ответ выглядит как заглушка. Прибор сломал я, а вывод получился бы про источник.

---

## Заход: вид номера, роли и вес адреса (автономно, владелец спит)

### Н1. Метка «личный» на городских — ошибка моя, и она в месте выдачи

3-я сессия посчитала по базе: `p25.db` — 316 городских номеров помечены «личный» против 136
верных, 311 таких строк несут `source=P25-LYUDI-2S*.csv`. Проверил у себя: в `P25-LYUDI-2S-025`
673 городских против 144 мобильных — сходится.

Причина не в добыче. **Моя выкладка не несла колонки вида номера**, и заливке приходилось
угадывать. Чинить проверкой на стороне заливки бессмысленно: следующая выгрузка вернёт то же.
Признак ровно один — после нормализации номер начинается на **79**.

Сделано: общий `p25_nomer.py`, колонки `vid_nomera`, `nomer_ne_lichnyy`, `dobavochnyy`
начиная с выкладки 026. Разбор один на все каналы — три копии правила разошлись бы молча.

### Н2. Добавочный через ПРОБЕЛ (найдено тем же счётом)

64 номера не разбирались ни во что: `7-42135-31680 2507`, `3532 66-36-53 032`. Это городской
и добавочный через пробел. Добавочный я уже чинил на ЕИС, но там он через дефис
(`7-495-9261420-8559`), и правило написалось под дефис. **32 из 64 восстановлены**, остальные
32 помечены `не разобран`, а не выданы за номер.

Городской печатается сплошным (`+7 4213531680`): код города бывает 3, 4 и 5 знаков, таблицы у
меня нет, а разбить `42135-31680` как `(421) 353-16-80` значит напечатать неверный код города.

### Н3. Корень «закупк» не ловит родительный падеж

`Начальник отдела закупОК`, `Руководитель службы закупок`, `Управление закупок` уходили в
«неясно» — беглая гласная съедает подстроку. Корень → `закуп`. Снабжение 60 → 79.
Туда же: `Начальник технического отдела` и `управления технического надзора` (техническая
роль знала «технический директор», но не отдел и не надзор). Техническая 22 → 24.

### Н4. Мы продаём ИМ — их отдел продаж не наша цель

Из 30 человек с мобильным и ролью «неясно» **26 оказались продавцами самого предприятия**:
«региональный представитель», «менеджер отдела продаж», «руководитель группы продажи
сельхозтехники». Сайты выкладывают свой отдел продаж на видное место и часто с мобильными —
отсюда перекос. Заведена роль `продажи (их сторона)`: не выбрасываю, но и не считаю мерой.

**Проверка стороны идёт РАНЬШЕ проверки темы.** Поймано на «втором закрытом предприятии»:
ООО «КАМА» #376, Пожарский, мобильный прямо со страницы, роль «техническая» — а слово
«технический» пришло из подразделения «Служба технического сервиса», и человек принимает
претензии на их продукцию (почта `claims.kama@sveza.com`, блок «Группа по претензиям»).
Мера вернулась с 2 на **1**. Это честное число: закрыть предприятие человеком, который
компрессор не выбирает, — это не закрыть его.

### Н5. Вес адреса считал новость страницей структуры

У ФосАгро из 80 адресов в первую двадцатку попали ДЕВЯТЬ пресс-релизов:
`press/company/...izmenenii-STRUKTURy-vladeniya...` ловится на `struktur`,
`press/industry/RUKOVODitel-STRUKTURnogo-podrazdeleniya...` — сразу на оба. Раздел новостей
теперь опознаётся **по началу пути**, а не по словам внутри.

Порядок страниц пересчитывается ПРИ ЧТЕНИИ — это чинит все 315 уже снятых карт без обхода.
Но у **69 сайтов из 266 адресов ровно 40** — они упёрлись в потолок, и выброшенных адресов в
файле нет. Их надо переснимать: флаг `--perekartirovat <список>`.

### Н6. Сайт группы ловится самоназванием, а не только чужим ИНН

`phosagro.ru` **не пишет ИНН ни на одной из девяти страниц** — проверено пробой, слова «ИНН»
там нет вовсе. Заслон по чужому ИНН на нём молчит. Но сайт зовёт СЕБЯ «ФосАгро», а мы искали
«Апатит»: название дочки на сайте группы стоит всегда, а своим именем сайт группы зовётся
именем группы.

Оговорка: имя может быть **закодировано в самом домене** (`nng.gazprom-neft.ru` у
«Газпромнефть-ННГ») — тогда сайт его, как бы он себя ни звал. Проверено на 22 доменах с
известным заранее ответом, **расхождений 0**. Правило нарочно снисходительное: ошибка «чужой
принят за свой» оставляет прежний слабый исход, ошибка в обратную сторону отнимает у завода
его настоящий сайт.

Замечено попутно: `sibur.ru` заявлен как сайт ПЯТИ разных ИНН — это ловило старое правило
спорного домена. `phosagro.ru` заявлен одним, и потому молчало.

### Н7. В раздел попал, а по разделу не прошёлся

Владелец показал `phosagro.ru/.../materials-and-equipment/contacts-structure-materials-and-
equipment-department/` — «Зайцева Светлана Владимировна / Начальник отдела / +7 (8202) 59-33-72».
В моей карте её нет, а её родная сестра по разделу — есть. Страницы отделов не попадают ни в
`sitemap.xml`, ни в меню главной; найти их можно только открыв страницу раздела.

Сделан `p25_sosedi_razdela.py`: точки входа — страницы веса 0–4 **и их родительские пути**,
причём родители идут ПЕРВЫМИ. Проверено: при смешанном порядке первые шесть мест занимают три
страницы со словом `management`, и нужный раздел не помещается.

### Что это меняет в мере

Закрыто **1 из 491** (КАМАЗ-ЭНЕРГО, Ахмадуллин, ЗГД по производству, 8-927-481-32-22).
Технических без мобильного — 22, из них с известным доменом 11: это прицельная охота за
номером, запущена.

### Н8. Ходок объявил «не дошли» на 22 сайтах из 22 — это был прибор

Добор соседей по разделу записал в журнал 22 строки подряд: «оба захода не дошли», ссылок 0.
Среди них `nlmk.com`, `sibur.ru`, `polyus.com` — сайты, которые заведомо открываются, я с них
уже снимал страницы. **Двадцать два одинаковых отказа у двадцати двух разных доменов — это
всегда прибор.**

Причина: `p25_hodok.doshli()` для СПИСКА считает содержимым только поле `t`. Правило писалось
под разбор страниц (там текст в `t`) и стало молчаливым заслоном для всякого нового канала:
добор возвращает `ssylki`, ссылки приходили и выбрасывались, а ходок вдобавок делал второй
заход с отключённой проверкой сертификата — впустую, по два запроса на сайт.

Починено списком полей `SODERZHIMOE`. Замер до и после: **было 0 новых страниц с 22 сайтов,
стало 54 с четырёх.** 22 записи сломанного прибора из журнала убраны — это не показания о
сайтах, и оставлять их значило бы никогда к этим сайтам не вернуться.

### Н9. Формула приёмки взята у 3-й сессии и применена к себе

Их поправка (мера 13 → 9): счётчик мёл файлы, которые их же публикатор отложил как
непринятые, и читал поле, говорящее «НЕ первоисточник», как доказательство.

У себя проверил: мой сборщик читает файлы ПОИМЁННО, без шаблонов, — отложенные
`-slomannyy-razbor` и `-bez-citat` в него не попадают. Но их принцип «есть положительная
формула приёмки И нет формулы отказа» у меня не стоял вовсе. Поставил проверяемую:
**цифры номера должны быть в цитате**. Из 145 мобильных цитатой подтверждены 144, один нет —
в меру он не идёт. Обе закрытые строки проходят и по цитате, и по ссылке-адресу.

### Решения смены (владелец спит)

* Сайт группы **отдаю, но не как собственный сайт**: в `company.sayt` только вес 2 и 3.
  Выбросить нельзя — у АО «Апатит» весь раздел закупок стоит на `phosagro.ru`.
* Карточки Яндекса — **только подтверждённая принадлежность**: 84 номера на 80 предприятиях
  отдано, 333 карточки с неподтверждённой принадлежностью не отдаю.
* Порции сайтов собирались разовым кодом — написан `p25_otdat_sayty.py`, чтобы правило веса
  лежало в файле, а не в памяти.

### Н10. Посмотрел панель глазами — четыре находки, три из них мои

Зашёл под `user3` на `/p25/centro`, прочитал главную и карточки.

**Очередь правильная.** 491 компания, 20 ИНН из 20 на первой странице — мои, ровно в порядке
суммы (#1 РТ-ИНЖИНИРИНГ, #2 КОМПРЕССОР-ТЕХЦЕНТР, #3 АПАТИТ). Название в шапке
«Центробежные — база продаж» меня сперва сбило, я решил было, что под P25-адресом открыта
центробежная очередь. **Проверил числом — нет, база P25.** Опасение снимаю.

**Метка вида номера доехала.** На карточке РТ-ИНЖИНИРИНГА оба контакта показаны как «общий»,
и это сходится с моим файлом: `+7 4956003200`, городской, `nomer_ne_lichnyy = да`. Слов
«личный»/«мобильный» на странице не нашлось вовсе.

**Моя находка: пять мобильных стоят у двух разных людей каждый.** Разобрал поштучно — два
разных случая:
* «Аксёнов Денис» и «Денис Аксенов Не» (ЭЙЧ ЭНД ЭН) — ОДИН человек, две карточки с разным
  порядком слов. Дедуп брал первое слово за фамилию. Угадывать фамилию по длине пробовал и
  отверг: «Дукин Сергей Геннадьевич» даёт «сергей». Склейка идёт по ВЛОЖЕННОСТИ множеств
  слов — лишнее слово «Не» равенству мешает, вложенности нет. Склеено 6 записей.
* «Дукин Сергей» и «Черняев Владимир» (ТЯЖМАШ), отдел кадров Кировпейпера — РАЗНЫЕ люди с
  одним номером. Значит номер не человека, а бюро. **Признак «начинается на 79» отличает
  мобильный от городского, но НЕ личный от отдельского**, а для меры ТЗ важно второе.
  Заведён вид `мобильный отдела`, 8 человек, в меру не идут.

**Моя вторая: 16 «должностей» — это соседняя строка.** «тел. (8482)» у десяти человек одного
завода, «по пятницам с 9.00 до 12.00 часов» у одного. Все осели в «неясно» и меру не тронули,
но заполненная мусором клетка ХУЖЕ пустой: пустую добирает прицельный поиск должности, а
заполненную он пропускает как известную. Вычищены.

**Не моё, 1-й сессии — два наблюдения:**
* На карточке `Регион 119517` — это почтовый индекс, а не регион, и фильтр «Все регионы»
  забит двумя сотнями индексов вперемешку с названиями областей. Продавец фильтровать не
  сможет.
* `Источник: P25-LYUDI-2S.csv` — постоянное имя, а не пронумерованный снимок. Ровно на
  постоянном имени 3-я сессия потеряла 40 строк: файл сменился между двумя заливками.
  Ссылаться надо на `P25-LYUDI-2S-034.csv`.

### Третье закрытое предприятие, с оговоркой

**#184 ООО «БЕРГАУФ НЕВЬЯНСК»** — Кайгородцев Алексей Петрович, технический директор,
`+7 912 650-38-03`, почта `a.kaigorodtsev@bergauf.ru` совпадает с фамилией, страница
`bergauf.ru/contacts/`. Карточка оказалась богатой: **десять мобильных с настоящими
должностями** (логистика, закупки, начальник отдела логистики).

Оговорка: `bergauf.ru` у меня «не подтверждён» для ИНН 6621010115 — домен несёт «бергауф», но
слова «невьянск» на нём нет. Он может быть техническим директором ГРУППЫ, а не невьянского
юрлица. Считаю закрытым по тому же правилу, по которому беру людей с `phosagro.ru` для
АО «Апатит»: контакт рабочий и достижимый, а привязку к юрлицу помечаю, а не выдаю за
доказанную.

Проверил и отдельно, не повторяю ли ошибку ООО «КАМА»: там ДОЛЖНОСТЬ была сервисная
(«руководитель направления технического сервиса») и почта `claims@`. Здесь должность —
«технический директор», а «свяжитесь с технической поддержкой» описывает, как до него
дойти, а не чем он занят.

### Н11. Экзамен 3-й сессии: 10 из 12, и оба промаха — «два места на одно правило»

Прислали двенадцать случаев, шесть отрицательных. Прогнал `razobrat()` не трогая ничего:
пятизначные коды городов прошли все (я нигде не требую длины кода), а `ИНН 7706613770` и
`договор № 2024051234` **приняты за городские номера** — десять цифр получают семёрку
спереди и выглядят целым номером.

Заслон у меня БЫЛ — в серверной части добытчика номеров, где он смотрит слово ПЕРЕД цифрами.
В общий `p25_nomer` он не переехал. То есть у меня самого было ровно то, о чём предупреждает
3-я сессия: одно правило в двух местах, чинится одно, расходятся молча. Перенёс в общий
модуль, с оговоркой: слово «тел»/«моб»/«доб» рядом ОТМЕНЯЕТ запрет, иначе «тел. № 123-45-67»
потеряется из-за знака №. **Стало 16 из 16** (их двенадцать + мои четыре про добавочный).

### Н12. Поиск должностей отработал, а находки я потерял — и хорошо, что потерял

`rc=0`, данные вернулись, файла нет. Причина: `stdout_tail` — это ХВОСТ, и цельный JSON
в него не поместился, метка `НАЙДЕНО_JSON` ушла за край. Печатаю теперь КУСКАМИ с номерами
(`NAYDENO 3/7 [...]`), в конце — сколько кусков было: если часть срезана, это видно числом,
а не молчанием.

Хорошо, что потерял, потому что качество было негодное, и в файле оно бы осело:
* «Гурина Наталья Геннадьевна» → запрос ушёл как `site:tplusgroup.ru "Геннадьевна"`, и
  должность приехала от **Слюсаревой Елены Геннадьевны**. Моё `familiya()` брало самое
  длинное слово, а отчество длиннее фамилии. Та же ошибка, что я в этот же час чинил в
  сборщике, — и снова в двух местах.
* «Шаталова А. С.» из КОГАЛЫМ НПО-СЕРВИС → «руководителем» со страницы ООО «КОЛЕСТОН»,
  ИНН 3128009531. Другой человек, другая компания, другой город. Теперь страница обязана
  быть НА ДОМЕНЕ предприятия либо называть само предприятие.
* «замене ворот» как должность — из «Проведение работ по замене ворот». Должность обязана
  НАЧИНАТЬСЯ со слова должности, а не быть обрывком фразы.
* Имя из одних инициалов без домена не ищем вовсе: однофамильцев много, проверить нечем.

### Н13. 985 новых страниц разделов разбор не увидел бы никогда

Добор соседей принёс их на сайтах, уже помеченных в журнале пройденными. Гонять сайт заново
незачем — добавлен режим `--tolko-novye`: берутся ровно те адреса, которых нет в разобранных.
277 сайтов, 5 780 страниц. Так не тратятся запросы на прочитанное и не плодятся двойники.

### Н14. Откат контейнера №10 — и он же поймал мою ошибку в проверке

Владелец показал карточку «Люди предприятия» с источниками «поиск-ЛПР-3с-песочница» и спросил,
не моё ли. Проверил по файлам: **ни одного из 15 человек в моих данных нет**, строк с меткой
«поиск-ЛПР» — ноль, мои источники это `etpgpb.ru`, `roseltorg.ru`, страницы заводов. Не моё,
3-я сессия признала своё.

Но проверка взяла выкладку **006**, хотя последняя 035. Это оказался ОТКАТ КОНТЕЙНЕРА: HEAD
стоял на `c1e0a07`, выкладок 6 вместо 35, все прогоны убиты. Восстановление по своему же
рецепту (`git stash push -u` → `git checkout -B <ветка> origin/<ветка>`) вернуло всё: работа
была запушена. Десятый откат за смену; `avtopush.sh` держит потерю в пределах четырёх минут.

Отдельно стоит записать, ЧТО именно спасло: я не поверил числу «006», а посмотрел, почему оно
такое. Счётчик, взявший не тот файл, выглядит точно как счётчик, взявший тот.

### Н15. «Файла нет» и «строк мало» печатались одной фразой — и скрыли опечатку

Запустил разбор карточек РТС: `[РТС-тендер] в файле 0 строк — пропускаю`, при том что в файле
42 строки. Причина у меня в двух местах сразу:
* префикс я передал как `P25` вместо `P25-`, модуль искал `P25rts-po-kompaniyam.csv`;
* фраза «в файле 0 строк» одинакова для «файла нет» и «файл пуст», и опечатка выглядела как
  факт о площадке.

Плюс порог `len < 50` молча выбросил бы РТС и с правильным префиксом: списков там всего 42.
Разделил на три исхода («ФАЙЛА НЕТ», «файл есть, но строк 0», «строк всего N — беру всё
равно»), порог снят. Пошло: 42 карточки, 6 предприятий.

### Н16. Главный резерв был не в каналах, а выше по течению: 308 предприятий без поиска сайта

Раннер стоял на 3 из 30. Считая, чем занять остальное, посчитал охват по каждому каналу — и
нашёл узкое место не там, где искал:

```
предприятий всего                                    491
  с годным сайтом (подтверждён / название / группа)  105
  сайт вообще искали                                 183
  сайт НЕ ИСКАЛИ НИ РАЗУ                             308
```

Среди них **#7 (49,5 млн), #14, #15, #21, #22**. Это выше по течению всего остального: нет
сайта → нет карты → нет страниц → нет людей → предприятие не закрыть в принципе. Все мои
технические роли и все снабженцы пришли с сайтов, и другого пути для них нет.

**Потолок ЕИС измерен и он низкий.** Номера процедур есть только у **32 предприятий из 491** —
остальные не госзакупщики, а коммерческие покупатели, их в ЕИС просто нет. Из 32 пройдено 18,
запущен остаток (142 карточки по 15 предприятиям, по 10 на предприятие, чтобы охватить всех,
а не вычерпать одного).

**Сбербанк-АСТ разбирать не буду, и вот почему числом.** В списке 71 249 строк, но предприятий
из очереди P25 там **15**, а источник у всех — «Госзакупки по 44-ФЗ»/«ФЗ-94», то есть те же
извещения, номера которых уже забирает ЕИС (`P25-sberast-po-kompaniyam.csv` стоит первым в его
`ISTOCHNIKI`). Разбор карточек Сбера дал бы третью копию того же факта.

Запущено параллельно шесть прогонов, раннер 15 из 30:
поиск сайтов (308), разбор новых страниц разделов (277 сайтов / 5 807 страниц),
перекартирование упёршихся в потолок (40), ЕИС-остаток (142), поиск должностей (60), автопуш.

### Н17. «308 без сайта» было моей ошибкой счёта — на деле 70

Владелец поправил: список сайтов давно выкладывали. Проверил — он прав, и ошибка ровно та,
за которую я час назад хвалил себя в Н16.

Я определил «сайт искали» как **«ИНН есть в моём файле подтверждений»**. Но сайты от 1-й
сессии шли прямо в карту, минуя подтверждение. Пересчёт по ВСЕМ файлам, где у ИНН стоит
непустой сайт:

```
предприятий всего                              491
  сайт известен (включая списки соседей)       421
  сайта нет нигде                               70
```

Не 308, а **70**. Я погнал бы искать заново 238 предприятий, у которых сайт уже есть — из них
177 уже картированы, 145 уже разобраны на людей. Поиск остановлен на 95-й секунде и
перезапущен на настоящем остатке.

**Урок ровно тот же, что я записывал про других:** мера, считающая по одному файлу, меряет
файл, а не мир. Мой Н16 был правильным по форме («считай охват») и неверным по существу
(«охват по одному источнику»).

### Н18. ЕИС: из 670 «обходов» настоящих 258, остальные — заглушки, записанные успехом

Остаток ЕИС дал 15 карточек и НОЛЬ людей, при том что канал давал лучшую отдачу. Смотрел
не отдачу, а журнал: у всех 243 карточек 223-ФЗ длина текста ровно **2 452 знака**. Двести
сорок три разные закупки не весят одинаково — это одна и та же страница.

Открыл её глазами: **главная страница ЕИС** («Официальный сайт Единой информационной системы…
Мой регион: Не выбран»). Адрес 223-ФЗ уводит на главную. А часть 19-значных отдаёт 67 знаков —
«Запрашиваемая страница не существует» (это номера по ФЗ-94, закону 2005 года, их извещения
лежат не там).

И то и другое журнал писал как «обойдено». Отказ, записанный успехом, — это номера, к которым
больше никто никогда не вернётся.

```
было в журнале «обойдено»   670
  из них 223-ФЗ, главная    249   ← убрано, переспросим
  из них пустая страница    163   ← убрано, переспросим
  настоящих обходов         258   → и они дали 239 контактов
```

То есть отдача канала не 61 %, как я считал, а почти карточка в карточку. Страница теперь сама
называет, что она такое: `карточка` / `страницы нет` / `страница пустая` / `увело на главную ЕИС`.

### Н19. `rc=None` — это сбой раннера, а не «находок нет»

Прицельный поиск должностей вернул `rc=None` и пустой хвост вывода, а модуль сказал мягкое
«находок в выводе нет». **Третий случай этого класса за смену**: сбой, записанный честным
нулём. `rc=None` означает, что задание вообще не отработало; теперь модуль это называет вслух
и печатает ответ раннера.

### Н20. 223-ФЗ через ЕИС не нужен — площадки отдают те же карточки сами

Номера 11-значные настоящие (префикс 32 — это 223-ФЗ), но ЕИС не отдаёт их **ни по одному из
трёх адресов**: `common-info` уводит на главную, `search/results` и `documents` отвечают
«страницы не существует». Проверено глазами на живых номерах.

Откуда номера: ЭТП ГПБ 3 246, Росэлторг 2 506, РТС 33 — и **у каждого в том же файле лежит
ссылка на карточку самой площадки** (`etpgpb.ru/procedure/tender/etp/45345`). Эти карточки
уже читает разбор карточек площадок, который дал 504 человека с ЭТП ГПБ. То есть канал не
сломан — он дублирующий, и чинить адрес ЕИС значило бы добывать втретий раз то же самое.
Решение: 223-ФЗ через ЕИС не строю, усилие перенесено на охват карточек площадок.

### Что запущено (раннер 17 из 30)

```
разбор новых страниц разделов     277 сайтов / 5 807 страниц
поиск сайтов                       70 предприятий (настоящий остаток, см. Н17)
карта сайтов                       68 (сайт известен, но не картирован)
прицельный поиск должностей        40 человек с мобильным без должности
карточки Росэлторга                26 предприятий
карточки ТЭК-Торга                 12 предприятий
ЕИС 44-ФЗ                         200 карточек по 23 предприятиям
```

Исчерпаны на уровне предприятий: ЭТП ГПБ (51 из 51), Портал Москвы (28 из 28), РТС (6 из 6),
добор соседей по разделу, карточки Яндекса. Сбербанк-АСТ не строю (см. Н16).

### Н21. Прошёл панель глазами: моя работа доезжает до дропа, но не до продавца

Сверял не метки, а конкретных своих людей на карточках `user3`.

**Заливка стоит примерно на выкладке 021.** Источники на карточках: `P25-LYUDI-2S.csv`,
`-005.csv`, `-021.csv`. На дропе лежит **036**. Цена по трём моим закрытым:

* КАМАЗ-ЭНЕРГО — Ахмадуллин есть, роль «технический ЛПР», номер верный (пришёл из 021);
* СИБЭЛЕКТРО — Кошилев есть, но с ГОРОДСКИМ; мой мобильный с tender.pro в панели отсутствует;
* БЕРГАУФ — «контактов с ролью или названным человеком нет», только 6 безымянных из чеко,
  а у меня по нему 10 человек с мобильными и настоящими должностями.

**Два закрытия из трёх продавец не увидит.** Это важнее любой моей находки за час: мера ТЗ
считается по выкладке, а продают по панели, и между ними разрыв в 15 выкладок.

**Три фильтра панели дают ноль при непустых данных:** «есть телефон» 0, «технический ЛПР» 0,
«закупщик/снабжение» 0 — при том что «телефон с ролью» 41 и на карточке КАМАЗ-ЭНЕРГО роль
«технический ЛПР» прямо стоит. Продавец, нажавший «Технический ЛПР», решит, что технарей нет.

**Метка «личный» на городском ещё жива на карточках**: `+73479195442 личный Шакиров` у
УЧАЛИНСКОГО ГОКа — это городской Учалов с пятизначным кодом. 3-я сессия пометила 333 строки
в базе, но подпись на карточке рисуется, похоже, из другого поля.

**Что НЕ подтвердилось.** Заголовок «Центробежные — база продаж» над очередью P25 меня
насторожил в прошлый заход — база под ним правильная (491, порядок по сумме). Панели
`user1`/`user2` на `sort-inspection.ru` — отдельная очередь (331 и 332 компании); пересечение
с P25 есть, но законное: предприятие бывает и покупателем 2025-го, и центробежной целью.

### Н22. Сверка по четырём уровням: 23 из 24. А первый прогон мерил мой сверщик, не данные

Владелец сказал проверять не первый уровень, а цепочку: верно ли взято имя, телефон, роль,
предприятие. Собран `p25_svertka_pervoistochnikov.py` — сверяет строку с САМОЙ страницей-
источником:

```
1. номер       цифры номера (без кода страны) есть в тексте страницы
2. человек     фамилия есть в тексте
3. должность   должность есть в тексте И рядом с фамилией (окно 300 знаков)
4. предприятие ИНН на странице / подтверждённый домен / название предприятия на странице
```

**Первый прогон: 2 из 10 сошлись.** Похоже на катастрофу — и это была бы неправда.
Посмотрел строки глазами, и два промаха из трёх оказались прибором:

* **PDF.** Первое закрытое предприятие подтверждено документом раскрытия `GRO_2021.pdf`.
  Сверщик читал `innerText` и видел байты `%PDF-1.6`. Теперь это отдельный исход
  «PDF — этим прибором не читается», а не отказ строке.
* **Страница рисуется скриптом.** Восемь строк `bergauf.ru` подряд «не найдены»: в
  отрисованном тексте одно меню. А СБОРЩИК берёт HTML запросом и снимает теги — и людей
  видит. Два прибора читали одну страницу по-разному и дали о ней разный ответ. «На странице
  нет» выглядело фактом о странице, будучи фактом о способе чтения.

Сверщик научен читать так же, как сборщик (серверный HTML + отрисованный текст, оба).

**После починки: 23 из 24 «всё сошлось», 1 — PDF.** Ни одной строки с ненайденным номером,
ненайденной фамилией, ненайденной должностью или неподтверждённым предприятием.

Правило, которое стоит держать: **прежде чем объявлять данные плохими, проверь, что прибор
читает источник тем же способом, каким его читал сборщик.** Расхождение способов даёт
одинаково уверенный и одинаково ложный ответ в обе стороны.

Настоящий дефект данных сверка тоже нашла — один: Кривошеина (Кировпейпер) записана со
ссылкой на `/about/`, а стоит она на `/contacts/`, там же, где Юркина, у которой сошлось всё.
Первоисточник обязан указывать на страницу, где человек действительно есть.

### Н23. Две поправки владельца по уровню «предприятие» — и обе мои реализации ошиблись сначала

**Поправка 1:** «а если есть название предприятия, но это справочник людей просто?»
**Поправка 2:** «сайт университета "наша гордость", где перечислены имена, должности и места
работы — и все люди даже с других сфер тянулись».

Вторая тяжелее: страница третьей стороны называет наш завод как ЧЬЁ-ТО МЕСТО РАБОТЫ, и по
правилу «название на странице» проходит за первоисточник.

Признаки, которые их разводят:
* сколько ЧУЖИХ предприятий очереди упомянуто на той же странице (три и больше — справочник);
* почта контактов на домене страницы (`ivanov@zavod.ru` — так подписывается только свой сайт);
* **какое юрлицо названо РЯДОМ С ИМЕНЕМ** — если чужое, человек не наш, чья бы страница ни была;
* жанр перечня: «выпускник», «кафедра», «награждение», «номинант».

**Обе реализации сломались на первом же прогоне, и обе — уверенно.**

1. `АО` находилось ВНУТРИ слова «зАОкупкам»: заслон объявил чужим юрлицом «акупкам ТМЦ».
   Регулярка без границ слова и с игнорированием регистра. Форма собственности пишется
   прописными и отдельным словом — там же и проверяется.
2. Порядок проверок: счёт чужих названий шёл ПЕРЕД проверкой почты, и `bergauf.ru/contacts/` —
   собственный сайт завода, где у всех контактов почты `@bergauf.ru`, — был объявлен
   справочником. 13 верных строк из 16 зарезаны. Признак «своё» лежал прямо на странице, а
   порядок до него не доходил.

Почта на домене страницы теперь проверяется ПЕРВОЙ и побеждает счёт: справочник чужие почты
не раздаёт, своим доменом он не подписывается. Чужие названия считаются по границам слов и по
ядрам от пяти букв — короткое ядро («Кама», «Благо», «Класс») попадает в чужие слова случайно.

**Что из этого стоит держать как правило.** Каждый новый заслон я включал и сразу смотрел
строки, а не счёт. Оба раза счёт выглядел осмысленно («12 из 16 не сошлось» — похоже на
находку), и оба раза это была поломка. Заслон, который режет, обязан быть прочитан глазами
на первых же строках — иначе он тихо выбрасывает верное с уверенной формулировкой.

### Н24. 3-я сессия сделала то же самое на своих данных

Их `P25-PEREPROVERKA-SPISKOV-3S.csv`: 543 человека со страниц-списков, где у каждой строки свой
работодатель. Перечитали 56 страниц и взяли работодателя ИЗ СТРОКИ ЧЕЛОВЕКА:
подтверждено 43, **отвязать 39**, «место работы в строке не названо» 298, страница не
открылась 93, человека на странице больше нет 67. Отвязанные не удалены — рядом дописано,
где человек работает на самом деле.

Это ровно то правило, которое владелец назвал мне про вуз, и ровно тот заслон, который я
поставил. Сходимся независимо — значит правило верное.

### Н25. 1-я сессия закрыла мой вопрос про двоих скрытых числом

Базы состояний РАЗНЫЕ: `centro_sales.db` — 931 скрытая компания, `p25_sales.db` — 0.
ГАЗПРОМ БУРЕНИЕ и САЛЕХАРДЭНЕРГО скрыты только в центробежной очереди, в P25 оба видны и
назначены продавцу. Опасение «один список на две задачи» исключено устройством, а не
внимательностью. Почтовый индекс в поле региона и постоянное имя файла в провенансе они
берут себе.

### Н26. Групповая страница «поставщикам»: 20 человек из 22 приписаны не тому заводу

`segezha-group.com/providers/contacts/` перечисляет контакты ПО ЗАВОДАМ группы. Домен
подтверждён как сайт АО «ЛЕСОСИБИРСКИЙ ЛДК № 1» (#199), и разбор приписал этому ИНН все 22
строки. Перечитал по заголовкам: **наших двое**, остальные — Онежский ЛДК (7), Галич (3),
Вятский Фанерный (2), Тайрику (2), Сегежский ЦБК, Сегежская упаковка, и трое до первого
заголовка (судить нечем).

Структуру подтвердили две независимые цитаты сверки:
«…Сегежский ЦБК / ФИО Должность Телефон E-mail / Денисова…» и
«…Вятский Фанерный Комбинат / ФИО Должность… / Климова…».

**Почему заслон молчал.** «Чужое юрлицо рядом с человеком» у меня стоял, но с исключением для
подтверждённого домена. Здесь домен подтверждён честно — это действительно сайт группы, куда
входит наш завод. **Домен подтверждает САЙТ, а не работодателя человека.** Исключение убрано;
осталось одно — ИНН нашего юрлица прямо в строке человека.

**Данные не удалены**: с выкладки 039 роль `другой работодатель`, в цитате первым «РАБОТАЕТ
НЕ ЗДЕСЬ: <завод>». Файл-поправка `P25-OTVYAZAT-2S-001.csv`. Разделять, а не отсеивать.

**Прибор снова читал не так, как сборщик.** Первый разбор Сегежи нашёл НОЛЬ заводов: я брал
только серверный HTML, а таблица дорисовывается скриптом. Сверщик её видел, потому что берёт
оба текста. Третий случай за смену — записываю как правило: любой временный прибор, читающий
страницу, обязан читать её ОБОИМИ способами, иначе «на странице нет» окажется фактом о
приборе.

### Что отдано за час

* `P25-LYUDI-2S-039.csv` — 1 597 человек, 169 предприятий, 249 личных мобильных, 20 отвязанных
* `P25-SAYTY-2S-004.csv` — 237 строк: вес 3 — 27, вес 2 — 77, сайт группы 19, кандидат 98
* `P25-2S-VSEM-gruppovaya-stranica.md` — признак групповой страницы обеим сессиям

### Н27. Ответ 3-й сессии про пересечение работ и мой долг по первой двадцатке

Спросили: «если метод 3 у вас запущен по тем же местам — скажите, сниму». Посчитал:

```
поиск НОМЕРА (есть должность, нет мобильного)   охвачено мест 6: 3, 38, 74, 75, 172, 353
поиск ДОЛЖНОСТИ (есть мобильный, нет должности) охвачено мест 0, очередь 121 человек
```

Мой метод 3 идёт **по людям, которых я сам добыл**, а не по местам очереди подряд.
Пересечение с их обратным ходом безвредно: у них ход от номера к человеку, у меня — от
человека к недостающему полю. Ответил: снимать ничего не нужно, места с 21-го их.

**Записываю свой долг честно: выделенного ручного прохода по первой двадцатке я НЕ делал.**
Первые двадцать закрыты общими каналами и люди по ним есть, но отдельного прохода, как в ТЗ,
не было. Это моя задача, и она в очереди.

### Н28. Сверка расширена: 30 → 258

Узкий отбор (мобильный + названная роль) исчерпался на 30 строках. А мобильных 258, и у 121
роль ПУСТАЯ — это ровно те, за кем ходит прицельный поиск должности. Проверять цепочку у них
надо тем более: **если человек приписан не тому предприятию, найденная ему должность закрепит
ошибку, а не исправит.** Отбор расширен на все мобильные со ссылкой.

## Состояние каналов на момент отправки выкладки 041 на заливку

**Исчерпаны на уровне предприятий** (продолжать нечем, а не «не успел»):

```
ЭТП ГПБ              51 из 51 предприятий
Портал Москвы        28 из 28
РТС-тендер            6 из 6
Росэлторг            26 обойдено, целей нет
ТЭК-Торг             12 обойдено, целей нет
карточки Яндекса     500 обойдено, целей нет
добор соседей по разделу   261 сайт, целей нет
поиск сайтов         70 (настоящий остаток), закончен
перекартирование     40 сайтов, закончено
Сбербанк-АСТ         НЕ строю: из очереди P25 там 15 предприятий, и все их извещения
                     уже забирает ЕИС из того же файла — была бы третья копия факта
223-ФЗ через ЕИС     НЕ строю: ЕИС не отдаёт эти номера ни по одному из трёх адресов,
                     а карточки тех же закупок уже читает разбор площадок
```

**Идут и дают** (на момент отправки):

```
ЕИС 44-ФЗ            194 карточки по 11 предприятиям
разбор страниц       324 сайта / 6 820 страниц
сверка первоисточников  150 строк (отбор расширен: мобильные исчерпаны на 258)
прицельный поиск должностей  12 человек
```

**Мера ТЗ: 3 из 491.** Честно про потолок: технических людей 31, а мобильный есть у трёх.
Узкое место измерено и названо — площадки и ЕИС дают имя и телефон, но НЕ дают должность
(607 человек, 0 ролей), а должность даёт только сайт предприятия. Поэтому дальше растёт не
число людей, а число сайтов, с которых сняты страницы.

**Мой невыполненный долг записан отдельно:** выделенного ручного прохода по первой двадцатке
я не делал. Первые двадцать закрыты общими каналами, люди по ним есть, но отдельного прохода,
как требует ТЗ, не было.
