## enrich_kb_rest.py
**Назначение.** Извлечение фактов о различных типах оборудования (осушители, фильтры, ресиверы, генераторы газов и прочие) из дополнительных листов прайс-листов с помощью Fable API.
**Роль в системе.** обогащение
**Входы.** `katalogi/rest-sheets.json` (файла нет на диске) и `katalogi/kb-enrichment-rest.json` (файла нет на диске). Точное распределение на входные и выходные данные в коде не видно.
**Выходы.** В коде не видно (найденные пути файлов отсутствуют на диске).
**Провайдер.** Вызывает провайдера (использует Fable API), названия моделей в коде не видны.
**Раннер.** не вызывает
**Запуск.** Аргументы командной строки не предусмотрены. Запуск выполняется командой: `python enrich_kb_rest.py`.
**По одному ИНН или списку.** Нет. Признаков работы по ИНН или списку в коде не обнаружено. Чтобы добавить запуск по ИНН или списку, минимально потребуется добавить обработку аргументов командной строки (прием ИНН или пути к файлу со списком) и логику фильтрации обработок внутри функции `worker`.
**Зависимости.** От кого зависит: в коде не видно (файлы `katalogi/rest-sheets.json` и `katalogi/kb-enrichment-rest.json` отсутствуют на диске). Кто зависит от него: в коде не видно.
