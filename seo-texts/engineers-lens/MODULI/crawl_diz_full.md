## crawl_diz_full.py
**Назначение.** Скрипт выполняет краулинг и сбор данных по брендам и моделям оборудования (DIZ). Точные детали функционала в коде не описаны, так как докстрока пустая.
**Роль в системе.** Сбор из внешнего источника.
**Входы.** 
- `diz-brands-models.json` (1229 строк)
- `diz-brands-pages.json` (778 строк)
(Разделение файлов на входные и выходные в коде не видно).
**Выходы.** В коде не видно.
**Провайдер.** Не вызывает.
**Раннер.** Не вызывает.
**Запуск.** `python crawl_diz_full.py --max-time <значение>`
**По одному ИНН или списку.** Нет. Признаки работы по ИНН или списку ИНН отсутствуют. Чтобы модуль мог работать по ИНН, минимально требуется добавить обработку соответствующих аргументов командной строки (например, `--inn` или `--file-list`) и связать их с логикой функции `work`.
**Зависимости.** Связан с файлами `diz-brands-models.json` и `diz-brands-pages.json`. Другие зависимости в коде не видны.
