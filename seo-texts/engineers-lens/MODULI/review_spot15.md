## review_spot15.py

**Назначение.** Проведение выборочной проверки (спот-чека) 15 наиболее посещаемых страниц с помощью набора ревьюеров для оценки качества данных.

**Роль в системе.** Проверка.

**Входы.** `spot15.txt` (14 строк).

**Выходы.** `review-spot15.md` (919 строк).

**Провайдер.** Вызывает провайдера, но конкретные модели в коде не видны.

**Раннер.** не вызывает.

**Запуск.** Аргументы командной строки в коде не видны. Запуск: `python review_spot15.py`.

**По одному ИНН или списку.** Нет. Признаки работы по ИНН или списку ИНН отсутствуют, аргументов командной строки нет. Чтобы модуль мог работать по ИНН, минимально требуется добавить прием аргументов командной строки (ИНН или файл со списком ИНН) и передачу их в функции `main` или `one`.

**Зависимости.** Из путей к файлам видна связь с `spot15.txt` и `review-spot15.md`. Другие зависимости в коде не видны.
