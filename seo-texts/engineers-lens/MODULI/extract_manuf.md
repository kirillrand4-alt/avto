## extract_manuf.py

**Назначение.** Извлечение и очистка фактов о промышленных предприятиях с помощью внешнего провайдера для пополнения базы знаний.

**Роль в системе.** Обогащение.

**Входы.** Входные файлы в коде не видны.

**Выходы.**
* `kb/manuf-facts.json` (1355 строк)

**Провайдер.** Вызывает провайдер. Какая именно модель используется и что запрашивается, в коде не видно.

**Раннер.** Не вызывает.

**Запуск.** `python extract_manuf.py` (аргументы командной строки в коде не представлены).

**По одному ИНН или списку.** Нет. Признаков работы по ИНН или списку в коде нет. Чтобы это добавить, минимально нужно ввести аргументы командной строки для приема ИНН или файла со списком и передавать их в функцию `worker`.

**Зависимости.** От кого зависит: в коде не видно. Кто зависит от него: в коде не видно (формирует файл `kb/manuf-facts.json`, который может использоваться другими компонентами системы).
