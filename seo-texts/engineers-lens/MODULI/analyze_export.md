## analyze_export.py

**Назначение.** Скрипт выполняет анализ выгруженных данных Bitrix и расчет статистики по заполнению свойств.

**Роль в системе.** Проверка.

**Входы.**
* `bitrix/export_file_2ov3pw9ju90trsle.csv` (файла нет на диске)
* `props-fill-table.csv` (184 строк)

*(Точное разделение на входы и выходы в коде не определено, указано по смыслу названий и наличию файлов).*

**Выходы.**
* `export-stats.json` (0 строк)

**Провайдер.** не вызывает.

**Раннер.** не вызывает.

**Запуск.** `python analyze_export.py` (ключи командной строки в коде не обнаружены).

**По одному ИНН или списку.** Нет. В коде отсутствует признак работы по списку компаний и нет параметров командной строки. Чтобы модуль мог работать по ИНН или списку, необходимо добавить передачу аргументов (например, ИНН или пути к файлу со списком) и механизм фильтрации анализируемых данных.

**Зависимости.** Зависит от исходных файлов `bitrix/export_file_2ov3pw9ju90trsle.csv` и `props-fill-table.csv`. Формирует файл статистики `export-stats.json`. Зависимости от других скриптов по путям не видны.
