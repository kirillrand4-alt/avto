## crawl_products.py
**Назначение.** Собирает карточки товаров с веб-страниц (название, ссылка, цена) детерминированным способом без использования LLM. Нужен для индексации продуктов компаний.
**Роль в системе.** сбор из внешнего источника.
**Входы.** В коде не видно.
**Выходы.** 
* `products-index.json` (0 строк).
**Провайдер.** не вызывает.
**Раннер.** не вызывает.
**Запуск.** Аргументы командной строки в коде не обнаружены. Запуск осуществляется вызовом скрипта `python crawl_products.py`.
**По одному ИНН или списку.** Нет. Признаки обработки списка или передача ИНН отсутствуют. Для работы по ИНН или списку необходимо добавить обработку аргументов командной строки и передачу идентификаторов в функции `main` или `one`.
**Зависимости.** В коде не видно.
