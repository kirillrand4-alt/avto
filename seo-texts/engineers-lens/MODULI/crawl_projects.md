## crawl_projects.py
**Назначение.** Модуль предназначен для скачивания и первичной обработки данных о проектах с помощью функций `fetch`, `parse` и `work`. Авторское описание в коде отсутствует.
**Роль в системе.** сбор из внешнего источника.
**Входы.** 
- `projects-urls.txt` (1034 строк)
- `projects-index.json` (15260 строк)
(Направление движения данных в коде не видно, указаны связанные файлы).
**Выходы.** 
- `projects-index.json` (15260 строк)
- `projects-urls.txt` (1034 строк)
(Направление движения данных в коде не видно, указаны связанные файлы).
**Провайдер.** не вызывает.
**Раннер.** не вызывает.
**Запуск.** `python crawl_projects.py --max-time <значение>`
**По одному ИНН или списку.** Нет. Признаков работы по ИНН или списку компаний в коде не обнаружено. Чтобы добавить такой функционал, минимально требуется ввести аргументы командной строки для приема ИНН (или пути к файлу со списком) и реализовать фильтрацию или формирование запросов на основе этих данных.
**Зависимости.** Завязано на работу с файлами `projects-index.json` и `projects-urls.txt`. Других зависимостей в коде не видно.
