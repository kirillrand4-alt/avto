# ЖУРНАЛ 3-й СЕССИИ — задача «база машин + контакты». Начат 09.08

Порядок себе: сперва `drop_client.sh list` и чтение свежего, потом действие. За сегодня я
уже один раз объявила своё, не прочитав чужого, — больше не повторяю.

## Запись 1. Прочитала `ZHURNAL-BAZA-2S.md` (14:30). Их заявку на реестры ПРИНИМАЮ

2-я сессия берёт ОПО, ЭПБ и сверку принадлежности. Не спорю и не дублирую: у неё по P25
уже собран корпус с полями `tipy_mashin`, `sreda_dokazana`, `marki`, и четыре признака
принадлежности домена с экзаменом 22/22. Мои `epb_*` скрипты отдаю ей как есть:

```
git show origin/claude/procurement-file-delivery-guxgbq:seo-texts/epb_nashi_mashiny.py
git show origin/claude/procurement-file-delivery-guxgbq:seo-texts/epb_shirokiy.py
git show origin/claude/procurement-file-delivery-guxgbq:seo-texts/epb_sroki.py
```

Из моего сбора в `fact` уже лежит 3 385 строк с меткой «реестр ЭПБ, сбор 3-й сессии» —
берите как вход, чтобы не ходить по второму разу.

## Запись 2. Их таблицу `mashiny` подписываю и прошу ДВЕ колонки

Хранилище нужно, и их схема верная. Одна правка, и она из прямого указания владельца:
**источники НАКАПЛИВАЮТСЯ, а не заменяются.** Мы это уже теряли: из 773 пар «ИНН + номер»
двумя независимыми разборами подтверждены были 186, и все схлопывались в один ярлык —
подтверждённое дважды становилось неотличимо от подтверждённого однажды.

```
mashiny(inn, chto, klass, sreda, marka, kolichestvo,
        istochnik, ssylka, citata, data_nablyudeniya, sila,
        istochniki   TEXT,     -- все источники через « | », накопительно
        istochnikov  INTEGER)  -- их число
```

`sila` их: сильное / среднее / слабое — принимаю дословно.

## Запись 3. Что беру я, и почему именно это

**КАНОН — единый словарь.** Три сессии = три линейки, мы это проходили сегодня трижды.
Канон должен быть один файл, подписанный всеми. Собираю:

```
машины     компрессор, турбокомпрессор, центробежный компрессор/нагнетатель, газодувка,
           воздуходувка, бустер, винтовой/поршневой компрессор, ресивер
установки  компрессорная станция, ВРУ, азотная/кислородная станция, генератор азота,
           генератор кислорода, МКС, осушитель воздуха
среды      сжатый воздух, сжатый газ, пневматика, азот, кислород
модели     из slovar_modeley (2с) + slovar_marok (3с) + klassify_marki для незнакомых
ОКПД2      кандидаты: 28.13.2x (компрессоры), 20.11 (газы промышленные: азот, кислород),
           28.25.14 (очистка газов), 33.12.x (ремонт). Коды ПРОВЕРЮ по классификатору
           ЕИС, а не возьму из головы
заслоны    «турбокомпрессор» рядом с «кондиционер|трактор|сельхоз|автотранспорт|стартер»
           — НЕ наша машина (на первых 8 фактах ИНН 0245022178 это 8 из 8 ложных);
           границы слова обязательны — уже платили за «мост» внутри «недвижимости»,
           «АНО» внутри «Иванов», «МЕТР» внутри «параметр»
```

Почему это моё: у меня мера машины с пробой 0 провалов из 16 и три оплаченных заслона.

**ЗАКУПКИ И B2B по канону.** Никем не заявлено, а владелец назвал b2b прямо.

```
ОКПД2 в ЕИС      сейчас НЕ ИСПОЛЬЗУЕТСЯ ВООБЩЕ: грепом по «окпд|okpd|okpd2» одна строка
                 на весь seo-texts. Это ключ к закупкам без слов — по коду, а не по
                 названию предмета
площадки         ЭТП ГПБ, Tender.pro, Росэлторг, ТЭК-Торг, РТС, Сбер-А, B2B-Center,
                 Фабрикант, Портал Москвы. Скрипты `*_po_ocheredi` — файлы 2-й сессии;
                 2с, если считаете площадки своими, скажите, отдам без спора
ТО / ЗИП / масло закупка ремонта и запчастей доказывает ПАРК лучше покупки новой машины
```

**ТЕКСТЫ, ВАКАНСИИ, НОВОСТИ** — моё по прежнему распределению, у меня же и починки:
`col_hh` с токеном (0 → 88 items, 58 работодателей) и заслон на оболочку в `fetch_article`
(google отдавал 20 000 знаков CSS, карточка 44-ФЗ — 19 999 знаков рассказа ЕИС о себе).

## Запись 4. 1-й сессии — контакты целиком

Задача 3 владельца («обогатить контактами, максимально результативно») — ваша, у вас
шкала ролей, турникет и панель. Я отдаю вход: работодатели из hh (58 за один заход,
из них 45 по запросам, где машина названа в самом запросе) и людей из вложений ЕИС.

Числа канала сайта, чтобы вы не тратили на него силы: из 4 556 компаний с сайтом человек
с должностью и мобильным — 34, это 0,7 %. Сайт даёт общий адрес, это его потолок, а не
дефект.

## Запись 5. Мера «полноты», чтобы слово «полная база» стало числом

Их единицу принимаю: предприятий с ДОКАЗАННОЙ машиной И доказанным техническим контактом.
Добавляю две строки, которые меряют полноту, а не только качество:

```
ПОКРЫТИЕ    сколько ИНН имеют хоть одно доказательство машины (сейчас: 990 по сигналам,
            и это НЕ то же, что 32 497 фактов в centrifugal.db — базы разные, единицы
            разные; свести по ИНН обязана одна сессия, беру на себя)
ГЛУБИНА     сколько из них с НАЗВАННОЙ маркой/моделью (можно писать про их машину)
ДВОЙНОЕ     сколько подтверждено ДВУМЯ независимыми источниками (колонка istochnikov > 1)
```

Первое, что сделаю числом: свести `enrich.db/signals` (990 ИНН с машиной) и
`centrifugal.db/fact` (32 497 фактов) по ИНН. Пока не сведено — у нас три линейки на один
предмет, и «полная база» непроверяема.

## Запись 6. Общие ресурсы — замок в силе

`seen_news`, квота провайдера, пул xmlriver, `enrich.db`, `sender.db`, dadata. Перед
запуском — строка в `ZAMOK-obshchie-resursy.md`. Ничего на общих не запускаю до ответов.

— 3-я сессия, 9 августа
