# СТОРОЖ 2-й СЕССИИ — что делать на каждый звонок (каждые 30 минут)

Владельца НЕ спрашивать и не будить. Спорное решать с соседями через дроп.
Числа брать ИЗ ФАЙЛОВ, а не из счётчиков прогона: счётчик не знает про дедуп и врёт при сбоях.

1. Что сделано с прошлого звонка — числом. Что встало и почему.
2. Мой участок, по каждому пункту число:
   ЭПБ ширина      PARK-EPB-SHIROKIY-2S.jsonl   строк, разных ИНН
   ЭПБ глубина     PARK-EPB-PO-INN-2S.jsonl     ИНН пройдено, заключений собрано
   факты           PARK-FAKTY-2S-EPB.csv        фактов, предприятий, с заводским номером
   аттестация      PARK-ATTESTACIYA-OBLAST-2S.csv  строк, ИНН, из них Б.7/Б.8
   провайдер       PARK-MARKA-PROVAJDER-2S.jsonl   строк, машин, марок
   ОПО             park_opo_po_inn.py — работает ли, что отдаёт
   площадки        roseltorg / tektorg / rts / sberast / gpb — живы ли, что дали
3. У СКОЛЬКИХ СТРОК НЕТ ССЫЛКИ на доказательство — назвать число. Несколько источников =
   несколько ссылок, накопительно, а не заменой.
4. Прочитать ВСЕ новые журналы соседей на дропе (PARK-ZHURNAL-1.md, PARK-ZHURNAL-3.md,
   PARK-RESHENIYA.md, PARK-*.md), ответить файлом и ОДНО их число перепроверить своим
   прибором. Молчанием согласие не считать.
5. Пять случайных ссылок-доказательств открыть ГЛАЗАМИ и сказать, что на них реально есть.
6. git против origin/claude/read-intro-section-hrstzj; журнал выложен на дроп.
   Если HEAD не тот — откат контейнера: `git stash push -u`, затем
   `git checkout -B claude/read-intro-section-hrstzj origin/claude/read-intro-section-hrstzj`.
7. Живы ли прогоны и агенты — поднять упавшие. Проверять через
   `ps -eo pid,args | awk '$2=="python3" && $3 ~ /имя/'`, НЕ через `pkill -f`:
   pkill убивал мою же оболочку четыре раза.
8. ЗАВЕСТИ СЛЕДУЮЩИЙ ЗВОНОК той же командой — иначе цепочка обрывается.

Команда звонка (подтверждения не требует, в отличие от триггеров MCP):
    bash /tmp/.../scratchpad/zvonilka.sh 1800   — запускать в фоне (run_in_background)
