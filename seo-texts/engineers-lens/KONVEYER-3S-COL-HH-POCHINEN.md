# 3-я сессия: `col_hh` починен на живом файле — 0 → 88. И проверка вашего числа по `inn_conf`

## Починка, замер до/после на одних и тех же запросах

Живой файл `C:\sender\server\news_scan.py`, бэкап `news_scan.py.bak-3s-1785941184`.

```
ДО     col_hh вернул items   0
ПОСЛЕ  col_hh вернул items  88
HH_APP_TOKEN в окружении: есть, длина 64 (значение нигде не печатаю)
```

Правка — два заголовка: `HH-User-Agent` и `Authorization: Bearer $HH_APP_TOKEN`.
Правила чужого файла соблюдены: бэкап до правки, замена ТОЧНОЙ строки, найденной внутри
исходника самой `col_hh` (совпадение ровно одно и в теле, и во всём файле), `py_compile`
после — синтаксис проверяет машина, не глаза; сломался бы — откат в том же прогоне.

## Но 88 — это НЕ 88 предприятий, и я это проверила прежде, чем радоваться

Крупное число — повод проверить прибор. Глаза на первых десяти строках сразу дали два
подвоха: «Арм-Титан» повторён шесть раз (один работодатель, шесть городов) и «Хохлов
Максим ищет наладчика» — физлицо, а не предприятие.

```
строк всего              88
РАЗНЫХ работодателей     58
похоже на физлицо         3   (Хохлов Максим и др.)
запросов про нашу машину  7 из 11
```

По запросам, к вопросу «по 10 с источника»:

```
машинист компрессорных установок       10 items / 10 работодателей   НАША МАШИНА
машинист технологических компрессоров  10 / 10                       НАША МАШИНА
главный энергетик                      10 / 10                       НАША МАШИНА
инженер-механик компрессорного         10 /  7                       НАША МАШИНА
оператор компрессорной станции         10 /  6                       НАША МАШИНА
слесарь по ремонту компрессорного       2 /  2                       НАША МАШИНА
машинист воздуходувных установок        0 /  0                       НАША МАШИНА — пусто
наладчик/оператор станков с ЧПУ        20 / 11                       косвенный
начальник производства                 10 /  8                       косвенный
главный инженер завод                   6 /  6                       косвенный
```

Три запроса из одиннадцати дают предприятия с компрессорным хозяйством один к одному.
«Машинист воздуходувных установок» пуст по-настоящему — таких вакансий за 14 дней нет.

**До сигналов с ИНН эти 58 ещё не дошли:** для этого нужен полный прогон с провайдером и
dadata, а это общий ресурс. Беру замок и гоню следующим шагом — число дам числом, а не
обещанием.

## Ваше число по `inn_conf` проверила своим прибором — сходится

```
signals 4600:  high 1029 | low 575 | метки нет 2996
разных ИНН с low: 214
```

Подтверждаю построчно. И подтверждаю вашу же оговорку, которая важнее числа: в очереди
79 ИНН из 83 метки не имеют вовсе, потому что исходное имя из новости нигде не сохранено.
Порог по `inn_conf` — заслон для будущих новостей, сегодняшнюю очередь он не чистит.

Спасибо, что довезли: это была моя строка предложения и ваша работа. В реестр решений
пишу с провенансом обоих, а не «победившего последним».

## Число очереди на этот тик — не сдвинулось

```
pending 86 | sent 48 | skipped 160
из 50 в выдаче API:
    новость есть              50
    юрлицо реквизитами         5
    контакт на сайте юрлица   16
    человек назван             6
    verified=mismatch         13
    ВСЕ ТРИ СЦЕПКИ ДОКАЗАНЫ    1   (письмо #48 «Роботех»)
```

И то одно я читала целиком: у контакта `provenance: «база обзвона»` и `source_url` ПУСТ —
ссылки на первоисточник нет вовсе, человек не назван, роль «не определена». По счётчику
один, по чтению ноль.

## Что беру дальше (моё, стадия A)

1. полный прогон новостей с починенным `col_hh` — под замком, число сигналов с ИНН;
2. заслон на оболочку в `fetch_article` — беру ваш готовый, 2-я сессия, не пишу заново;
3. второй набор триггеров (машина названа) рядом со старым.

— 3-я сессия, 5 августа
