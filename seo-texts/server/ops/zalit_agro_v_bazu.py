# -*- coding: utf-8 -*-
"""Завести агро-компании сбора Чеко в базу рассылки под Meyer.

Владелец 06.09: «пишем всем, но сначала от 30 млн будем писать».

ДВА МЕСТА, И ОБА ОБЯЗАТЕЛЬНЫ. Получатель в панели нужен, чтобы компания
попала в отбор генерации. Строка в enrich.companies нужна, чтобы генератор
УВИДЕЛ факты: он берёт их оттуда, а не из карточки получателя. На
белорусах этот шаг пропустили — карточки были заполнены, компаний в
обогащении не было, и генератор писал вслепую. Судья снял сорок писем из
пятидесяти двух за выдуманный ассортимент.

ЧТО КЛАДЁМ КАК ФАКТЫ. Только проверяемое: название и ОГРН из реестра,
ОКВЭД с расшифровкой прямо из карточки Чеко, выручка с годом отчётности,
среднесписочная численность, регион по коду ИНН. Ничего не додумываем —
у этих хозяйств нет сайтов, и любая догадка вылезет выдумкой.

Запуск: python zalit_agro_v_bazu.py [--primenit] [порог=30000000]
"""
import io
import json
import os
import re
import sqlite3
import sys
import time
from collections import Counter

sys.path.insert(0, r"C:\sender")
sys.path.insert(0, r"C:\sender\server")
from sender.dtos import RecipientIn                            # noqa: E402
from sender.store import Store                                 # noqa: E402
import enrich_db as EDB                                        # noqa: E402

ЖУРНАЛ = r"C:\sender\server\checko_finansy.jsonl"
ЗАЛИТО = r"C:\sender\_ops\agro-zalito.jsonl"
ГРУППА = "Агро зерно 2026"
ИСТОЧНИК = "чеко-агро-2026"
ПРИМЕНИТЬ = "--primenit" in sys.argv or "--apply" in sys.argv
ПОРОГ = 30_000_000
for а in sys.argv[1:]:
    if а.startswith(("порог=", "porog=")):
        try:
            ПОРОГ = int(а.split("=", 1)[1])
        except ValueError:
            pass

АДРЕС = re.compile(r"[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}")
РЕГИОНЫ = {
    "01": "Адыгея", "02": "Башкортостан", "03": "Бурятия", "04": "Алтай",
    "05": "Дагестан", "06": "Ингушетия", "07": "Кабардино-Балкария",
    "08": "Калмыкия", "09": "Карачаево-Черкесия", "10": "Карелия",
    "11": "Коми", "12": "Марий Эл", "13": "Мордовия", "14": "Якутия",
    "15": "Северная Осетия", "16": "Татарстан", "17": "Тыва",
    "18": "Удмуртия", "19": "Хакасия", "20": "Чечня", "21": "Чувашия",
    "22": "Алтайский край", "23": "Краснодарский край",
    "24": "Красноярский край", "25": "Приморский край",
    "26": "Ставропольский край", "27": "Хабаровский край",
    "28": "Амурская область", "29": "Архангельская область",
    "30": "Астраханская область", "31": "Белгородская область",
    "32": "Брянская область", "33": "Владимирская область",
    "34": "Волгоградская область", "35": "Вологодская область",
    "36": "Воронежская область", "37": "Ивановская область",
    "38": "Иркутская область", "39": "Калининградская область",
    "40": "Калужская область", "41": "Камчатский край",
    "42": "Кемеровская область", "43": "Кировская область",
    "44": "Костромская область", "45": "Курганская область",
    "46": "Курская область", "47": "Ленинградская область",
    "48": "Липецкая область", "49": "Магаданская область",
    "50": "Московская область", "51": "Мурманская область",
    "52": "Нижегородская область", "53": "Новгородская область",
    "54": "Новосибирская область", "55": "Омская область",
    "56": "Оренбургская область", "57": "Орловская область",
    "58": "Пензенская область", "59": "Пермский край",
    "60": "Псковская область", "61": "Ростовская область",
    "62": "Рязанская область", "63": "Самарская область",
    "64": "Саратовская область", "65": "Сахалинская область",
    "66": "Свердловская область", "67": "Смоленская область",
    "68": "Тамбовская область", "69": "Тверская область",
    "70": "Томская область", "71": "Тульская область",
    "72": "Тюменская область", "73": "Ульяновская область",
    "74": "Челябинская область", "75": "Забайкальский край",
    "76": "Ярославская область", "77": "Москва", "78": "Санкт-Петербург",
    "79": "Еврейская АО", "83": "Ненецкий АО", "86": "Ханты-Мансийский АО",
    "87": "Чукотский АО", "89": "Ямало-Ненецкий АО", "91": "Крым",
    "92": "Севастополь", "93": "ДНР", "94": "ЛНР",
    "95": "Запорожская область", "99": "Москва",
}

# --- что известно из карточек Чеко --------------------------------------
данные = {}
for с in io.open(ЖУРНАЛ, encoding="utf-8", errors="replace"):
    с = с.strip()
    if not с:
        continue
    try:
        z = json.loads(с)
    except Exception:                                          # noqa: BLE001
        continue
    if z.get("сбой"):
        continue
    и = str(z.get("inn") or "")
    if и:
        данные[и] = z

c = sqlite3.connect("file:%s?mode=ro" % r"C:\sender\enrich.db", uri=True,
                    timeout=180)
реестр = {}
for и, о, н, к in c.execute(
        "SELECT inn, ogrn, name_short, okved_main FROM requisites "
        " WHERE src='checko-sbor-agro'"):
    реестр[str(и)] = (str(о or ""), str(н or ""), str(к or ""))
уже_в_обог = {str(r[0]) for r in c.execute("SELECT inn FROM companies")}
c.close()

store = Store(r"C:\sender\sender.db")
занятые = set()
with store._lock:
    for (и,) in store._conn.execute(
            "SELECT inn FROM recipients WHERE inn IS NOT NULL").fetchall():
        занятые.add(str(и))

счёт = Counter()
к_заливке = []
for и, (огрн, имя, квэд) in реестр.items():
    z = данные.get(и)
    if not z:
        счёт["карточка Чеко не получена"] += 1
        continue
    почты = [a.lower() for a in АДРЕС.findall(str(z.get("emails_checko") or ""))]
    if не_почты := (not почты):
        счёт["почты нет"] += 1
        continue
    try:
        выр = int(str(z.get("revenue_rub") or "0") or 0)
    except ValueError:
        выр = 0
    if not выр:
        счёт["выручка не известна — во вторую очередь"] += 1
        continue
    if выр < ПОРОГ:
        счёт["выручка ниже порога"] += 1
        continue
    # ТОЛЬКО ВЫРАЩИВАНИЕ ЗЕРНОВЫХ (владелец 06.09). Письмо написано под
    # хозяйство, которое растит зерно и подрабатывает его у себя: 01.11 и
    # 01.11.1. Молочный скот (01.41) и смешанное хозяйство (01.50) под
    # этот текст не подходят, им нужен свой.
    if not str(квэд or "").startswith(("01.11", "01.12", "01.13")):
        счёт["не зерновые (01.41/01.50 и прочее)"] += 1
        continue
    if и in занятые:
        счёт["получатель уже есть"] += 1
        continue
    занятые.add(и)
    расшифровка = str(z.get("okved_main_checko") or квэд or "")
    регион = РЕГИОНЫ.get(и[:2], "")
    extra = {
        "gruppy": [ГРУППА],
        "istochnik": "сбор Чеко по ОКВЭД, карточка компании",
        "ogrn": огрн or None,
        "activity": расшифровка or None,
        "okved": квэд or None,
        "revenue_rub": выр,
        "revenue_year": z.get("fin_god") or None,
        "ssch": z.get("ssch") or None,
        "phone": str(z.get("phones_checko") or "") or None,
        "pochty_eshchyo": почты[1:] or None,
        "city": регион or None,
        "city_source": "код региона в ИНН",
        "site": str(z.get("site_checko") or "") or None,
        "bez_sayta": not str(z.get("site_checko") or "").strip(),
    }
    к_заливке.append((RecipientIn(
        email=почты[0],
        domain=почты[0].split("@", 1)[1],
        inn=и,
        company_name=имя,
        segment="meyer",
        source=ИСТОЧНИК,
        region=регион or None,
        tz="Europe/Moscow",
        extra=extra), расшифровка, выр, z))
    счёт["К ЗАЛИВКЕ"] += 1

коды = Counter(str(р.extra.get("okved") or "?")[:7] for р, _, _, _ in к_заливке)
нет_в_обог = sum(1 for р, _, _, _ in к_заливке if р.inn not in уже_в_обог)

итог = ["=" * 78,
        "=== СВОДКА: ЗАЛИВКА АГРО В БАЗУ ===",
        "режим: %s; порог %s руб; группа «%s»"
        % ("ПРИМЕНЕНО" if ПРИМЕНИТЬ else "СУХОЙ ПРОГОН",
           format(ПОРОГ, ",d"), ГРУППА), ""]
for к, в in счёт.most_common():
    итог.append("   %-40s %6d" % (к, в))
итог += ["", "по кодам:"]
for к, в in коды.most_common(8):
    итог.append("   %-10s %6d" % (к, в))
итог += ["", "из них нет в enrich.companies: %d" % нет_в_обог]

if ПРИМЕНИТЬ and к_заливке:
    db = EDB.EnrichDB()
    ф = io.open(ЗАЛИТО, "a", encoding="utf-8")
    залито = ошибок = в_обог = 0
    for р, расшифровка, выр, z in к_заливке:
        try:
            rid = store.upsert_recipient(р)
        except Exception as ex:                                # noqa: BLE001
            ошибок += 1
            continue
        # факты в обогащение — с повтором на занятой базе
        for _п in range(6):
            try:
                db.upsert_company(р.inn, name=р.company_name,
                                  division="meyer",
                                  okved=str(р.extra.get("okved") or ""),
                                  activity=расшифровка,
                                  best_email=р.email,
                                  revenue_rub=выр)
                в_обог += 1
                break
            except Exception as ex:                            # noqa: BLE001
                if "locked" not in str(ex) and "busy" not in str(ex):
                    break
                time.sleep(2.0)
        залито += 1
        ф.write(json.dumps({"id": rid, "inn": р.inn, "почта": р.email,
                            "имя": р.company_name, "выручка": выр},
                           ensure_ascii=False) + "\n")
        if залито % 200 == 0:
            ф.flush()
            os.fsync(ф.fileno())
            print("   залито %d из %d" % (залито, len(к_заливке)), flush=True)
    ф.flush()
    os.fsync(ф.fileno())
    ф.close()
    итог += ["", "залито получателей: %d (ошибок %d)" % (залито, ошибок),
             "записано в обогащение: %d" % в_обог]
    группы = store.recipient_groups().get("по_id") or {}
    итог.append("в группе «%s»: %d"
                % (ГРУППА, sum(1 for g in группы.values() if ГРУППА in g)))

итог += ["", "--- пять примеров ---"]
for р, расшифровка, выр, _ in к_заливке[:5]:
    итог.append("   %-13s %-30s %-26s %12s руб"
                % (р.inn, str(р.company_name)[:30], р.email[:26],
                   format(выр, ",d")))
    итог.append("      факты: %s | регион %s"
                % (расшифровка[:60], р.region))
print("\n".join(итог))
