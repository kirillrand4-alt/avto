# -*- coding: utf-8 -*-
"""Есть ли на сервере запасное чтение Status из ВЕРХНЕГО заголовка.

Эту часть правки я дописал позже основной - после того, как на ней упали
два прежних теста (в их письмах Status лежит не в части
message/delivery-status, а в заголовке верхнего уровня). Без неё жёсткая
отбивка может остаться без кода.
"""
import io

путь = r"C:\sender\sender\dsn.py"
т = io.open(путь, encoding="utf-8").read()
куски = {
    "структурная проверка": "def dsn_po_strukture",
    "запасной Status из верхнего заголовка": 'msg.get("Status", "")',
    "проверка формата кода": r'[245]\.\d{1,3}\.\d{1,3}',
}
for имя, к in куски.items():
    print(f"{имя}: {'ЕСТЬ' if к in т else 'НЕТ'}")
