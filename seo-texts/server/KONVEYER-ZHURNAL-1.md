# Журнал 1-й сессии по конвейеру «новость → компания → письмо»

Отдельный журнал под новую общую задачу владельца: конвейер должен искать
подходящие новости, правильно вычленять компанию и правильно определять
приоритетную роль для обращения. Работа втроём, распределение и протокол —
в `KONVEYER-VSEM-TROIM-start.md`. Моя стадия — **роль**.

## 5 августа. Замер стадии «лучший ролевой контакт»

Прочитал `docs/15-konveyer-novost-kompaniya-pismo.md` и код
(`enrich_contacts._best_by_role`, `_ROLE_RANK`, `ai_quota._request`), после чего
замерил живую `C:\sender\enrich.db`.

    контактов в enrich.emails                20 398
       общий 9 130 | роль пустая 4 397 | снабжение 1 872 | продажи 1 596
       приёмная 1 417 | директор 693 | ГЛ.ИНЖЕНЕР 259 (1,3 %)
    компаний с best_email и не конкурент      4 556
       письмо уходит на СЛАБУЮ роль           3 643   (80 %)
    домен почты разошёлся с доменом сайта     1 566
    сверка только по названию (тёзка)         1 073
    контакт со страницы агрегатора            4 377   (21 %)
    битых адресов-первоисточников             2 103   (в т.ч. 124 на саму новость)

**Вывод, который перевернул мою же первую догадку.** Я шёл править ранжирование
ролей. Оно оказалось уже правильным — технари сверху, решение владельца 29.07.
Считать надо было не порядок, а наполнение: технической роли нет у 98,7 %
контактов. Письмо уходит на `info@` не потому, что ранжирование ошиблось, а
потому что технаря никто не нашёл.

## Реестр технических ЛПР

Из тех 3 643 писем на слабую роль технический человек УЖЕ ИЗВЕСТЕН у 461:
309 — в самой `enrich.people`, 152 — в базах обзвона.

Собрал единый реестр `C:\sender\tehlpr.db` (таблица `tehlpr`, индекс по `inn`),
копия для глаз — `TEHLPR-REESTR.csv` на дропе.

    людей 3 699 на 1 048 ИНН
       с почтой 277, с личным мобильным 106
       источник: живая ссылка не с агрегатора 3 652, агрегатор 0, без ссылки 47

Схему `enrich.db` не трогал: конвейер — работа другой сессии. Отдельный файл
читается тремя строками и так же легко выбрасывается.

**Ошибка, пойманная на себе при сборке.** Первый сухой прогон отрапортовал
«с не-агрегаторным источником 3 699 из 3 699» — победа над собственным
знаменателем: пустая ссылка попадала в корзину «хороший источник». Разделил на
три корзины, стало 3 652 / 0 / 47. И телефоны брал из `person.phone`, где они
почти не заполнены; канон ходит в `contact` — после правки личных мобильных
90 → 106.

## Два чужих заслона, применённых к себе: 13 → 10

2-я сессия прислала признак стороны, 2-я и 3-я независимо — работодателя из
строки человека. Применил оба к своим закрытиям по P25:

    было каноном                                    13
    минус «их сторона» (сервис/претензии/гарантия)   −2
    минус «другой работодатель»                      −1
    -----------------------------------------------------
    ЗАКРЫТО ЧЕСТНО                                   10 из 491

Помечено 7 строк (`другой работодатель` / `их сторона`, причина первой строкой
в поле подтверждения), не удалена ни одна. Обратное чтение из базы: 7.

Проверка чужих замечаний ПРИБОРОМ, а не чтением кода, дала три разных ответа
на три пункта одной записки:

* `_ROLE_RANK` со снабжением на нуле — **устарело**: живой серверный файл уже
  технический сверху, с фолбэком `_canon_role`, неизвестная роль = 13 (лучше
  «общего» 14), а не 9;
* падежи «закупок» — **канон конвейера их ловит**, все три образца дают
  «снабжение/закупки». Вывод верен для её собственного разбора, не для конвейера;
* «технический сервис» под видом инженера — **подтвердилось**: живой канон
  превращает «руководитель направления технического сервиса» в «инженер
  (не главный)». Заодно «энергетик» поднимается до «гл.энергетик».

Разница между «прочитал код» и «спросил прибор» здесь стоила бы двух лишних
правок и одной пропущенной.
