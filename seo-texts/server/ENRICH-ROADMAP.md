# 🗺️ ЗАДАЧИ НА ЗАВТРА (сводка сессии 2026-07-23, приоритет сверху)

## 🔴 БЛОКЕР ИНФРАСТРУКТУРЫ (первым - КОРЕНЬ найден 2026-07-23)
0. DOLPHIN_TOKEN ПРОТУХ = корень ВСЕХ дельфин-бед: dolphin_list 401, профили 500 при старте,
   VK 500, смена прокси 401 (Remote API Unauthorized). ОДИН токен блокирует всю правую
   половину дерева (VK/hh/справочники/2ГИС/JS-сайты).
   ФИКС: обновить DOLPHIN_TOKEN в runner-secrets.env (Dolphin Anty -> Настройки -> API ->
   новый Bearer-токен). После: (1) оп dolphin_set_proxies раскидает 78 прокси (на дропе
   dolphin-proxies.txt), (2) профили стартуют, (3) VK/hh/справочники поедут.
   Прокси (78 шт) НЕ виноваты - просто не смогли назначить без токена.

## 🟢 ГЛАВНАЯ СБОРКА (самое ценное, БЕЗ дельфина - прямой доступ с сервера)
1. MULTI-ЭТП КОНТАКТЫ по 4 рабочим площадкам (разведаны и подтверждены на живых карточках):
   ЕИС (есть) + ЭТП ГПБ + ТЭК-Торг(Роснефть) + Tender.pro. Поиск по ИНН -> карточка ->
   ФИО+email+телефон закупщика (source etp:<площадка>). АРХИВ по всем (не только активные) +
   фильтр свежести (>3 лет НЕ берём) + дедуп по человеку. Горячий флаг если предмет закупки
   = компрессор/фильтр/воздуходувка. Приоритет сверху вниз по выручке. xmlriver 25р/1000 - дёшево.
2. ФИНАЛЬНЫЙ ЕДИНЫЙ СКОРИНГ + выгрузка продажникам: pressure_equip(3)/rtn_opo(2)/hh/ЕИС-ЭТП/
   контакты -> таблица с провенансом каждого поля (source_url у каждого). Конечная точка.

## 🟡 ВЛИТО В КОД, НУЖЕН ЧИСТЫЙ ЗАМЕР % (замер-20 отработал, дал реальные email)
3. Обогащение enrich_one: staff-краул + резолв ИНН(склонения) + ЕГРЮЛ-email + ЕИС-контакт +
   SMTP-отсев мёртвых + кэш ИНН-сайт + карточка Я + 2-й уровень обхода + site-block-фикс.
   ВСЁ влито (флаги zakupki_check/smtp_check). Замер-20: сайты+email находятся (gk-smk.ru,
   avtoban.ru). НУЖНО: догнать чистый замер % покрытия (coverage_probe, параллельный, 423с/20).
4. Спот-чек 92 спасённых ингестером лидов (правильные ли ИНН).

## 🟠 ЖДЁТ ДЕЛЬФИНА / РЕШЕНИЯ
5. VK-группы (СРЕДНИЙ бизнес - владелец: у крупных конкуренция выше). Токен добыт (app включён),
   IP у всех профилей общий -> ретрай по профилям готов. ЖДЁТ живого дельфина (сейчас 500).
6. hh ИНВЕРСИЯ вакансия->компания: API hh закрыт целиком (forbidden всем), но hh.ru-сайт
   открыт -> парсить через дельфин с ожиданием JS-отрисовки (SPA). Генерация горячих лидов.

## 🔵 БЭКЛОГ (идеи владельца, дёшево, не срочно)
7. Новости ОТ КОМПАНИИ: «<компания> модернизир OR построит...» xmlriver по базе сверху вниз
   по выручке. ~4025р полный проход (25р/1000). Комплементарно неводу.

---

# Обогащение контактов: дорожная карта и статус (2026-07-23)

## ✅ Внедрено и проверено на живых данных
- Резолв ИНН со склонениями (dadata-варианты + защита от тёзок + ретрай на 429). Тест Т1 ✅.
- staff-краул + роли + source_url у каждого email. Тест Т2 ✅ (prokompressor.ru: 16 стр, 8 email со staff).
- ЕИС-закупки: контакт закупщика (ФИО+email+тел) из карточек. Живой пример Водоканал СПб ✅.
- phone-match: перенос email внутри групп одного телефона базы 161к. 3479 записано (source
  phone-match:<ИНН>, роль «общий (phone-match)»). ОТКАТ: op phone_match_rollback.
- Чистка dzen/контент-платформ из enrich.db (6 вычищено). Блок-лист расширен.

## ✅ Внедрено, ждёт живого замера покрытия
- ЕГРЮЛ-email (dadata findById, source egrul:dadata) — в новостях, ингестере, хвосте без сайта.
- VK-группы компаний (source vk-group) — op vk_probe.
- Кэш ИНН->сайт (enrich.db, TTL 90д, site_source cache:enrich-db, no_site_cache off).
- Я.Карты по mapurl + 2ГИС/zoon рендером браузера (source maps:yandex / dir).
- Второй уровень обхода (подстраницы офисов/отделов) + приоритет закупочных страниц.
- site-block ДЫРА закрыта: закрытый капчей сайт рендерится браузером, а не бросается.
- Карточные контакты — с подписью источника (serp-card:yandex) и ролью (ниже ЛПР).

## ⚠️ Известные проблемы транспорта
- hh.ru API отдаёт 403 на IP сервера (ДЦ). Новостной hh идёт через мобильный прокси.
  ЧИНИТЬ: маршрутизировать find_hh_compressor через прокси-пул news_scan ИЛИ дельфин.

## 🔜 Следующие большие сборки (одобрено владельцем «на всех компаниях»)
1. zakupki_mass — ЕИС по ВСЕМ компаниям сверху вниз по выручке (source zakupki:eis + горячий
   сигнал если предмет=компрессор/воздуходувка). Код готов, самочейн+стоп-флаг. ЗАПУСТИТЬ.
2. Реестр аттестованных Ростехнадзора (pub.fsa.gov.ru, ~500к записей: ФИО+должность гл.инж/
   гл.энерг/ответств. по ПБ). Поиск по ИНН. ПРОТОТИП ПАРСЕРА -> на все.
3. Декларации промбезопасности (подписант = технический директор). На все.
4. Финальный единый скоринг: pressure_equip(3)/rtn_opo(2)/hh/ЕИС/ЛПР-ФИО -> выгрузка
   продажникам с контактами и провенансом каждого поля. Параллельный слой по всей базе,
   сверху вниз по выручке.

## 🎯 ПОДТВЕРЖДЕНО СКРИНШОТАМИ (2026-07-23): multi-ЭТП контакты = приоритет №1
Проверено с сервера (200 без логина) + владельцем на реальных карточках:
- ЕИС zakupki.gov.ru - работает у нас (zakupki:eis).
- ЭТП ГПБ etpgpb.ru - блок «Сведения об организаторе» БЕЗ логина: ФИО+персональный
  email+телефон+доб. Пример: закупка «фильтры для винтового компрессорного агрегата» ->
  Сысоева Н.В., n.sysoeva@ttu-nn.ru, +7 831 200-01-03 доб.286 (ЛУЧШИЙ формат + горячий
  сигнал по предмету). Плюс частные секции (Газпром бурение/Интер РАО/Газпром нефть) - нет в ЕИС.
- Росэлторг roseltorg.ru - поиск по ИНН есть, НО в карточке процедуры контактов НЕТ,
  только члены комиссии без ролей (проверено владельцем 2026-07-23). Для контактов ЛПР
  БЕСПОЛЕЗЕН - только организатор-компания (не человек). НИЗКИЙ приоритет.
- Сбербанк-АСТ sberbank-ast.ru - в основном 44-ФЗ (госы), тот же контур что ЕИС (владелец
  2026-07-23) -> ДУБЛЬ ЕИС, отдельно парсить смысла нет. НИЗКИЙ приоритет.
- ТЭК-Торг tektorg.ru - контур РОСНЕФТЬ+Интер РАО (топ-цель компрессоров): извещение с
  контактным лицом БЕЗ логина. Пример АО РНПК (Рязанский НПЗ): Лифанова А.В.,
  avlifanova@rosneft.ru, 8-4912-933254 доб.4138. Есть АРХИВНЫЕ + коммерческие торги.
- Tender.pro tender.pro - КОММЕРЧЕСКИЕ тендеры МСБ (нет в ЕИС/госЭТП). Контакт часто в
  СВОБОДНОМ ТЕКСТЕ комментария, не в поле: пример Мария-РА - «обращаться к начальнику
  отдела оборудования Евгению Епишеву, тел.8-923-654-00-34». Парсер: выдирать ФИО+тел+email
  из текста (регекс+провайдер). ГЛУБОКИЙ архив (до 2016) -> ФИЛЬТР СВЕЖЕСТИ (1-3 года,
  старьё пометить stale: человек мог сменить работу).
- Закрыты: RTS-tender(503), OTC(302 логин), Фабрикант(раздел торгов 403).
ВАЖНО (наблюдение владельца): тянуть НЕ только активные, а АРХИВ закупок компании =
вся история контактных лиц за годы (снабженец работает и после закрытия тендера) ->
в разы больше ФИО+контактов на одну компанию.
СБОРКА: единый multi-ЭТП парсер по ИНН -> карточки организатора (ФИО+email+тел) +
горячий флаг если предмет=компрессор/фильтр/воздуходувка. source: etp:<площадка>.

АРХИВ ПО ВСЕМ 6 ПЛОЩАДКАМ (решение владельца 2026-07-23): собираем НЕ только активные,
а всю историю закупок компании - нужен КОНТАКТ снабженца, не активный тендер (снабженец
работает и после закрытия). В разы больше ФИО на компанию. Технические страховки:
1) КАЖДЫЙ контакт с датой закупки + флаг свежести: <=2л=fresh, 2-3г=stale, >3 ЛЕТ = НЕ
   БЕРЁМ ВООБЩЕ (владелец 2026-07-23: снабженец почти наверняка сменился). Видно возраст.
2) ДЕДУП по человеку (email/телефон): один снабженец ведёт десятки закупок -> схлопнуть,
   оставив самую свежую закупку как source_url.
3) Приоритет свежих: есть и свежий и старый контакт -> свежий побеждает, старые запасные.

## ⏸ Отложено (по слову владельца, ждёт его решения/ключей)
- Ключи API 2ГИС и/или Яндекс.Карты (браузерный путь работает, API чище) — ЖДЁМ КЛЮЧИ.
- СБИС / Контур.Фокус (руководители+учредители по ИНН, платно) — лучший ЛПР-канал, ОТЛОЖЕНО.
- Доноры новостей -> автофиды каталога — «потом».
- Обратный поиск телефона (GetContact) — ОТКЛОНЕНО (152-ФЗ, серые данные).

## ❓ Открытый вопрос владельца
- Паспорта готовности к ОЗП: сложность = нет единого реестра (каждый регион публикует у себя,
  часть скан-копиями PDF, СМЭВ юрлицам недоступен). Реализуемо, но по-региональный сбор +
  OCR сканов -> дорого/долго. Аттестации РТН и декларации ПБ дают тех же ЛПР дешевле.

# 📏 ЗАМЕРЫ ИСТОЧНИКОВ ТЕХНИЧЕСКИХ ЛПР (2026-07-29, база продажников 555 ИНН)

Все цифры — с живого прогона по базе, не с одной компании. Порядок по отдаче.

| Способ | Охват прогона | Что дал | Вывод |
|---|---|---|---|
| Страницы руководства на сайте (обход) | 555 | **188 человек в 69 компаниях**, из них 15 гл. инженеров, 4 нач. производства, 2 гл. энергетика | ЕДИНСТВЕННЫЙ источник с объёмом |
| Перечтение кэша новой регуляркой | 201 файл / 2673 стр. | 29 новых людей, 45 телефонов (42 с ролью) | дёшево, повторный обход не нужен |
| Переразметка кэша провайдером | 12 компаний (проба) | 35 телефонов, ВСЕ с ролью | лучший выход на вложенный рубль |
| Страницы руководства из выдачи (xmlriver) | 78 | 52 человека в разборе, но people 188 → 189 | почти всё уже было; оставлен как добор |
| Гриф «УТВЕРЖДАЮ» в документации закупок | 555 | 29 человек, из них **1** технический ЛПР | почти пусто, но стоит копейки |
| Расшифровка инициалов сшивкой (Оськин К.В. → полное имя) | вся база | **2 пары** | эффект переоценён |
| hh: контакт в вакансии | 32 | 8 вакансий, **1 контакт** (и тот кадровик) | как источник контактов пуст; ценны САМИ вакансии как сигнал |
| Тендерные агрегаторы (rostender/synapsenet/tenderguru) | 109 | **0** | см. миражи ниже |

Отдельно: сегментация по MX (свой почтовый сервер против Mail.ru/Yandex) как
ФИЛЬТР не годится. Своя почта действительно коррелирует с выручкой (х4), но
77,5% компаний на Mail.ru и 76,1% на Yandex — тоже с выручкой ≥1 млрд, то есть
исключение по этому признаку выбросило бы три четверти крупных.

## 🐞 ДЕФЕКТ ИЗВЛЕЧЕНИЯ, НАЙДЕННЫЙ ЗАМЕРОМ (2026-07-29)

Регулярка телефона требовала код ровно из ТРЁХ цифр, поэтому «8 (3812) 39-45-67»
и «+7 (34764) 6 42 27» не находились вообще — то есть терялись номера
региональных заводов, ради которых база и собиралась. На 201 странице кэша:
старая форма видела 1385 номеров, новая — 1767. Плюс без `(?<!\d)` регулярка
читала «телефон» из середины ОГРН в подвале сайта. Починено, кэш перечитан,
101 нетелефонная строка удалена из базы с журналом. Вывод на будущее: цифры
покрытия имеют смысл только вместе с проверкой самой формы извлечения.

## ❌ ИСТОЧНИКИ-МИРАЖИ (проверены, НЕ работают для «ЛПР по ИНН»)
- Аттестация Ростехнадзора ЕПТ (qr.gosnadzor.ru/prombez): поиск ТОЛЬКО по номеру протокола,
  НЕ по ИНН/организации (проверено владельцем на скриншоте 2026-07-23: ввод ИНН -> «протокол
  не найден»). Обратного поиска от компании к людям нет by design. ВЫЧЕРКНУТО.
- pub.fsa.gov.ru - это Росаккредитация (лаборатории), вообще не тот реестр. ВЫЧЕРКНУТО.
- Паспорта ОЗП: нет единого реестра, по регионам + OCR сканов. Дорого. ОТЛОЖЕНО.
- Тендерные агрегаторы как источник ЛИЧНЫХ адресов заказчика (rostender.info,
  synapsenet.ru, tenderguru.ru и соседи): 109 компаний -> 0 контактов, 2026-07-29.
  Ноль проверен поштучно, чтобы не выдать сбой кода за пустоту: выдача даёт по
  3-4 ссылки на агрегаторы по каждому ИНН, страницы качаются без капчи (58-145 КБ),
  ИНН на странице есть. Пусто СОДЕРЖИМОЕ: rostender показывает только описание
  тендера, synapsenet прячет контакты за регистрацией («Войти»), tenderguru отдаёт
  СВОИ info@ и 8-800. За анонимный доступ контакты заказчика больше не отдают.
  Код оставлен выключенным (`lpr_sources --src agg`) — оживёт при платном доступе.

## ⚠️ ОБСУЖДЕНО, НО НЕ НАПИСАНО (аудит 2026-07-23)
- Декларации промбезопасности (подписант=техдир): НЕ начато. ПРАВИЛО: сначала руками
  проверить форму источника (есть ли поиск по ИНН), потом код - как научил провал с ЕПТ.
- Проверки качества: флаг персонального домена (@mail.ru != домен сайта), phone_mismatch
  (тел базы != тел сайта -> базовый протух), свежесть сайта (год копирайта/новостей),
  полная SMTP-валидация ящика (сейчас только mx_ok = домен принимает почту, не ящик).
- Финальный единый скоринг (pressure/rtn/hh/ЕИС/ЛПР -> выгрузка с провенансом): не начато (конечная точка).
- Проверки качества (precision-линза): SMTP-валидация ящика, флаг персонального домена,
  phone_mismatch, свежесть сайта - идеи, решения по внедрению нет.

## ⚠️ НАПИСАНО, НО НЕ ПРОВЕРЕНО НА ЖИВЫХ ДАННЫХ
- hh адресный (find_hh_compressor): 403 с IP сервера -> нужен прокси/дельфин-транспорт.
- ЕГРЮЛ-email / VK-группы / кэш сайта / Я.Карты-mapurl / 2ГИС-рендер / 2-й уровень обхода /
  site-block-фикс: код есть, живого замера покрытия/отдельных тестов не делал.
- ЕИС как источник в ОБЩЕМ enrich_one (сейчас только отдельный zakupki_mass, в основной
  путь не влит).

## Диагностические оп-ы (для тестов, не боевые)
resolve_test, hh_probe(debug), vk_probe, zakupki_probe, kg_probe, dolphin_conn1/diag,
dolphin_stop_all, fsa_probe, phone_match(diag_only), clean_bad_sites, mark_shared_phones.

## Не доверено/проверено
- Спот-чек 92 спасённых лидов (правильные ли ИНН) - не делал.
- Постадийные: T2 staff-краул ✅; T3/T4 отдали 404 при заборе (перегнать); T5 hh=0 (тот же 403).


## 🔄 hh: ПЕРЕВОРОТ МОДЕЛИ (идея владельца 2026-07-23)
Было: компания -> вакансия (перебор базы, у 99% нет вакансий = впустую).
СТАЛО: ВАКАНСИЯ -> компания. Ищем ВСЕ свежие компрессорные вакансии по РФ (машинист
компрессорных установок / оператор компрессорной станции / машинист воздуходувных /
слесарь по ремонту компрессорного), берём работодателя -> резолвим в ИНН (dadata) ->
это ГОРЯЧИЙ ЛИД (у компании есть компрессорное хозяйство, расширяется). Не обогащение,
а ГЕНЕРАЦИЯ лидов - встроить как коллектор в news_scan (эндпоинт /vacancies публичный,
/employers закрыт всем). hh адресный (компания->вакансия) оставить как доп. обогащение.
ПРОВЕРИТЬ РУКАМИ: api.hh.ru/vacancies?text=... - открыт ли JSON с сервера/браузера.

## 🟡 VK-группы: ДОКАЗАНО РАБОТАЕТ, транспорт флапает (2026-07-23 вечер)
ПРОЙДЕНО: app включён, OAuth в профиле, токен валиден. Владелец ВИДЕЛ живой ответ
groups.search «Компрессор Центр» -> 820 групп (Фабрика Компрессоров, prokompressor_ru,
Центр Техники Курган...). Токен привязан к IP профиля; IP у всех профилей ОДИН ->
работает через любой. Код: _vk_api_via_dolphin ходит через ОДИН закреплённый профиль
(_VK_PIN_PROFILE, стоп после, 2 ретрая); dolphin_set_proxies ИСКЛЮЧАЕТ VK-профиль
(его IP не менять - токен привязан). ОСТАЛОСЬ: дельфин интермиттентно 500 (та же
хрупкость весь день) -> автопуть VK возвращает пусто. НУЖНА стабилизация дельфина
(ретраи/health-check/возможно рестарт приложения). VK не виноват - транспорт.

## ✅ КОРЕНЬ ДЕЛЬФИН-БЕД УСТРАНЁН (2026-07-23 вечер)
DOLPHIN_TOKEN был протухшим (авторизация в приложении сброшена). Владелец перелогинился
-> токен ожил: dolphin_conn1 на 829115394 = start+connect+title OK. Local API отвечает
(было 401 -> стало EBUSY на занятом профиле = норм). ДОБАВЛЕНО: dolphin_is_running()
+ skip_if_running + _next_dolphin_profile(skip_busy) - не плодить окна на занятых
профилях (наводка владельца: профиль открывался 3 раза). 78 прокси на дропе
(dolphin-proxies.txt), op dolphin_set_proxies готов (Remote API флапает SSL EOF -
нужны ретраи/no-proxy-opener).

## 💡 ИДЕЯ В БЭКЛОГ (владелец 2026-07-23, НЕ делать сейчас): новости ОТ КОМПАНИИ
Симметрично hh-инверсии. Сейчас news_scan ищет ОТ СОБЫТИЯ (любой капекс -> резолв компании,
широкий невод). Идея: ОТ КОМПАНИИ - берём компанию из базы (приоритетно ядро/центробежные/
средний бизнес) -> запрос «"<компания>" (модернизир OR построит OR запустит OR расширяет OR
инвестир)» через xmlriver -> адресный капекс-сигнал именно про неё.
ПЛЮС: точное попадание, ИНН уже известен (не надо резолвить), нет тёзок, целим в СРЕДНИЙ
бизнес (владелец: у крупных конкуренция выше, нужны средние).
ЭКОНОМИКА (владелец: xmlriver = 25₽/1000 запросов): 161к компаний = ~4025₽ за ПОЛНЫЙ проход
по ВСЕЙ базе (×3 за пагинацию/2 ПС = ~12к₽). ДЁШЕВО -> можно по всей базе, не только сегменты.
Приоритет всё равно сверху вниз по выручке (первыми обработать ценных). Комплементарно неводу.
## Идея владельца 2026-07-24: «Алиса AI отдаёт контакты»
Алиса (нейро-ответ Яндекса) агрегирует справочники — бейджи источников на её ответе:
star-pro.ru, saby.ru (СБИС). Пример: ВМЖК ИНН 5007113179 → vmgk50@bk.ru.
РЕШЕНИЕ: не скрейпить Алису (браузер/хрупко), а читать её ИСТОЧНИКИ напрямую:
- saby.ru/profile/<ИНН>-<КПП> — карточка СБИС с email;
- star-pro.ru — справочник с email из ЕГРЮЛ-выгрузок.
Статус: пробы fetch_grep запущены (бот-защиту проверяем). Если отдают — добавить оба
в find_directory_contacts (цепочка справочного добора после list-org).
