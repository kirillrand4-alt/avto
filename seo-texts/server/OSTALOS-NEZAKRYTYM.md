# Что осталось незакрытым — реестр с числами

Замерено на живой базе 12.08.2026, вечер. Все числа — запросы к `C:\sender\enrich.db`,
`obzvon-index.db` и журналам сервера, не по памяти. База: **18 947 компаний,
31 299 адресов, 8 209 компаний с сайтом**.

---

## A. Данные уже в базе — можно править сразу, обхода не требуют

### A1. 353 строки «чужой сайт», у которых сайт всё ещё записан своим
`verified='mismatch'` и при этом `site` заполнен. По принятому 11.08 правилу такой
сайт должен уезжать в `cand_site` (кандидат), а не значиться подтверждённым.
- всего: **353**
- из них у **167** `cand_site` уже занят другим адресом — там нужен выбор, а не перенос
- чистый перенос: **186**

Цена бездействия: рассыльщик видит `site` и может сослаться на чужой сайт в письме.

### A2. Контакты на холдинговых доменах — ваше решение не применено
Ваша команда была: «только тех роли прикрепи и подпиши что это холдинг».
- доменов базы обзвона, за которыми числится больше одного юрлица: **941**
- наших компаний, сидящих на таких доменах: **162**
- контактов у них: **589**

Не сделано ничего: ни отбора по техническим ролям, ни подписи «холдинг».

### A3. Мусор в уже собранных адресах
Фильтры вшиты в код и на новых прогонах работают, но старое не вычищено:
- мёртвая доменная зона (`.tools`, `.aero`, `.trade`, `.co`): **266**
- хостеры и доски объявлений: **21** (`contact@rusprofile.ru`, `help@creatium.io`,
  `info@synapsenet.ru`)
- обрывки вложений (`.docx`, `.xlsx` в адресе): **0** — этого мусора в базе нет

### A4. Лучший адрес по новой шкале ролей — пересчёт задним числом не запускался
Из 5 086 компаний с двумя и более адресами новый выбор отличается у **1 177**:
- ранг строго лучше: **646** (`salova@kaustik.ru` общий → `bachenin@kaustik.ru` гл. инженер)
- ранг тот же, перестановка равных: **531**
- ухудшений: **0**

### A5. Новые поля пусты, потому что заполняются только новыми прогонами
| поле | заполнено |
|---|---|
| `verified_url` (ссылка-доказательство) | **0** |
| `region_sverka` (сверка географии) | **0** |
| `site_title` | 2 930 |
| `site_description` | 2 326 |
| `emails.razdel` | 1 643 |
| `activity` | 9 469 |

`verified_url` и `region_sverka` выложены на сервер только сегодня. Задним числом их
можно посчитать по кэшу скачанных страниц, но кэш крошечный — см. D5.

---

## B. Охват — где в базе дыры

### B1. Половина компаний без единого адреса
- **9 557 из 18 947 (50%)** не имеют ни одной почты
- из них **3 785** обошли и не нашли ничего
- остальные **5 772** ни разу не обходились

Всего никогда не обходились **8 240** компаний (часть из них имеет почты из checko,
закупок и hh).

### B2. 6 203 компании имеют почты, но ни одной целевой роли
Только `общий` / `бухгалтерия` / `кадры` / `приёмная`. Письмо им уйдёт в никуда.

### B3. 10 738 компаний вообще без сайта
Из них **6 763** и без кандидата в сайты. Для них обогащение через сайт невозможно
в принципе — нужен другой канал (справочники, закупки, hh, телефон).

### B4. Прогон №3 завершён, но не разобран
ОКВЭД 24 и 25 из базы обзвона, выручка от 100 млн. Последняя запись стадии —
13:49 МСК, за 75 секунд наблюдения ноль движения, служба раннера RUNNING.
- обходов: **5 557**
- компаний, отдавших почты: **2 393 (43%)**
- адресов: **5 747**
- целевых ролей: **1 758**

Роли: общий 3 632, продажи 1 025, снабжение/закупки 314, директор 255,
нач. производства 54, инженер 28, техдиректор 25, гл. инженер 20, гл. технолог 14.

Не разобрано, почему у 57% пусто. Прошлая проверка агентами на другой выборке
говорила: 63% сайтов не отдали содержимое (капча, редирект, пустой ответ).

---

## C. Правила, вынесенные вам на решение

### C1. 91 увод выбора на чужой домен
`info@idilikafood.ru` → `job@btk2000.ru` (другая фирма). Лечение штрафом за адрес
вне домена компании измерено и **отвергнуто — дороже**:

| штраф | уводов гасит | целевых ролей теряет |
|---|---|---|
| +2 | 51 из 91 | 35 |
| +3 | 60 | 61 |
| +5 | 86 | 138 |
| всегда свой домен | 91 | 159 |

Снабжение массово сидит на бесплатной почте вне домена (`mtk-eng@yandex.ru`,
`omto_dsr@mail.ru`) — любой штраф выкидывает их ради `sale@` и `info@`. Источник
тоже не разделитель: 42% уводов с тендерных площадок, остальные из своего обхода.

### C2. «Улика обязательна для сайта» — отвергнуто по цене
5 003 сайта из 8 177 (61%) уехали бы в кандидаты.

### C3. Мобильные прокси для справочников, 78 обычных для массы
Три живых мобильных прокси лежат на сервере в `C:\sender\proxies-mobile.txt`
(в репозиторий не попадают). Правило разделения не вшито в код.

---

## D. Панель и инфраструктура

### D1. «Переписка» в карточке лида — диагноз готов, чинить вам
Красное окно — не в коде. Проверено:
- журнал панели: `/api/leads/12/dialog` → **500** восемь раз, `/api/leads/9/dialog` → **200**
- код на диске проигран напрямую, минуя HTTP: лид 12 отдаёт **2 письма, 1 ветку**,
  `jsonable_encoder` и `json.dumps` проходят без ошибки

Причина: служба `SenderPanel` запущена **11.08**, а `sender/store.py`, `sender/api/app.py`,
`sender/leaddesk.py` и `web/dist` переписаны **сегодня в 13:31**. Служба держит в
памяти старый код. Лечится одной вашей командой:

    Restart-Service SenderPanel -Force

Кто-то (соседняя сессия) выложил панель сегодня и не перезапустил службу.

### D2. Пометка «доказано / предположено» в панели
Согласована, не сделана. Данные для неё готовы: `verified` + `verified_url`.

### D3. CLAUDE.md указывает неверный путь конфига
Записано `C:\sender\config.yaml` — **такого файла нет**. Панель запущена без
`--config`, значит берёт `./sender.yaml` при рабочем каталоге `C:\sender`, то есть
реальный конфиг — **`C:\sender\sender.yaml`**. Строку в CLAUDE.md надо поправить,
иначе каждая следующая сессия будет искать не там (я потратил на это три захода).

### D4. Сохранять ВСЕ страницы-источники контакта
Сейчас у адреса одна ссылка. `source_url` заполнен у **28 065 из 31 299 (90%)**,
уникальных страниц — 13 553. Вторая и третья страницы, где тот же адрес встретился,
не хранятся.

### D5. Кэш скачанных страниц почти пуст
**202 файла** на 18 947 компаний. Ваше правило от 29.07 — «всё, что скачиваем,
складывай, чтобы анализировать без повторного краулинга» — фактически не соблюдается:
конвейер зовётся без `cache_dir`. Из-за этого любой пересчёт задним числом (A5)
требует нового обхода вместо секунд по диску.

### D6. Надёжные имена контактов
`person` заполнен у **8 423** адресов, из них **8 236** похожи на ФИО. Но 1 836 из
них — из чужих сборников: справочники 713, `zakupki:eis` 619, `tender.pro` 504.
Именным приветствием такие подкреплять нельзя. Правило (белый список источников
плюс живая ссылка) не вшито.

---

## E. Закрыто замером — снимаю с повестки

### E1. Протухшие ссылки-доказательства — тревога не подтвердилась
Выборка 300 случайных `source_url`: **275 открылись, 25 пустых, 0 недоступных**.
Прежняя оценка «не открывается каждая четвёртая» была получена на маленькой выборке
агентами и не подтвердилась.

### E2. Конвейер okved не нужен
Полные коды ОКВЭД есть в выгрузке базы обзвона у 100% строк — ходить за ними на
checko незачем.

---

## Что мешает двигаться дальше

Три вещи, каждая упирается в вас:

1. **Перезапуск `SenderPanel`** — вернёт «Переписку» (D1).
2. **Решение по холдингам** (A2) — я знаю ЧТО делать, но 589 контактов трогать без
   вашего «да» не буду.
3. **Разрешение на массовую правку старых строк** (A1, A3, A4) — это 353 + 287 + 1 177
   записей в живой базе. Всё обратимо, но переписывает то, чем уже пользуется
   рассыльщик.

Остальное (B, D4–D6) — работа без развилок, могу брать по вашему порядку.

— сессия обогащения, 12.08.2026
