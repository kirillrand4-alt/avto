# Сендер-сервис: состояние и как продолжить

Свой сервис холодной B2B-рассылки для ООО «Руспром» — замена coldy (SMTP-отправка
с наших Яндекс360/VK ящиков, IMAP-приём/ответы, свой живой прогрев, юр-гейты
ФЗ-38/152, стык с AI reply-desk). Причина «своего»: база не уезжает на сторону
(coldy — ИП Ахматзянов, база уходит к нему), контроль, экономия тарифа. Разбор coldy —
в `FEATURES-PLAN.md`.

## ТЕКУЩЕЕ СОСТОЯНИЕ (код в git, здесь же)

Код **уже в репозитории** (`seo-texts/sender/`), НЕ в scratchpad. Сгенерирован
изолированно по `CONTRACT.md` через провайдер (`claude-fable-5`), интегрирован.

- **13 модулей + `dtos.py` + `errors.py`**: config, store, suppression, validation,
  personalize, gates, sender, imap_watcher, cadence, unsub, analytics, warmup,
  orchestrator. `errors.py` появился 2026-07-18: без него у каждого модуля были
  ПРИВАТНЫЕ фолбэк-классы исключений и `except PersonalizationGateError`
  оркестратора не ловил бросок из personalize (инвариант §5.4 мёртв).
- **Тесты: 508/508 pass** (`python3 -m pytest sender/tests/` из `seo-texts/`;
  для интеграционных нужен `pip install aiosmtpd`, иначе они skip).
- Инварианты из контракта на месте: идемпотентность (sha256+UNIQUE+ON CONFLICT),
  резюм после рестарта (lease+recover_stale), гонка «ответил→не слать дубль»,
  hard/soft bounce, юр-гейт (List-Unsubscribe + RFC 8058), kill-switch с min_volume.

### Сессия 2026-07-18 (ветка `claude/persona-prompt-seo-sender-vi4tcq`) — что сделано

1. **6 тестов unsub починены** (тесты, не логика): локальный стаб ValidationError
   не совпадал с классом модуля; порча последнего b64-символа подписи флакала
   (его младшие биты отбрасываются декодером); кейс invalid-base64 бил не в ту ветку.
2. **orchestrator.py был ОБРЕЗАН при генерации** (tick обрывался на шаге 2, run/
   _safe_pause отсутствовали) — достроен через провайдер, префикс сохранён.
   **analytics._as_since тоже был обрезан** (глотал любой datetime → окно since
   игнорировалось). **cadence.plan_campaign был заглушкой** (возвращал []) —
   реализован. Урок: «код компилится» ≠ «код дописан», без тестов обрезки не видны.
3. **Тесты analytics (21) и orchestrator (16)** сгенерены провайдером и добиты
   руками; вскрыли разрывы стыков: count_events без mailbox_id/sequence_step_id,
   отсутствие iter_mailbox_states, отсутствие sender/errors.py.
4. **Дешёвые правки validation/imap**: null-MX RFC7505 → hard-invalid (те 6 411);
   офлайн typo-карта (detail.typo_suggestion, адрес НЕ переписывается);
   detail.provider_class consumer|hosted|corp|unknown; MX/A-кэши над резолверами;
   greylist 4.x.x → ретрай `:sbrN`-суффиксом idempotency_key (потолок
   imap.soft_bounce_max_retries=2, стоп при has_reply), НЕ suppress.
5. **Тормоз по bounce доработан**: пороги в контракте 8.0/6.0 → 3.0/2.5;
   `provider_bounce_pct` 2.5 — bounce × провайдер получателя (report-only trip:
   авто-suppress всего mail.ru снёс бы половину базы, решает оператор);
   **канареечные волны в cadence** (canary_size=150 → hold canary_hold_hours=4 →
   замер hard-bounce ≤ canary_max_bounce_pct=3 → событие canary_passed, стейтлесс
   по журналу). **store.set_recipient_validation** — раньше mx_provider/valid_status
   НИКТО не писал (plan_campaign(valid) не запланировал бы никого).
6. **Интеграционная песочница** `tests/test_integration_sandbox.py` (aiosmtpd,
   реальные модули, IMAP-инжект через classify/_process_event): волна → bounce →
   стоп-на-ответ → дедуп → резюм после рестарта. Поймала 5 боевых багов:
   окно Москвы применялось к UTC-часам (сдвиг отправок на вечер) и теряло tz;
   pick_mailbox/send жёстко брали wall-clock (сломан инжект часов тика);
   pick=None терминально хоронил письмо (mark_skipped) — теперь возврат через
   lease; mailbox_state никто не сеял (первая отправка падала в increment_sent) —
   сеет bootstrap; пейсинг `int(cfg or 90)` глотал явный 0.

## ОСТАЁТСЯ (для следующей сессии, в порядке дешевизны)

> **Проверенная дорожная карта улучшений — в `REVIEW-FINDINGS.md`** (4 прогона ревью
> max-усилие + фактчек-верификатор против кода: 295 CONFIRMED / 65 PARTIAL / 9 ALREADY_EXISTS
> / 5 NEW). Там: критичные дыры (Bitrix-коннектор, consent_log, Постмастер, атрибуция-в-каждом-
> письме, суб-классификация ответов), PARTIAL-доработки, 5 новых идей под квал-лид, что НЕ
> строить (уже есть), спека уведомлений. Сверено построчно на коде/данных.

1. ~~Почистить 6 тестов unsub~~ — **сделано 2026-07-18** (см. выше; реальные причины
   чуть отличались от прежней заметки: не env-мок, а идентичность класса исключения
   + флак порчи последнего b64-символа).
2. ~~Дописать тесты analytics/orchestrator~~ — **сделано 2026-07-18** (и попутно
   достроены обрезанные orchestrator/analytics/cadence — см. выше).
3. ~~Интеграционный прогон на песочнице~~ — **сделано 2026-07-18**
   (`tests/test_integration_sandbox.py`, 8 тестов, 5 багов поймано).
4. **Финальный smoke доставляемости** — на реальном домене с 2 ящиками (перед
   пилотом): SPF/DKIM/DMARC pass + попадание в инбокс. Реальный домен нужен ТОЛЬКО
   на этом шаге; всё до — на песочнике. ← ЕДИНСТВЕННЫЙ оставшийся пункт этого блока.
5. ~~Критичные дыры из REVIEW-FINDINGS~~ — **код закрыт 2026-07-18 (вечерняя
   часть сессии), 466 тестов**. Сделано: атрибуция ООО+ИНН в КАЖДОМ письме
   (personalize._ensure_attribution, безусловный футер); consent_log ФЗ-152
   (store + записи из send/one-click/жалобы/текстового «отпишите», юр-линия
   владельца: адресное B2B-предложение); `bitrix.py` (лиды через входящий
   вебхук, идемпотентно, disabled без URL); `reply_classify.py` (hot/интерес/
   OOO/отказ/отписка + телефон + AI-хук; автоответ пишется как reply_auto и
   НЕ стопит цепочку); `postoffice.py` (клиент Постофиса, endpoints сверить
   при получении токена); `notify.py` (Telegram, дебаунс через журнал, тихие
   часы, notify_gate_trips из orchestrator).
   **Ждёт ТОЛЬКО доступов владельца** — полный чеклист: `OWNER-TODO.md`
   (вебхук Bitrix + роутинг менеджеров, TG бот-токен + chat_id, токен
   Постофиса, ящики/DNS для smoke). Уроки генерации те же: оба телефонных
   регекса пришли структурно битыми, 2 тест-файла обрезаны шлюзом на ~27КБ —
   чинить руками быстрее третьего фикс-раунда.

6. **Фаза 1 (операционный слой) — код закрыт 2026-07-18 (ночная часть), 508 тестов.**
   `importer.py` (стрим CSV 648МБ: автодетект разделителя/кодировки, русские
   алиасы заголовков, батч-upsert, suppression-импорт, резюмируемая DNS-валидация);
   `cli.py`+`__main__.py` (`python -m sender`: init-db/import/suppress-import/
   validate/campaign-*/run[--once --dry-run]/status/pause/resume/stats; сквозной
   смоук пройден); `unsub_server.py` (one-click RFC 8058 + человеческая GET-страница
   + /health); `config/sender.example.yaml` (валидирован боевым Config.load);
   `RUNBOOK-DEPLOY.md` (Windows NSSM первым, systemd вторым). Плюс движковый фикс:
   IMAP-коннект без таймаута вешал tick навечно (imap.connect_timeout_sec=20).
   Остался только smoke 1.6 (нужны доступы владельца, OWNER-TODO).
7. **Фаза 2.0 (дизайн сайта) — `SITE-DESIGN.md` готов 2026-07-18.** Мульти-модельная
   панель через провайдер: 8 персон → кросс-опыление → судья → фактчек-линза против
   API движка (~150 фич; линза сняла 3 «дыры» судьи как ложные — логика уже в коде).
   Итог: значимая доля макета UI-ONLY; дефицит API — топ-10 NEW-BACKEND методов
   (готовый бэклог Фазы 2.1) + три BUILD-NEW слоя (HTTP/WS transport, auth,
   lead-desk). Сырьё панели на drop: site-design-panel-raw-2026-07-18.tar.gz.

### Валидация + защита от bounce (ревью+сверка с кодом 2026-07-17)

Данные подтверждены на базе: **74.4% получателей Mail.ru/Яндекс** (51.1%+23.3%),
**6 411 мёртвых MX = 3.6%** (гарантированные баунсы). На 74% MX всегда резолвится →
проверить существование ЯЩИКА до отправки там нельзя; upfront-валидатор снимает лишь
мёртвый домен+синтаксис+role+disposable (единицы %). Значит основная защита — НЕ
upfront-чек, а тормоз по ЖИВОМУ bounce-rate.

**Тормоз по bounce у нас ЕСТЬ (`gates.py`), но слишком грубый — доработать, НЕ строить с нуля:**
- порог `domain_bounce_pct: 8.0` / `mailbox_bounce_pct: 6.0` → **опустить до 2-3%** (8% =
  новый домен-двойник уже сгорел, пока накопится);
- ключ rate сейчас домен/ящик → **добавить измерение × провайдер получателя** (репутация
  Mail.ru и Яндекс раздельная, считать баунсы отдельно);
- **канареечные волны** — слать сегмент малыми волнами, замерять live hard-bounce-rate,
  только потом остальное (сейчас пауза срабатывает ПОСЛЕ удара по первой полной волне).

**Дешёвые правки в `validation.py`/`imap_watcher` (проверены, валидны):**
- Null/dead-MX (RFC7505) → `hard-invalid` (те самые 6 411);
- офлайн typo-коррекция домена (mial.ru→mail.ru, yandx.ru→yandex.ru) — нулевой IP-риск;
- тег `recipient_provider` на строку результата → cadence делит темп consumer/корп;
- greylist 4.x → **ретрай** (переслать позже), НЕ suppress (RU часто гриллистят; сейчас
  soft уже не суппрессится, но и не ретраится);
- кэш MX-резолвов (не долбить 161к дважды).

**НЕ делать (скептик + сверка, подтверждено):** RCPT-проба со СВОИХ отправляющих IP
(74% accept-all/greylist → врёт + харвестинг-сигнал); живой disposable-фид (в B2B РФ ~0%);
IDN/.рф-нормализация (почти всё ASCII); эвристика спам-ловушек (не предсказуемы). Глубокая
mailbox-проверка — ТОЛЬКО на неопределённый сегмент (custom/корп) и ТОЛЬКО внешним
сервисом/с отдельных прогретых IP, не на весь 74% consumer.

⚠️ Урок про ревью-харнесс: прогон БЕЗ линзы-верификатора «сверь с текущим кодом» склонен
утверждать «у вас нет X», хотя X частично есть (так вышло с bounce-gating). Выводы любых
ревью сверять с кодом/данными до внесения — как здесь.

### Пробелы против coldy (проверка покрытия 2026-07-17)

Ядро рассылки воспроизведено (P0). Не воспроизведены (обвязка):
- **Коннектор CRM Bitrix24** — есть только интерфейс `ReplyDeskSink`, коннектора нет.
  Владелец на Bitrix — P1, важно.
- **Исходящие MIME-вложения** — sender их не строит (для холодной часто и не надо).
- **Глубокая валидация (спам-ловушки, полный анти-bounce)** — есть MX+провайдер+
  catch-all/role/disposable; deep-проход — внешним RU-валидатором.
- **API** — не построен (P2).
- **AI-предобработка базы** (реальное имя с сайта + проверка на ЦА) — частично: своя
  база уже с названиями/ОКВЭД.
- **Подписи ящиков** — проверить, реализованы ли полноценно (керворд есть в sender/config).
Сознательно пропущены (правильно): warmup-пул (Google-only), Leadbase 7М (своя база),
под-ключ/авто-оплата. Источник сравнения — `FEATURES-PLAN.md` + на drop
`coldy-parse-2026-07-17.tar.gz` (сырьё страниц coldy).

## Deliverability-контекст (почему модули так устроены)

Мягкое знание из разбора coldy и переписки с их саппортом — иначе не очевидно из кода:

- **Прогрев у нас ЖИВОЙ, не пул.** Warmup-пул coldy — только Google-ящики; для нашей
  базы (74% Mail.ru/Яндекс) почти бесполезен. Mail.ru забанил инструменты прогрева.
  Поэтому `warmup.py` = живая рассылка микродозами: рамп **2-3/день → ~30-50/день/ящик**
  за 3-4 недели по чистейшему A-срезу. Это и есть «прогрев» под RU-инбоксы.
- **Инфраструктура инбоксов (смета):** ~10 доменов-двойников `.ru` (~2 500 ₽/год) с
  301-редиректом на основной сайт (основной `prokompressor.ru` НЕ трогать); ~30-35
  ящиков = 20 Яндекс360 + 10-15 VK WorkSpace (~250 ₽/ящик/мес ≈ 7.5-9 тыс/мес).
  **Провайдер-сплит**: mail.ru-домены и yandex-домены — раздельными пулами.
  Западные (Google WS / M365 / Mailreef и т.п.) НЕ брать — иностранные IP плохо
  проходят Mail.ru/Яндекс. (Детали и вердикты — на drop: `coldy-inboxes.json`.)
- **Google-контур (опция):** в базе ~706 ценных компаний (100 млн+ выручки) на
  Google-хостинге — единственное место, где Google-пул coldy уместен. 2-3 отдельных
  ящика можно прогреть им под этот сегмент.
- **Юр-гейт письма-1 (ФЗ-38):** атрибуция ООО «Руспром» + ИНН обязательна уже в первом
  письме (3 ревьюера единогласно ПРОТИВ «бренд называем позже»); мягкая личная подпись
  менеджера — ок. Владельцу выбрать юр-линию (адресное B2B-предложение vs согласие).
- **Каденция engagement-gated:** касание 1 всем валидным → 2 не-отскочившим → 3-4
  только вовлечённым. Гейт complaint-rate <0.1%/домен (стоп при >0.3%), bounce <2-3%.
- **База уже чистится от конкурентов:** suppression 39 ИНН (на drop, `base-cleaning-
  2026-07-17.tar.gz`); ждём корп-список ИНН конкурентов от владельца.

## Как продолжить в НОВОЙ сессии

⚠️ **Свежайший код — в ветке `claude/persona-prompt-seo-sender-vi4tcq`** (сессия
2026-07-18; она включает всю историю `claude/youthful-sagan-ny4fm6`). Новая сессия
стартует на свежем клоне дефолта → там пусто. ПЕРВЫМ делом переключись на ветку:

```
git fetch origin && git checkout claude/persona-prompt-seo-sender-vi4tcq
```

> Работай по `PERSONA-PROMPT.md`. Читай `seo-texts/sender/SENDER-STATE.md`,
> `REVIEW-FINDINGS.md`, `CONTRACT.md`. Прогони тесты из `seo-texts/`:
> `python3 -m pytest sender/tests/ -q` (нет pytest/aiosmtpd — `pip install pytest
> aiosmtpd`). Ожидаемо: **508 passed / 0 failed**. Продолжай с раздела «ОСТАЁТСЯ»
> (остались п.4 smoke на реальном домене — нужен домен от владельца — и п.5 дыры из
> REVIEW-FINDINGS, начинать с Bitrix-коннектора/атрибуции). Тяжёлое — через провайдер
> (`gen_provider`, `claude-fable-5`), в фоне. Сырьё (база obzvon_all, датапаки,
> VERIFY-JSON) — на drop, локально его нет; для кода оно не требуется.

## Извлечённые уроки (грабли) — чтобы новая сессия не наступала

1. **Баг шлюза router.cheap: при `thinking=adaptive` код иногда уходит в thinking-блок,
   а `text` пустой** (`stop=end_turn, thinking=26k, text=0`). НЕ защита, НЕ упор в
   max_tokens — вывод ушёл не в тот канал. Обход: кодоген с `thinking=False`.
2. **`gen_provider.call` хардкодит `max_tokens=16000`** — мало для крупных модулей.
   Для кодогена звать `gp._raw_stream(msgs, model, 32000-64000, thinking=False, effort=...)`.
3. **Шлюз НЕ отбраковывает большой `max_tokens`** (принял 200000). Потолок — у модели:
   Fable 5 — 128K вывода, Sonnet 5 / Opus — ~64K.
4. **Изолированной генерации нужен общий типовой модуль ПЕРВЫМ.** DTO собраны в `dtos.py`
   из §3 контракта механически (без API). Генерить его ДО остальных модулей.
5. **Экстрактор кода режет по маркерам `# FILE:`**, а не по числу ```-блоков (модель
   иногда склеивает impl+тесты в один блок).
6. **Устойчивость прогона:** ловить исключение помодульно, фолбэк effort/thinking,
   ретраи. Иначе один пустой ответ убивает весь прогон (так упало на imap_watcher).
7. **max-усилие Fable 5 работает** для качества — но только с `thinking=False`.
8. **Тесты бывают брителе кода:** чинить РЕАЛИЗАЦИЮ; тест править только если он
   противоречит контракту (см. unsub).
9. **Не называть типовой модуль `types.py`** — затеняет stdlib `types` (падает запуск
   из папки). У нас переименован в `dtos.py`.
10. **Провайдерский фикс-цикл не всесилен** — если 3 круга не сошлись (unsub), дешевле
    разобрать руками.
11. **(2026-07-18) Шлюз режет ХВОСТ стрима на ~27 КБ** при `stop=end_turn`: ответ
    обрывается посреди строки, но выглядит «завершённым» (3 попытки подряд дали ровно
    27114 байт). Фикс-циклы при этом бесполезны (каждый ответ снова обрезан). Признак:
    незакрытая скобка/строка в конце файла. Лечение: просить файлы <25 КБ (дробить),
    хвост дописывать руками. Ответы 16-23 КБ проходили целиком.
12. **(2026-07-18) Модель выдумывает поля DTO** даже при реальных сигнатурах в промпте
    (RecipientIn.provider, CampaignIn.status, MessageIn.mailbox_id, subject_tpl).
    Дешевле всего лечится sed-ом по одному образцу ошибки; в промпт класть ПОЛНЫЙ
    текст dtos.py, а не только сигнатуры store.
13. **(2026-07-18) «Компилится» ≠ «дописан»**: три модуля (orchestrator, analytics,
    cadence.plan_campaign) были обрезаны/заглушены с прошлой генерации и это никак
    не проявлялось до написания тестов. Проверять хвост файла и «pass-заглушки»
    у КАЖДОГО сгенерённого модуля до интеграции.
14. **(2026-07-18, ночь) Экстрактору НЕЛЬЗЯ фолбэчить по basename**: маркер
    `# FILE: ...importer.py` матчится и на tests/test_importer.py — тестовый файл
    затирал реализацию. Извлекать ТОЛЬКО по точному пути; реализация восстановима
    из raw-*.txt (сырые ответы всегда писать на диск — это спасло дважды).
15. **(2026-07-18, ночь) Фоновые фикс-раунды гоняются с ручными правками**: пока
    правишь файл руками, фоновый цикл перезаписывает его целиком своей (старой)
    версией. Либо фон, либо руки — не одновременно на одном файле.
16. **(2026-07-18, ночь) Обрез хвоста шлюза НЕ детерминирован**: фактчек отдал
    55КБ целиком, а файлы по 21-27КБ резались. Лимит "<=550 строк" в промпте —
    страховка, но проверка «хвост цел» обязательна для КАЖДОГО файла.
17. **(2026-07-18, ночь) Смоук CLI ловит то, что юниты не видят**: перепутанный
    порядок аргументов конструкторов у ВСЕХ шести классов движка, выдуманные
    имена модулей/полей, зависание тика на недоступном IMAP. Сквозной смоук
    руками — обязательный шаг после кодогена обвязки.
