# Промпт инженеру (ветка claude/persona-prompt-seo-sender-vi4tcq)

Контекст: система холодной рассылки `seo-texts/sender/`. Полная карта модулей —
`SENDER-ARCHITECTURE.md`; анализ пересечений — `overlap-analysis.json`. Тесты
сейчас зелёные (718 passed). Ниже — что осталось. Приоритеты сверху вниз.
Всё через провайдерский API для тяжёлого (правило владельца), stdlib для движка,
каждую правку — с тестами, не ронять существующие 718.

---

## P0 — функциональный пробел: канал уведомлений Max (VK)

**Статус:** НЕ сделано. `notify.py` умеет только `_send_telegram`. Сервер в РФ,
`api.telegram.org` режется с серверного IP — нужен нативный РФ-канал Max.
Спека готова: `MAX-NOTIFY-SPEC.md`. Реализовать по ней:

1. `MaxSink` / `_send_max` в `notify.py` — тот же интерфейс, что у Telegram.
   ⚠️ Эндпоинты/схему Max Bot API (base URL, метод отправки, авторизация
   `?access_token=`, поле `chat_id`) сверить с АКТУАЛЬНОЙ докой Max Bot API, не
   хардкодить по памяти.
2. Конфиг `notify`: `channel: telegram|max|both` (по умолчанию telegram —
   обратная совместимость), `max_token_env`, `max_ops_chat_id`.
3. Дебаунс/тихие часы/приоритеты P1-P3 — общие для обоих каналов (вынести из
   `_send_telegram`, не дублировать).
4. Telegram ходит через `HTTPS_PROXY`, Max — напрямую; сбой одного канала не
   роняет второй.
5. Тесты: mock HTTP; channel=max → Telegram не дёргается; both → оба;
   отсутствие channel → только telegram; сетевой сбой одного канала.

От владельца нужно: создать Max-бота, дать `MAX_BOT_TOKEN` + `max_ops_chat_id`.

---

## P1 — латентный баг: расхождение идентичности исключений/DTO

Из overlap-анализа: `errors.py` и `dtos.py` — канонические источники, но многие
модули держат **приватные fallback-копии** классов (`try import ... except:
class ...`). Реальный риск: если определения разойдутся, `except
PersonalizationGateError` в `orchestrator` НЕ поймает исключение из `personalize`
(разная identity классов) — молчаливый пропуск инварианта.

Задача: сделать `errors`/`dtos` жёстко импортируемыми во всём дереве (как пакет),
fallback-копии убрать или свести к единственному источнику. Проверить, что
`except`-ветки ловят реальные классы (тест на identity: raise из одного модуля,
catch в другом).

---

## P2 — консолидация общих примитивов (рефактор, не удаление модулей)

Полностью лишних модулей НЕТ (warmup ≠ sender: warmup вызывает sender.send;
cadence ≠ orchestrator; gates ≠ warmup). Но общие куски размазаны — вынести в
`shared`/утилиты:

1. **Рамп-кривая/дневной лимит** считаются в `sender.can_send_now`,
   `warmup.daily_target`, `orchestrator._seed_mailbox_states`, `config` — единый
   резолвер лимита по (provider, ramp_day), все четверо зовут его.
2. **HMAC-токен** (`tracking` и `unsub` — дословно одинаковый формат
   base64url(payload).base64url(sig), один секрет) → общий `sign_token/verify_token`.
3. **DNS-резолвинг** (`dns` и `validation` — dnspython + фолбэк nslookup) →
   общий резолвер MX/таймаутов.
4. **Нормализация email/домена** (`suppression` и `validation` — lower/strip/IDNA)
   → одна функция (иначе стоп-лист рассинхронится с валидацией).
5. **review_chain** дублирует вызов LLM-caller + парсинг JSON из `review_lenses` —
   судью собрать через тот же механизм линз.
6. **DI-wiring** графа зависимостей дублируется в `cli` и (частично) `api` —
   вынести композиционный корень (`build_deps`) и звать из обоих.

Каждый пункт — отдельный небольшой PR с тестами. P2 не блокирует запуск, делать
после P0/P1.

---

## Что НЕ трогать
- Защитное дублирование инварианта «стоп-на-ответ» (claim_due_messages в store +
  imap_watcher + orchestrator) — намеренная многослойная страховка §5, оставить.
- Слоистость QA автоответчика (механический qa_reply → 8 линз → судья) — оставить.

---

## P1.5 — пробелы под сценарий рассылки владельца (из PANEL-HOWTO.md)

Разбор панели вскрыл, что заявленный владельцем сценарий пока не собирается.
Приоритет высокий — без этого КЦ/Meyer не разведём и тайминг не тот.

1. **Таргетинг по сегменту (КЦ vs Meyer) — БЛОКЕР.** Сейчас
   `cadence.plan_campaign` берёт `iter_recipients(valid_status="valid")` без
   фильтра по сегменту/кампании → каждая кампания шлёт по ВСЕЙ базе. Поле
   `recipients.segment` пишется импортом, но не используется. Сделать: фильтр
   `segment` в `iter_recipients`; кампания несёт целевой сегмент (напр.
   `Campaign.segment` или audience-фильтр в `config_json`); `plan_campaign`
   передаёт его. Тесты: кампания КЦ не трогает получателей Meyer.
2. **Загрузка базы из панели** (сейчас только CLI `import`): эндпоинт
   `POST /recipients/import` (multipart CSV) с выбором сегмента + прогресс;
   переиспользовать `importer.import_csv`. Экран в SPA.
3. **Добавление домена из панели** (сейчас только `config.yaml`): экран доменов
   поверх существующих `GET /domains` + `dns.py`; запись ящиков.
4. **Тайминг «9:00 по зоне получателя»** — хранить таймзону получателя (вывести из
   региона при импорте/валидации), `cadence._shift_into_window` считать в зоне
   получателя, а не глобальной `timezone`.
5. **Пейсинг по региону** — очередь/интервал на регион (сейчас `send_pacing` на
   ящик, глобально). Дать в конфиг `send_pacing.per_region_interval_sec`.

Пн-Чт с 9:00 (одна зона) и пейсинг 5 мин на ящик — уже работают через
`config/sender.yaml`, доработки не требуют.

---

## P1.6 — перенести баллы приоритета из базы в получателей + порядок отправки

В базе обзвона есть готовые колонки: `Макс. балл по связке` (1-5),
`Итоговый балл приоритета`, `Приоритет × выручка / 10000` (PxR). Сейчас импорт их
не тянет, а порядок отправки — по `id`.

Сделать:
1. Импортёр (`importer.py`) кладёт эти три поля в получателя (новые колонки
   `priority_max`, `priority_total`, `pxr` в recipients; автодетект заголовков).
2. `iter_recipients` / `cadence.plan_campaign` — порядок отправки ФАЗОВЫЙ, из
   конфига кампании `send_order: pilot_asc | priority_desc`:
   - `pilot_asc` (обкатка) — по `pxr ASC` (малые/дешёвые первыми, тренируемся);
   - `priority_desc` (боевая фаза) — по `pxr DESC` (приоритетные первыми).
   Плюс опц. порог `min_priority_max` (отсечь балл 2-3).
3. Панель: показать PxR/балл в списке получателей, дать сортировку/фильтр.
Тесты: при заданном пороге получатели с баллом<порога не планируются; порядок по PxR.
