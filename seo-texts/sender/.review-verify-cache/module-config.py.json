{
 "module": "config.py",
 "findings": [
  {
   "line": 408,
   "severity": "critical",
   "summary": "Секрет unsub_secret_env проверяется только на присутствие в env, но не читается и не используется в Config",
   "scenario": "При инициализации Config проверка `if secret_env not in self._env` успешна, но сам секрет никогда не извлекается из окружения. Когда модуль tokens.py попытается получить секрет через `config.legal().unsub_secret_env`, он получит только имя переменной, а не её значение. Если tokens.py не делает собственный `os.environ[secret_env]`, подпись one-click формируется из имени переменной вместо секрета — токены становятся предсказуемыми/подделываемыми, отписка обходится.",
   "fix_hint": "Добавить в LegalCfg поле `unsub_secret: str`, извлечь секрет из `self._env[secret_env]` при построении LegalCfg и сохранить его значение. Аналогично для password_env в MailboxCfg."
  },
  {
   "line": 383,
   "severity": "high",
   "summary": "Пароли ящиков проверяются на присутствие в env, но не извлекаются и не хранятся в MailboxCfg",
   "scenario": "MailboxCfg содержит только `password_env` (имя переменной), но не само значение пароля. Если код отправки обращается к `mailbox.password_env` и пытается передать строку-имя в `smtp.login(user, mailbox.password_env)`, аутентификация SMTP провалится. Если отправщик делает `os.environ[mailbox.password_env]` самостоятельно, то Config не выполняет обещанную валидацию наличия секрета — env может измениться между загрузкой конфига и отправкой.",
   "fix_hint": "Добавить в MailboxCfg поле `password: str`, извлечь значение из `self._env[pw_env]` при построении MailboxCfg. Сохранять сам пароль в конфиге (immutable после load) безопаснее, чем полагаться на изменяемое окружение."
  },
  {
   "line": 220,
   "severity": "medium",
   "summary": "Минимальный YAML-парсер не экранирует спецсимволы в ключах и не поддерживает многострочные значения",
   "scenario": "Если в конфиге присутствует ключ или значение с символами `{}[]:|>`, парсер может вернуть некорректную структуру или упасть. Пример: signature содержит многострочный текст с `>` (folded scalar) — парсер вернёт строку '>footer text', а не развёрнутое значение. Другой сценарий: ключ содержит двоеточие (например, 'http://example.com' как ключ) — регулярка `_MAP_ENTRY_RE` разобьёт его неправильно, нарушив структуру конфига.",
   "fix_hint": "Добавить обработку YAML-конструкций `>`, `|` для многострочных значений. Для ключей с двоеточием требовать кавычки или запретить использование двоеточий в ключах конфига."
  },
  {
   "line": 139,
   "severity": "medium",
   "summary": "_strip_comment не обрабатывает escaped quotes внутри строк",
   "scenario": "Если в конфиге присутствует строка вида `signature: \"footer with \\\" quote and # not a comment\"`, парсер интерпретирует `\\\"` как конец кавычки и обрежет остаток строки после `#` как комментарий. Результат: signature содержит неполное значение, нарушается юр-атрибуция (ФЗ-38: отсутствие полной подписи с ИНН). Если подпись критична для отписки, это fail-open: письма уходят без корректной подписи.",
   "fix_hint": "Добавить обработку escaped quotes (`\\\"`, `\\'`) в _strip_comment: не завершать кавычную область при `\\\"`, а продолжать до следующей незаэкранированной кавычки."
  },
  {
   "line": 324,
   "severity": "low",
   "summary": "provider_split.routing не валидируется на полноту покрытия mx_provider",
   "scenario": "Если routing не содержит записи для провайдера получателя (например, mx_provider='yahoo'), отправщик не найдёт пул и может либо упасть, либо выбрать дефолтный пул (не специфицированное поведение). Это не потеря письма, но неопределённость в маршрутизации: если дефолт не задан, письмо не отправится; если задан — может нарушиться рамп-логика (использован не тот пул).",
   "fix_hint": "Добавить проверку: либо routing должен содержать запись 'default', либо документировать поведение при отсутствии провайдера в routing."
  },
  {
   "line": 370,
   "severity": "low",
   "summary": "window.days допускает дубликаты (например, [1,1,2])",
   "scenario": "Если days=[1,1,2], валидация пропустит список (все значения в диапазоне 1..7), но логика планирования может обработать дубликаты некорректно: либо дважды планировать в понедельник, либо игнорировать дубликат (неопределённое поведение). Если планировщик использует `if day in window.days`, дубликаты не критичны; если итерирует список — возможны дубли отправок в один день.",
   "fix_hint": "Преобразовать days в set при построении WindowCfg: `days=tuple(sorted(set(days)))`, выбросить ConfigError при дубликатах."
  }
 ]
}