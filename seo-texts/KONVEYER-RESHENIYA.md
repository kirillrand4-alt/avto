# Решения по конвейеру. Только дозапись. В прод — после трёх согласий

Порядок: замер числом -> предложение с сухим прогоном -> проба на различение
(с отрицательными случаями) -> две подписи -> внедрение -> запись факта.
Возражение с числом блокирует; возражение без числа — нет.
Изменение без НАЗВАННОЙ причины не применяем.

---

## Р-001. Стадия C: один выбор адресата вместо двух расходящихся правил

**Кто:** 3-я сессия. **Дата:** 05.08.

**Замер** (живая enrich.db, 20 398 адресов, 5 095 компаний):
    роль вне семи точных ключей _ROLE_RANK              5 141
      из них ТЕХНИЧЕСКАЯ или ЗАКУПОЧНАЯ                   397  <- ранг 9, хуже info@
    v1 и v2 выбрали РАЗНЫЙ адрес                         967 из 3 170
    best_email = кадры/пресса/бухгалтерия                  98
    адрес у нескольких ИНН                                113 адресов / 271 компания
    есть технический адрес, а письмо на общий              182

**Корень:** `_best_by_role` (enrich_contacts.py:405, пишет companies.best_email) —
точное совпадение строки по 7 ключам; `best_email_v2` (:5257) — подстроки с
запретами. Расходятся по существу: «бухгалтерия» у первого лучше «общего», у
второго запрещена.

**Предложение:** `p25_rol_adresata.py` — роль по подстроке С ГРАНИЦЕЙ части адреса,
свой домен как провенанс, запрещённый адрес помечается видом (не выбрасывается),
причина выбора возвращается строкой.

**Проба на различение:** 10 случаев, 2 отрицательных. Враньё 0 из 10, расхождений
со старым правилом 8 из 10.

**Сухой прогон** (5 095 компаний), разложен по причинам:
    общий/слабый -> ТЕХНИЧЕСКИЙ или закупки      209
    чужой домен -> домен предприятия             154
    адрес посредника -> адрес предприятия        144
    непокупающий отдел -> годный адрес           126
    был пуст -> назначен адресат                 528
    прочее изменение                             523   <- ПРИМЕНЯТЬ НЕ ПРЕДЛАГАЮ
    стало пусто (только кадры/бухгалтерия)        16   <- верно, слать некому

**Прошу применить 1 161** (четыре названные группы + «был пуст»); 523 «прочих»
разобрать отдельно.

**Найдено в собственном предложении при сухом прогоне:** правило «адрес у
нескольких предприятий» переносится с телефонов на почту НЕ буквально — у холдинга
один закупщик на несколько юрлиц (zakupki@tatprof.ru у 2 ИНН, catmen@rusal.com у 5).
Первый заход их убивал, ложных «слать некому» было 199. Починено различением по
домену, стало 0.

**Подписи:** 1-я сессия — ____ ; 2-я сессия — ____
