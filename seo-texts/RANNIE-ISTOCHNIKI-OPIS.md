# Три ранних источника: что пробовала, какие коды, берём или нет

Замер 16.09.2026. Всё ниже проверено живым запросом с двух слоёв — из песочницы и
с сервера владельца. Правило, из которого собран файл: **код ответа HTTP это ОТВЕТ.**
401, 403, 451, 503 значат «хост достигнут, вход закрыт». Недоступен только тот, кто
не ответил вовсе — в таблицах это код `0`.

Короткий ответ на три вопроса вынесен в конец, в раздел 5.

---

## 1. ФРП, frprf.ru — **БЕРЁМ, но медленно. Источник был закрыт не с той стороны**

### 1.1. Почему было закрыто

```
из песочницы        403, 1606 байт, заголовок server: ddos-guard
                    тело: «It looks like that the website owner has restricted
                    access from your current IP address»
с сервера владельца 200. Главная 207 595 байт, /press-tsentr/novosti/ 85 091 байт,
                    sitemap.xml 200, sitemap-iblock-9.xml = 853 записи
```

Это **геоблок по стране выхода**, а не защита от ботов, не неверный адрес и не наш
дефект. Сервер владельца (российский IP) отдаёт тот же адрес кодом 200. То есть
«frprf.ru 403» из прошлых заметок было свойством песочницы, а не источника.

### 1.2. Мобильные прокси: владелец разрешил жечь — жечь нечего

Проверены все три адреса из `C:\sender\proxies-mobile.txt`, каждый с авторизацией,
запросом к frprf.ru и к fedresurs.ru:

```
194.143.150.98:1650   код 0, WinError 10054 — соединение закрыто удалённой стороной
77.51.189.183:10498   код 0, то же
bproxy.site:10917     код 0, то же
```

**Все три прокси мертвы.** Общий пул `dolphin-proxies.txt` не трогала по указанию.
Прокси и не понадобился: сервер отдаёт 200 напрямую.

### 1.3. Почему col_frp дал 0. Проверены все три версии поломки по отдельности

Коллектор лежит в `C:\sender\server\news_scan.py` (1866 строк, в git его нет).
Что он делает: берёт **пресс-ленту** `https://frprf.ru/press-tsentr/novosti/` через
`VC._fetch`, вытаскивает из неё ссылки, и оставляет те, где текст длиннее 25 знаков
и содержит одно из слов «завод, цех, производств, линию, займ, проект, млн, млрд».
Реестр займов он не читает вовсе.

```
ВЕРСИЯ 1: источник закрыт ................. НЕТ. VC._fetch вернул 85 091 знак,
                                            method=direct, meta={'http_status': 200}
ВЕРСИЯ 2: дедуп seen_news съел ............ НЕТ. На странице 6 новостей,
                                            в seen_news НИ ОДНОЙ из шести.
                                            Ключей с frprf.ru всего 14 из 139 030
                                            КОНТРОЛЬ: выдуманный ключ -> 0
ВЕРСИЯ 3: фильтр отбросил ................. НЕТ. Логика коллектора, прогнанная
                                            на живой странице, дала 6 элементов из 6
                                            КОНТРОЛЬ: выдуманное слово «щварцкопфер»
                                            в тех же ссылках -> 0
```

Все три названные версии отпадают. Остаётся четвёртая, и она видна прямо в коде:

```python
html, method, meta = (VC._fetch(path) if hasattr(VC, '_fetch') else (_get(path), 'direct', {}))
if not html or (isinstance(meta, dict) and meta.get('captcha_type')):
    continue          # <- отказ хоста ТИХО превращается в пустой список
```

**Коллектор молча глотает отказ хоста.** А отказ этот массовый: DDoS-Guard держит
частотный заслон. Мои замеры на живом сервере:

```
постраничный список ?PAGEN_1=1..14 ........ 503 на ВСЕХ 14 страницах
70 карточек подряд с паузой 1,5 с ......... 503 на 61, 200 на 8, 404 на 1
150 карточек подряд без пауз .............. 200 на 31, не открылось 119
одиночная /press-tsentr/novosti/ .......... 200 стабильно
```

То есть col_frp вернул 0 не потому, что в мире пусто, а потому что в момент прогона
хост ответил 503, и `if not html: continue` сделал из этого «новостей нет».
Это ровно тот случай из правил: **одинаковый ноль неотличим от честного нуля без
контроля.** Чинится одной строкой — писать код ответа в лог коллектора; и второй —
ставить паузу.

### 1.4. Пробная выгрузка и что в записи есть

`seo-texts/rannie_frp.py`. Идёт по sitemap, не по постраничному списку, с паузой.

```
sitemap-iblock-9.xml ........... 853 записи, из них 471 карточка новости
lastmod .......................... есть, но ПРОТУХШИЙ: верхние значения 2020-12-23,
                                   сортировка по нему поднимает старьё 2015-2017 годов
взято карточек (70 попыток) ...... 8 (остальное 503)
у 8 карточек: дата ............... 8 из 8
              сумма займа ........ 3 из 8 («610 млн руб», «2,43 млрд руб»)
              название юрлица .... 1 из 8 («НПО Лаборатория специальных сталей и сплавов»)
              ИНН ................ 0 из 8
КОНТРОЛЬ выдуманного слова в выгрузке .... 0
```

Сверка с живой базой `C:\sender\_ops\PARK-BAZA-EDINAYA-3S.csv` (6 754 929 байт,
время файла 11.08.2026 09:13, разделитель «;», utf-8-sig): в базе **2 077 предприятий
по названию и 2 482 ИНН**. Из выгрузки ФРП совпало 0 имён, новых 1.
КОНТРОЛЬ: выдуманное название в базе -> 0. Числа маленькие потому, что взялось 8
карточек, а не потому, что ФРП бедный — это замер прибора, а не источника.

### 1.5. Вывод по ФРП

**БЕРЁМ.** Источник открыт с сервера владельца. Но брать надо по-другому, чем сейчас:

- ходить по sitemap, а не по `?PAGEN_1=` (постраничный список гарантированно 503);
- пауза не меньше 5 секунд, иначе 503 на 87 % запросов;
- ежедневный инкремент (первая страница ленты читается стабильно) реален,
  архивный обход 471 карточки — это часы, планировать отдельно;
- **ИНН в источнике нет.** Есть название, дата и сумма. ИНН добывается вторым
  шагом через dadata по названию (готовый приём — `seo-texts/pishchevka_dadata.py`).

---

## 2. Федресурс, fedresurs.ru — **НЕ БЕРЁМ. Нужен платный доступ**

Владелец велел дойти до диагноза и остановиться. Обходы не пробовала.
Воспроизводимая проба: `seo-texts/rannie_fedresurs.py`.

### 2.1. Коды ответа, с сервера владельца (песочница дала то же самое)

```
https://fedresurs.ru/                              401   1 598 байт   server: QRATOR
https://bankrot.fedresurs.ru/                      401   1 598 байт   server: QRATOR
https://fedresurs.ru/backend/companies/search      403     548 байт   тело nginx
https://fedresurs.ru/backend/encumbrances          403     548 байт   тело nginx
https://api.fedresurs.ru/                            0        —       хост не резолвится
https://fedresurs.ru/robots.txt                    200     373 байта
https://fedresurs.ru/api  /help/api  /about/opendata
https://fedresurs.ru/sitemap.xml                   200  22 446 байт   ОДИН И ТОТ ЖЕ размер
                                                                      на любой путь — это
                                                                      оболочка Angular
КОНТРОЛЬ выдуманный поддомен shvartskopfer.        0        —          не ответил вовсе
```

Контроль важен: выдуманный хост даёт **0**, живой даёт **401**. Значит 401 это
именно ответ Федресурса, а не общая поломка сети.

### 2.2. Что именно закрыто

Закрыто **всё публичное сразу**, а не отдельный раздел: и главная, и публичный поиск
сообщений, и карточка юрлица, и `bankrot.fedresurs.ru`. До выгрузки дело не доходит —
страница, с которой её берут, не открывается.

### 2.3. Машинный интерфейс у портала ЕСТЬ, и портал сам его называет

`robots.txt` перечисляет ровно эндпоинты API:

```
Disallow: /backend/companies/search
Disallow: /backend/persons/search
Disallow: /backend/nonresidentcompanies/search
Disallow: /backend/encumbrances          <- залоги, наш ранний индикатор
Disallow: /backend/realestate/search
Disallow: /backend/fnp-search
```

Искать скрытый API, как было с ЕГРЗ, не нужно: он документирован отказным списком.
И 403 на нём — **не Qrator, а проверка заголовков**: тот же адрес с `Referer`,
`Origin` и `X-Requested-With` отдаёт уже **404 с пустым телом**, то есть заслон
пройден, а путь или имена параметров угаданы неверно. Имена надо снимать с фронта,
а не угадывать. Дальше не шла по указанию.

### 2.4. Что нужно от владельца

Три разные вещи, их нельзя путать:

```
для ЧТЕНИЯ оптом   договор и ключ к API Федресурса. Оператор ЕФРСФДЮЛ — АО «Интерфакс»,
                   доступ платный, по договору. Единственный путь без обхода защиты
для ПУБЛИКАЦИИ     квалифицированная электронная подпись. НАМ НЕ НУЖНА: мы читаем
для разовой ручной обычный браузер с живым российским IP, проходящий Qrator.
проверки           Наши мобильные прокси на это не годятся — все три мертвы
```

### 2.5. Вывод по Федресурсу

**НЕ БЕРЁМ** без платного доступа. Хост достижим, машинный интерфейс существует и
назван, но публичного входа нет ни с одного из наших слоёв.

---

## 3. Техприсоединение к электросетям и к газу — **публичные данные ЕСТЬ, но заявитель в них НЕ НАЗВАН**

Это самый развёрнутый раздел: сюда переложено время, снятое с Федресурса.
Рабочий клиент: `seo-texts/rannie_tehprisoedinenie.py` (читается **из песочницы**).

### 3.1. Что вообще обязаны публиковать

```
электросети   ПП РФ № 24 от 21.01.2004, стандарты раскрытия информации.
              п. 19«д» — «Информация о поданных заявках на технологическое
              присоединение, заключённых договорах и выполненных присоединениях»
газ           ПП РФ № 872 от 29.10.2010 + приказ ФАС № 960/22.
              «Информация о регистрации и ходе реализации заявок на подключение
              (технологическое присоединение) к газораспределительным сетям»
инвестпрограммы  ПП РФ № 977 — перечень объектов сетевой организации
```

Данные публикуются регулярно и в машиночитаемом виде. Проблема не в доступе.

### 3.2. Электросети: снято и разобрано

Страница раскрытия Россети Центр (11 областей, ~9 млн потребителей):
`https://www.mrsk-1.ru/customers/services/tp/information/` — **код 200 из песочницы**,
370 078 байт, на ней **616 файлов xls/xlsx**, помесячно с 2012 года.

Свежий файл `MRSK_Centre_TP_01082026.xlsx`, код 200, 245 448 байт, разобран:

```
строк в файле ........................... 3 405
строк-договоров ТП ...................... 3 383
диапазон дат заключения ................. 2026-08-01 ... 2026-08-31, 31 различная дата
строк с мощностью ....................... 3 381; сумма 89 579,1 кВт; максимум 9 500 кВт
ИНН отдельным полем ..................... 0
строк с наименованием заявителя ......... 0
КОНТРОЛЬ несуществующий файл ............ 404 (200 означал бы сломанный прибор)
КОНТРОЛЬ «щварцкопфер» в выгрузке ....... 0
```

**Прибор пришлось чинить дважды, и оба раза он врал в мою пользу.** Записываю, чтобы
следующая сессия не купила это заново:

```
первый прогон сказал «ИНН: 348» ..... это хвосты float в стоимости:
                                      2204394.4900000002 -> «4900000002».
                                      Настоящих ИНН ноль
первый прогон сказал «юрлиц: 3396» .. это «ПАО «Россети Центр»» в каждой строке,
                                      то есть САМА сетевая компания, а не заявитель.
                                      Строк с ООО — ноль
первый прогон сказал «дат: 0» ....... даты в сыром xlsx лежат СЕРИЙНЫМ числом Excel
                                      (46239 = 05.08.2026), поиск по «20\d\d-\d\d-\d\d»
                                      честно находил ноль при 3 383 заполненных датах
```

Колонки формы 12, блок построчный: филиал · субъект РФ · № п/п · **номер договора ТП** ·
**дата заключения** · дата исполнения обязательств · **запрашиваемая мощность, кВт** ·
стоимость, руб · **центр питания (подстанция)**. Наименования заявителя и ИНН в форме
не предусмотрено.

### 3.3. Газ: снято и разобрано

`stavkraygaz.ru/upload/iblock/4cf/...xlsx`, код 200, 10 646 байт, разобран —
приложение 6, форма 1, АО «Газпром газораспределение Ставрополь». Строка = газопровод-
отвод или ГРС, колонки = количество поданных, отклонённых, рассматриваемых и
удовлетворённых заявок и объём газа в млн м3. **Заявители не названы.**

Мособлгаз (`mosoblgaz.ru`, из песочницы 403 DDoS-Guard, с сервера 200) отдаёт ту же
форму в старом формате .xls, 568 832 байта — формат тот же, приказ тот же.

### 3.4. Инвестпрограмма: снято и разобрано

`Prikaz_IPR_Rosseti_Centr_2025.zip`, код 200, 5 232 591 байт. Внутри приказ Минэнерго
и приложение `.xlsb` на 6 016 262 байта. Разобрано из `xl/sharedStrings.bin`:

```
строк текста извлечено .................. 47 115
«технологическ» ......................... 63 раза, и все — СВОДНЫЕ категории
                                          («ТП энергопринимающих устройств потребителей
                                          максимальной мощностью до 15 кВт / до 150 кВт /
                                          свыше 150 кВт»)
«ООО» ................................... 0
КОНТРОЛЬ «щварцкопфер» .................. 0
```

Объекты изредка выдают тип потребителя в названии («мясокомбинат … для Техприсоединения»,
«витаминный комбинат … для Техприсоединения»), но это описание объекта сети, а не
юрлицо и не ИНН.

### 3.5. Единственный найденный поимённый реестр — и он ИЗЪЯТ

ДРСК (Дальневосточная распределительная сетевая компания) держит раздел
`https://utp.drsk.ru/tpr_reestr/8` — из песочницы **451 Unavailable For Legal Reasons**,
с сервера владельца **200, 87 328 байт**. На странице стоит живой заголовок:

> «Реестры заявок на технологическое присоединение.
> Реестр заявок на подключение (технологическое присоединение) к электрическим сетям
> по Хабаровскому краю»

А самой таблицы нет: в сыром HTML она **закомментирована** (`<!-- ... </table>-->`).
Из 25 ссылок страницы ни одной на файл; в картинках лежит `xl.gif` — иконка Excel,
то есть выгрузка там когда-то была. Это не наш дефект и не защита: содержимое снято
владельцем сайта.

### 3.6. Что пробовала и чем ответило: сводка кодов

```
www.mrsk-1.ru/customers/services/tp/information/     200  370 078   песочница И сервер
www.mrsk-1.ru/.../MRSK_Centre_TP_01082026.xlsx       200  245 448   песочница
www.mrsk-1.ru/information/standart/investment/       302->200 69 292 песочница
www.mrsk-1.ru/upload/.../Prikaz_IPR_..._2025.zip     200  5 232 591 песочница
stavkraygaz.ru/upload/iblock/4cf/....xlsx            200   10 646   песочница И сервер
34gaz.ru/information-disclosure/...872/              200   34 204   песочница И сервер
muppeskms.ru реестр заявок .pdf                      200  468 484   песочница (текст
                                                                    в CID-кодировке,
                                                                    без pdf-библиотек
                                                                    не читается)
utp.drsk.ru/tpr_reestr/8                             451/200 87 328 песочница/сервер
mosoblgaz.ru                                         403/200 154 956 песочница/сервер
rosseti-sib.ru                                       0/200  151 557 песочница/сервер
ppr.so-ups.ru (портал перспективного развития)       403   38 579   песочница
                                                     403   —        сервер (геоблок обоих)
rosseti.ru / portal-tp.ru / connectgas.ru            200            песочница И сервер
www.rosseti-moscow.ru / gro.gazprom.ru               0              имя не резолвится
КОНТРОЛЬ несуществующий файл на mrsk-1.ru            404            прибор жив
```

### 3.7. Вывод по техприсоединению

**НЕ БЕРЁМ ПОД ГЛАВНУЮ ЦЕЛЬ, но канал не пустой.** Развёрнуто:

- публичные записи есть, их много и они регулярные: один Россети Центр даёт
  **3 383 договора ТП в месяц** с датой и мощностью, и так с 2012 года, 616 файлов;
- **ИНН заявителя в них 0. Наименования заявителя 0.** И это не случайность отдельной
  компании, а устройство обязательных форм: ПП 24 и ПП 872 предписывают раскрывать
  сеть и деньги, а не заявителя. Проверено на трёх независимых формах (форма 12
  электросетей, приложение 6 газа, приложение к инвестпрограмме) — ноль во всех трёх;
- поэтому **сшить эти записи с предприятием нечем**. Для нашей цели — личный номер
  ЛПР предприятия — источник в текущем виде не работает;
- что в нём всё-таки есть и чего выбрасывать нельзя (правило «разделять, а не
  отсеивать»): **дата + мощность + центр питания**. Договор на 9 500 кВт от конкретной
  подстанции в августе 2026 — это доказанный факт строящегося энергоёмкого объекта в
  радиусе этой подстанции. Как самостоятельная цель не годится, как **подтверждение**
  уже найденного другим каналом проекта — годится;
- **что нужно от владельца, если канал всё-таки нужен поимённо:** запрос в сетевую
  организацию на предоставление реестра заявок юридических лиц. Прецедент, что такой
  реестр бывает публичным, зафиксирован (ДРСК, раздел жив, таблица изъята), то есть
  это вопрос к организации, а не к закону.

---

## 4. Что осталось непроверенным — честно

```
ppr.so-ups.ru, портал перспективного развития энергетики СО ЕЭС .... 403 с ОБОИХ слоёв.
   Там лежит СиПР ЭЭС России 2026-2031 с перечнями перспективных потребителей.
   Это единственный из встреченных адресов, закрытый и из песочницы, и с сервера
муниципальные ТСО ................................................. реестр заявок в PDF
   (muppeskms.ru, 468 484 байта) скачивается, но текст в нём с собственной кодировкой
   шрифта; в песочнице pdf-библиотеки не ставятся (cryptography ломает pypdf и pdfminer).
   Разбор возможен, но это отдельная работа
имена параметров /backend/ Федресурса ............................. не снимала, снята задача
```

---

## 5. Короткий ответ

```
ФРП ................ БЕРЁМ. 403 из песочницы = геоблок; с сервера 200.
                     col_frp дал 0 НЕ из-за дедупа (0 из 6 новостей в seen_news)
                     и НЕ из-за фильтра (6 из 6 прошли), а потому что глотает 503:
                     заслон DDoS-Guard бьёт по 87 % запросов без пауз.
                     В записи есть дата и сумма, ИНН НЕТ

Федресурс .......... НЕ БЕРЁМ. 401 QRATOR на всём публичном, 403 nginx на /backend/,
                     api.fedresurs.ru не резолвится. Машинный интерфейс есть и назван
                     в robots.txt. Нужно от владельца: платный договор и ключ API
                     (оператор — АО «Интерфакс»). ЭЦП нужна только для публикации,
                     нам не нужна

Техприсоединение ... НЕ БЕРЁМ ПОД ЦЕЛЬ. Данные публичные и обильные: 616 файлов,
                     3 383 договора за август 2026, мощность и дата у 3 381 строки.
                     Но ИНН 0 и наименований заявителя 0 в трёх независимых формах.
                     Годится как подтверждение, не как источник имён
```
