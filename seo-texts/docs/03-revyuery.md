# 03. Ревьюеры, свипы и отчёты качества

Область: `seo-texts/review_*.py`, `engineer_sweep.py`, `eng_fix_review.py`, `rewrite_manifest.py`,
`model_compare.py`, `model_extract_test.py`, `role_model_test.py`, каталоги `eng-sweep*/`,
`opus-test/`, `manifest/`, отчёты `review-*.md`, `seo-effect-report.md`.

Все пути ниже — от `/home/user/avto/seo-texts/`, если не сказано иначе.

---

## 1. Что это и зачем

Механический QA (`qa_text.py`) ловит формальные нарушения: длинные тире, штампы, `<ul>`, цифры не из
payload. Он НЕ видит, что текст «звучит как перевод с немецкого», что автор перепутал адсорбцию с
химической реакцией, или что 760 однотипных страниц выглядят для Google как scaled content abuse.

Эта область — второй, семантический контур проверки. Он весь построен по одной схеме: **берём готовые
тексты, склеиваем в корпус, отдаём модели с промптом-персоной, складываем ответ в markdown-отчёт**.
Никакой автоматики поверх — человек читает отчёт и решает.

Ролей-персон в проекте четыре, и все четыре живут как константы в модулях:

| Персона | Где определена | Что ищет |
|---|---|---|
| Филолог-редактор | `review_philolog.py:20-44` (`PERSONA`) | кальки, канцелярит, неестественные коллокации, «роботность» интонации |
| Инженер-пневматик | `review_engineer.py:20-43` (`PERSONA`) | неверные физические зависимости, путаница терминов, ошибки подбора, опасные советы |
| Яндекс-SEO | `review_seo.py:24-40` (`YANDEX`) | Баден-Баден, переспам, шаблонность, near-duplicate между фасетами |
| Google-SEO | `review_seo.py:42-59` (`GOOGLE`) | Helpful Content, E-E-A-T, scaled content abuse (промпт на английском, ответ требует по-русски) |

Персоны переиспользуются импортом, а не копипастой: `review_all_50.py:8-10` тянет все четыре,
`review_engineer_50.py:7` — инженера, `guest-posts/review_sample.py:7-8` — филолога и инженера.
Есть и пятая копия инженерной персоны — инлайн в `regen_driver.py:29-37` (`ENG_HEAD`), уже не импорт,
а самостоятельный урезанный текст для проверки каждой 6-й перегенерированной страницы.

Кроме ревью текстов каталога в этой же папке живут три соседних, но отдельных сюжета:
ревью **писем холодной рассылки** (`review_variations.py`, `review_select.py`), ревью **кода сендера**
(`review_via_provider.py`, `eng_fix_review.py`) и **сравнение моделей по цене/качеству**
(`model_compare.py`, `model_extract_test.py`, `role_model_test.py`). Они попали сюда исторически, к
759 SEO-страницам отношения не имеют.

---

## 2. Точки входа и как запустить

**Общее для всех.** Каждый скрипт вызывает провайдерский API (`gen_provider.make_client()` /
`gen_provider.call()` / `gen_provider._raw_stream()`), модель по умолчанию везде `claude-fable-5`.
Нужен `PROVIDER_API_KEY` — читается из `seo-texts/.env` или из окружения (`gen_provider.py:118-133`).
Все скрипты запускаются из каталога `seo-texts/` (часть из них строит пути от `__file__`, часть —
от CWD; см. «Грабли»).

> ВНИМАНИЕ: любой запуск ниже жжёт квоту провайдера владельца. Ни один из них не нужен для текущей
> работы — все отчёты уже сгенерированы и лежат в репозитории. Запускать только по явной просьбе.

### 2.1 Ревью текстов каталога

```bash
cd /home/user/avto/seo-texts

# филолог по списку слагов (один вызов, весь корпус разом) -> review-philolog.md
python3 review_philolog.py philolog-sample.txt

# инженер по списку слагов (один вызов) -> review-engineer.md
python3 review_engineer.py review-battery-sample.txt

# Яндекс + Google подряд -> review-yandex.md и review-google.md
python3 review_seo.py review-battery-sample.txt
python3 review_seo.py            # без аргумента — вшитый список из 3-6 слагов, review_seo.py:66-71

# инженер на 50 случайных, 5 пачек по 10 параллельно -> review-engineer-50.md
python3 review_engineer_50.py    # аргументов НЕТ, выборка фиксирована seed=50

# все 4 персоны × 5 пачек по 10 (20 вызовов) -> review-all-50.md
python3 review_all_50.py         # аргументов НЕТ; выборка задаётся SEED (по умолчанию 2026)
SEED=1234 python3 review_all_50.py   # другая выборка

# спот-чек 15 топ-трафиковых страниц всеми 4 персонами (8 вызовов) -> review-spot15.md
python3 review_spot15.py         # аргументов НЕТ, читает spot15.txt
```

Формат входного файла со слагами — по одному слагу в строке, слаг = имя без префикса/расширения от
`gen/result-<slug>.json`. Готовые списки в репо: `philolog-sample.txt` (14), `review-battery-sample.txt`
(13), `spot15.txt` (15), `eng50-sample.txt` (50), `review-all-50-sample.txt` (50), `sweep-facets.txt`
(408), `regen-brands.txt` (351). Все слаги во всех этих файлах проверены — соответствующие
`gen/result-*.json` существуют.

### 2.2 Инженерный sweep (массовая проверка с парсимым выводом)

Это единственный ревьюер, чей вывод машинно разбирается и подаётся дальше в конвейер регенерации.

```bash
cd /home/user/avto/seo-texts
python3 engineer_sweep.py sweep-facets.txt     # 408 слагов -> 41 пачка
python3 engineer_sweep.py regen-brands.txt     # 351 слаг  -> 36 пачек
```

Аргумент обязателен (`engineer_sweep.py:55` читает `sys.argv[1]` без проверки — без аргумента будет
`IndexError`). Имя аргумента определяет теги всех выходов (`engineer_sweep.py:11`):
`_TAG = re.sub(r'\W+','_', basename(argv[1])).replace('_txt','')`, то есть `regen-brands.txt` →
`regen_brands`.

Что создаётся:
* `eng-sweep-<TAG>/<n>.txt` — сырой ответ модели по пачке n (кэш, `engineer_sweep.py:12-13, 40-42`);
* `engineer-flagged-<TAG>.txt` — слаги с оценкой `critical` или `important` (`:74`);
* `eng-sweep-report-<TAG>.md` — человекочитаемый свод (`:86`).

### 2.3 Манифест (деловое письмо владельца)

```bash
python3 review_manifest.py     # читает manifest.txt -> пишет review-manifest.md (review_manifest.py:34,39)
python3 rewrite_manifest.py    # читает manifest.txt -> пишет manifest-clean.md (rewrite_manifest.py:31,36)
```

`review_manifest.py` — персона «филолог + скептичный адресат», ищет и языковые, и логические дыры.
`rewrite_manifest.py` — чистовая редактура ТОЛЬКО по языку, с жёстким запретом трогать цифры
(`rewrite_manifest.py:14-16` перечисляет их поимённо). Оба без аргументов.

### 2.4 Ревью писем рассылки

```bash
python3 review_variations.py   # читает variations-pool.json -> пишет variations-100.json
```

Единственный ревьюер во всей области, который **вызывается не человеком, а кодом**:
`night_orchestrator.py:43-44` запускает его подпроцессом после сборки пула вариаций.

`review_select.py` делает ровно то же (тот же пул, тот же выход `variations-100.json`), но его никто
не вызывает — см. раздел 6.

### 2.5 Ревью кода и сравнение моделей (соседние сюжеты)

```bash
python3 review_via_provider.py                 # модель по умолчанию fable-5
python3 review_via_provider.py claude-sonnet-5 # модель первым аргументом (review_via_provider.py:134)
python3 eng_fix_review.py                      # REVIEW_MODEL=... переопределяет модель (eng_fix_review.py:37)
python3 model_compare.py <texts.json>
python3 model_extract_test.py [N]              # N — сколько текстов, по умолчанию 40
python3 role_model_test.py <texts.json> [N]    # N — целевое число email-«каши», по умолчанию 100
```

Все пять читают/пишут в **захардкоженный scratchpad чужой сессии** — он больше не существует, см.
раздел 6.

---

## 3. Как устроено внутри

### 3.1 Единый скелет ревьюера текста

Четыре шага, повторяющиеся почти дословно в `review_engineer.py`, `review_philolog.py`, `review_seo.py`:

1. `readable(slug)` (`review_engineer.py:12-17`, `review_philolog.py:12-17`) — берёт
   `gen/result-<slug>.json` и `gen/payload-<slug>.json`, вырезает теги регуляркой `<[^>]+>`,
   схлопывает пробелы, отдаёт `### [категория] H1\nтекст`.
   В `review_seo.py:11-21` версия богаче: добавляет URL, тип страницы (бренд/фасет), длину в знаках
   и список всех `href` из текста — SEO-персонам нужна перелинковка.
   В `review_all_50.py:21-25` версия добавляет `ID <slug>` в заголовок, чтобы модель могла ссылаться
   на конкретную страницу.
2. Склейка корпуса: `'\n\n'.join(readable(s) for s in slugs)`.
3. Один вызов `gp.call(client, [{'role':'user','content': PERSONA + корпус}], model='claude-fable-5')`.
   `gp.call` (`gen_provider.py:316`) делает до 8 попыток с экспоненциальными паузами, при HTTP 400
   на `thinking` — снимает thinking и повторяет.
4. Запись `.md`.

### 3.2 Пачки и параллельность

Как только корпус перестаёт влезать в один вызов, скрипты режут его на пачки по 10 текстов и гоняют
через `ThreadPoolExecutor`:

| Скрипт | Пачек | Воркеров | Вызовов всего |
|---|---|---|---|
| `review_engineer_50.py:30,33` | 5 × 10 текстов | 3 | 5 |
| `review_all_50.py:44,46,48` | 5 × 10, × 4 персоны | 4 | 20 |
| `review_spot15.py:21,23,25` | 2 (8 + 7 текстов), × 4 персоны | 4 | 8 |
| `engineer_sweep.py:56,59` | len(slugs)/10 | 4 | 41 / 36 |
| `review_variations.py:54` | по 1 письму | 8 | = размер пула |

В пачечных режимах к персоне приклеивается инструкция «пройдись по КАЖДОМУ, не пропуская»
(`review_engineer_50.py:14-16`, `review_all_50.py:16-18` — константа `BATCH_NOTE`).
`review_spot15.py:12` эту константу ещё и патчит на лету через `.replace('10 текстов', f'{len(slugs)} текстов')`,
потому что его пачки не по 10.

Сбой пачки не роняет прогон: исключение перехватывается и в отчёт попадает строка
`[пачка N не удалась: ...]` (`review_engineer_50.py:20-21`, `review_all_50.py:33-34`,
`review_spot15.py:16-17`). Проверено: в `review-all-50.md`, `review-engineer-50.md`,
`review-spot15.md` таких строк ноль — все прогоны прошли целиком.

### 3.3 Воспроизводимость выборок

Обе «случайные 50» фиксированы seed-ом и **проверены пересчётом**:

* `review_engineer_50.py:27-28` — `random.Random(50).sample(allslugs, 50)`; результат побайтно
  совпадает с `eng50-sample.txt`.
* `review_all_50.py:41-42` — `random.Random(int(os.environ.get('SEED','2026'))).sample(allslugs, 50)`;
  совпадает с `review-all-50-sample.txt`.
* `allslugs` в обоих случаях — отсортированный список всех 759 слагов из `gen/result-*.json`.
* Выборки разные: пересечение между ними — 4 слага из 50.

Важно: воспроизводимость держится, пока в `gen/` ровно те же 759 файлов. Добавится/исчезнет страница —
`sample` вернёт другой набор при том же seed.

### 3.4 Sweep: единственная ветка, замкнутая в конвейер

`engineer_sweep.py` отличается от остальных ревьюеров тем, что требует от модели строгий формат вывода
(`engineer_sweep.py:25-28`):

```
<slug> | <critical|important|minor|clean> | <очень краткое описание ошибки или ->
```

и разбирает его регуляркой `engineer_sweep.py:68`. Слаги с `critical`/`important` уходят в
`engineer-flagged-<TAG>.txt`, оттуда — в `defect-map.json`, оттуда — в `regen_driver.py:18`, который
инжектирует конкретный дефект в промпт регенерации (`regen_driver.py:102-108`, `defect_directive`).

**Цепочка проверена численно:**

* `sweep-facets.txt` (408 слагов) + `regen-brands.txt` (351) = 759, пересечение 0 → два свипа вместе
  накрыли ВСЕ страницы каталога ровно по одному разу;
* `eng-sweep/` — 41 файл-пачка, распознано 398 строк, флагнуто 89 (`eng-sweep-report.md:3-5`);
* `eng-sweep-regen_brands/` — 36 файлов, распознано 340 строк, флагнуто 124
  (`eng-sweep-report-regen_brands.md:3-5`);
* 89 + 124 = 213, и множество ключей `defect-map.json` **в точности равно** объединению этих двух
  flagged-файлов (сверено скриптом: `dm == union` → True);
* `defect-map-resolved.json` — 208 ключей: 8 коротких/битых слагов (`remeza`, `kraftmann`, `magnus`,
  `pneumatech`, `rkz`, `xeleron`, `evrazkompressor`, `podgotovka-vozdukha__separatory-tsentrobezhnye-tsyklonnye__berg`)
  заменены на 3 полных, остальные отброшены. Для всех 208 ключей `gen/result-*.json` существует;
  для 8 ключей из `defect-map.json` — нет.

Скрипта, который делает `defect-map.json` из flagged-файлов, в репозитории нет — судя по всему,
маппинг «слаг → [severity, описание]» собирался разово вручную/сессией из
`eng-sweep-report*.md`. Формат значения: `["critical"|"important", "текст дефекта"]`.

### 3.5 Ревью писем: `review_variations.py`

Логика (`review_variations.py:13-21` — рубрика, `:27-49` — вызов, `:51-78` — отбор):
модель прогоняет письмо через 6 линз за один вызов (доставляемость / ФЗ-38 и ФЗ-152 / факт-чек /
корректор / адвокат ЛПР / продажник) и возвращает JSON
`{"critical":[...], "warn":[...], "score":0-100, "one_line":"..."}`.
Дальше: письма с непустым `critical` выбрасываются, остальные сортируются по `score`, отбирается 100
с балансировкой «не более 5 из одного бакета» (`:61-66`), при недоборе — добор без ограничения (`:67-71`).

Кэш на письмо: `review-cache/v<id:03d>.json` (`:28`). Каталог `review-cache/` в `.gitignore`
(`seo-texts/.gitignore:5`) и локально отсутствует.

Вызов идёт не через `gp.call`, а напрямую через приватный `gen_provider._raw_stream(..., 900, thinking=False)`
(`:39`) — 4 попытки, thinking выключен, лимит 900 токенов на ответ.

### 3.6 Ревью кода: `review_via_provider.py` и `eng_fix_review.py`

`review_via_provider.py` — статический анализ 20 модулей сендера чужими руками:
список модулей жёстко в `:19-40`, ревью-промпт с приоритетами (потеря данных, ФЗ-38, доставляемость,
дедуп, гонки, инъекции) в `:42-64`, ответ строго JSON `{"findings":[{line,severity,category,summary,failure_scenario}]}`.
Модуль больше 750 строк режется на куски по 700 с перекрытием 40 (`:96-101`). Кэш по модулю в
scratchpad (`:88`). Выходы: `sender/REVIEW-FINDINGS.json` и `sender/REVIEW-FINDINGS.md` (`:16-17`).

`eng_fix_review.py` — ревью КОРРЕКТНОСТИ трёх фикс-коммитов, хэши зашиты в `:12-16`
(`4589553` ЮРИДИКА ФЗ-38, `8bcf449` ДОСТАВЛЯЕМОСТЬ, `51ae558` MEDIUM). Берёт `git show <commit> -- '*.py' ':!*/tests/*'`
(`:34`), обрезает до 60000 символов, просит вердикт `ok|incomplete|wrong|regression` по каждому изменению.

### 3.7 Сравнение моделей

Три скрипта с общей идеей: **fable-5 — эталон, остальные модели сравниваются с ним, метрика согласия**.
Модели везде одни и те же (`model_compare.py:14`, `model_extract_test.py:18`, `role_model_test.py:16`):
`claude-fable-5`, `claude-opus-4-8`, `claude-sonnet-5`, `claude-haiku-4-5`.
Цены (офиц. $/1M вход,выход) захардкожены одинаково: fable 10/50, opus 5/25, sonnet 3/15, haiku 1/5.
Оценка токенов грубая — `len(строки)/3`.

* `model_compare.py` — задача extract_roles, метрики: jaccard по email, согласие по ролям,
  по флагу конкурента, по owner_match, по выбору ЛПР. Отчёт `scratchpad/model_compare_report.json`.
* `model_extract_test.py` — то же на других данных плюс автоматическая рекомендация по порогам
  `:127-131` (email_recall ≥ 90, role_match ≥ 85, owner/competitor ≥ 90).
* `role_model_test.py` — узкая задача: только определение роли для email-«каши», которую
  детерминированный экстрактор (`server/contact_extract.py`, импортируется в `:11`) не разобрал.
  Порог рекомендации — согласие ≥ 95% (`:114-118`).

Итоги этих прогонов уже выжаты в два коротких документа-решения:
`MODEL-COST-DECISION.md` (10 сайтов, вывод: haiku на массовый вал + fable на спорные) и
`MODEL-EXTRACT-DECISION.md` (25-26 сайтов, вывод: гибрид не нужен, haiku 9.1× дешевле при −10% recall).
Два документа дают **разные рекомендации** — второй отменяет гибрид из первого. Читать надо второй.

---

## 4. Данные и где они лежат

### 4.1 Входы

| Файл | Что это | Кто читает |
|---|---|---|
| `gen/result-<slug>.json` (759) | готовый текст, поле `text_html` | все ревьюеры текстов |
| `gen/payload-<slug>.json` (759) | исходные данные страницы; используются только `category`, `h1`, `url`, `page_brand` | `readable()` во всех ревьюерах |
| `philolog-sample.txt` (14) | ручная выборка для филолога | `review_philolog.py` |
| `review-battery-sample.txt` (13) | ручная выборка для батареи | `review_engineer.py`, `review_seo.py` |
| `spot15.txt` (15) | топ-трафиковые страницы по SEO-выгрузке | `review_spot15.py` |
| `sweep-facets.txt` (408) | все НЕ-брендовые страницы | `engineer_sweep.py` |
| `regen-brands.txt` (351) | все брендовые страницы | `engineer_sweep.py` |
| `eng50-sample.txt`, `review-all-50-sample.txt` (по 50) | **выходы**, не входы: скрипты сами их пишут для воспроизводимости | — |
| `manifest.txt` (9.3 КБ) | деловое письмо владельца | `review_manifest.py`, `rewrite_manifest.py` |
| `variations-pool.json` (215 КБ) | пул сгенерированных вариаций письма-1 | `review_variations.py`, `review_select.py` |

### 4.2 Выходы — отчёты

| Файл | Размер | Чем сделан | Итоговая оценка в тексте |
|---|---|---|---|
| `review-philolog.md` | 18 КБ | `review_philolog.py` | **6/10** «пропитаны тонкой искусственностью» |
| `review-engineer.md` | 8.9 КБ | `review_engineer.py` | **8/10** «можно публиковать после исправлений» |
| `review-yandex.md` | 17 КБ | `review_seo.py` | критический риск — одинаковая подпись эксперта на 760 страницах |
| `review-google.md` | 16 КБ | `review_seo.py` | критично — «очевидные признаки Scaled Content Abuse» |
| `review-engineer-50.md` | 42 КБ | `review_engineer_50.py` | 5 пачек по 10, все прошли |
| `review-all-50.md` | 264 КБ | `review_all_50.py` | 4 персоны × 5 пачек, все 20 прошли |
| `review-spot15.md` | 107 КБ | `review_spot15.py` | 4 персоны × 2 пачки, все 8 прошли |
| `review-manifest.md` | 22 КБ | `review_manifest.py` | язык + аргументация письма |
| `manifest-clean.md` | 9.5 КБ | `rewrite_manifest.py` | чистовик письма |
| `eng-sweep-report.md` | 18 КБ | `engineer_sweep.py` (старая версия) | 398 строк: 41 critical, 48 important, 25 minor, 284 clean |
| `eng-sweep-report-regen_brands.md` | 25 КБ | `engineer_sweep.py` | 340 строк: 60 critical, 64 important, 18 minor, 198 clean |
| `seo-effect-report.md` | 5.8 КБ | **не скриптом**, вручную/сессией | модель эффекта текстов, см. ниже |
| `sender/REVIEW-FINDINGS.json` | 138 КБ | `review_via_provider.py` (коммит `757e488`, модель sonnet-5) | 112 находок: 18 critical, 27 high, 46 medium, 21 low |
| `sender/REVIEW-FINDINGS.md` | 8.1 КБ | **не скриптом** — см. раздел 6 | ручной fact-checked roadmap |

`seo-effect-report.md` — не вывод ни одного скрипта в репозитории (grep по всем `.py` даёт только
упоминание в `seo-effect-v2.md:67` и в `docs/01`). Это аналитическая записка: базовая линия из
SEO-выгрузки 01.01–15.07.2026 (13 453 клика по сайту, 220 кликов у 759 категорий, 277 страниц вообще
без поискового присутствия), CTR-кривая `CTR ≈ 9,3% × позиция^-0,97`, три сценария прироста
(+52% / +94% / +188%) и честная оговорка, что обещанные «13,5 лида/мес» из манифеста не следуют из
данных — прямой эффект «+1–4 лида/мес». Есть более поздний `seo-effect-v2.md`.

### 4.3 Выходы — машинные

* `engineer-flagged.txt` (89 слагов), `engineer-flagged-regen_brands.txt` (124) → `defect-map.json` (213)
  → `defect-map-resolved.json` (208) → вход `regen_driver.py`.
* `variations-100.json` (141 КБ) — отобранная сотня писем, в каждом добавлено поле `review`.
* `sender/REVIEW-FINDINGS.json` — вход для планирования правок сендера.

### 4.4 Каталоги

**`eng-sweep/`** (41 файл) — кэш пачек свипа по `sweep-facets.txt`. Формат файла — по одной строке
на страницу:
```
adsorbtsionnye__enger_2__goryachey-regeneratsii | important | Ресивер стоит перед циклоном, канон: охладитель→циклон→ресивер
```
Пачка 13 не удалась целиком, в файле лежит
`[batch 13 FAIL: RuntimeError('провайдер не отдал ответ за 8 попыток: ...')]` — эти 10 страниц инженером
не проверены (408 слагов − 10 = 398 разобранных строк).

**`eng-sweep-regen_brands/`** (36 файлов) — то же по `regen-brands.txt`. Провалились пачки 5 (10 страниц)
и 36 (последняя, 1 страница): 351 − 11 = 340.

**`opus-test/`** (3 файла) — `payload-atlas-copco-test.json` и два `result-atlas-copco-OPUS-TEST*.json`.
Ранний тест генерации на модели opus для одной страницы Atlas Copco (ключи как у обычного result:
`slug, url, title, description, h1, text_html`). Ни один скрипт в репозитории на этот каталог не
ссылается — артефакт разовой проверки. Воспроизводится через
`python3 gen_provider.py <slug> --payload opus-test/payload-... --out opus-test/result-... --model <модель>`
(`gen_provider.py:463-475`).

**`manifest/`** (3 файла) — `my-text.txt` (оригинал письма), `editor-text.txt` (редактура начальницы),
`editor-diff-review.md` (разбор: что редактура потеряла — целиком раздел 2 про нерелевантные
перенаправления, CTA с просьбой отревьюить тексты, приложенный файл отчёта). Добавлены коммитами
`8f3e06e` и `865fb5f`. Скрипта, который это сгенерировал, в репозитории нет — работа сессии напрямую.

---

## 5. Ограничения и грабли

1. **Кэш свипа запоминает и провалы.** `engineer_sweep.py:40-42` при повторном запуске возвращает
   содержимое `eng-sweep-<TAG>/<n>.txt` без проверки, что там не строка `[batch N FAIL: ...]`.
   То есть повторный прогон НЕ добьёт упавшие пачки 13 / 5 / 36 — их надо удалить руками:
   `rm eng-sweep/13.txt eng-sweep-regen_brands/5.txt eng-sweep-regen_brands/36.txt`.

2. **Тег свипа зависит от имени входного файла.** `engineer_sweep.py:11` делает
   `.replace('_txt','')` по всей строке, а не только с конца. Файл вида `moi_txt_spisok.txt` даст
   тег `moispisok`. Кроме того, `sweep-facets.txt` сейчас дал бы каталог `eng-sweep-sweep_facets/`,
   а НЕ существующий `eng-sweep/` — кэш прошлого прогона не подхватится (см. раздел 6).

3. **Провайдер рвёт стримы.** В логах `eng-sweep.log` и `eng-sweep-brands.log` видны десятки
   `сбой провайдера, ретрай N/7 ... TypeError("unsupported operand type(s) for +=: 'NoneType' and 'str'")`.
   Это штатное поведение шлюза router.cheap; `gen_provider.call` переживает его ретраями, но
   часть пачек всё равно теряется. Планируй, что 2-3% страниц останутся непроверенными.

4. **Персоны дублируются.** Инженерная персона существует в трёх редакциях: полная
   (`review_engineer.py:20-43`), sweep-версия с жёстким форматом вывода (`engineer_sweep.py:15-28`)
   и урезанная инлайн в `regen_driver.py:29-37`. Правка одной не влияет на две другие.

5. **`review_seo.py` без аргумента молча берёт вшитый список** (`:66-71`) — три компрессорных пилота
   плюс те из трёх некомпрессорных, для которых нашёлся result-файл. Легко получить отчёт по 3
   страницам, думая, что прогнал по всем.

6. **Слаги, которых нет, молча пропускаются.** `review_engineer.py:48`, `review_philolog.py:49`,
   `review_seo.py:72` фильтруют по `os.path.exists`. Опечатка в списке = страница просто не попала
   в корпус, без единого предупреждения. А вот `review_all_50.py:22` и `engineer_sweep.py:32` этой
   защиты не имеют — там будет `FileNotFoundError`.

7. **Разная база путей.** Большинство скриптов строят пути от `__file__` (`DIR = os.path.dirname(...)`)
   и работают из любого CWD. Но `review_search_report.py:8-13` и `review_select.py`/`review_variations.py`
   в части чтения используют относительные пути от CWD. Запускать всё из `seo-texts/`.

8. **Кэши ревью писем не совместимы между двумя скриптами.** `review_select.py:27` пишет
   `review-cache/<id>.json`, `review_variations.py:28` — `review-cache/v<id:03d>.json`. Один и тот же
   каталог, разные имена: повторный прогон другим скриптом заново оплатит все вызовы. Оба при этом
   перезаписывают один и тот же `variations-100.json`.

9. **`_raw_stream` — приватная функция.** `review_select.py:36`, `review_variations.py:39`,
   `review_via_provider.py:112`, `eng_fix_review.py:42`, `model_compare.py:49`,
   `model_extract_test.py:36`, `role_model_test.py:29` вызывают `gen_provider._raw_stream` напрямую,
   в обход `gp.call` с его 8 ретраями и авто-отключением thinking. Свои ретраи у них короче (3-5) и
   без экспоненты. Изменение сигнатуры `_raw_stream` сломает семь модулей разом.

10. **`eng_fix_review.py` привязан к трём конкретным хэшам** (`:12-16`). Если история переписывалась
    или коммиты в другой ветке — `git show` вернёт пустую строку, и модель будет ревьюить пустой дифф,
    не жалуясь.

11. **Оценка стоимости в model_*-скриптах приблизительная**: токены считаются как `len(строки)/3`
    (`model_compare.py:138-139`), цены — официальные Anthropic, а не router.cheap. Абсолютные суммы
    в `MODEL-COST-DECISION.md` доверять нельзя, соотношения — можно.

12. **Ревьюеры не блокируют публикацию.** Ни один из них не гейт: они пишут markdown, который читает
    человек. Единственная автоматическая петля — sweep → flagged → defect-map → `regen_driver`, и та
    была разовой (defect-map собирался вручную).

---

## 6. Что сломано или устарело

### 6.1 `review_select.py` — мёртвый дубль

Ни один файл в репозитории (на любой ветке, включая origin) не упоминает `review_select`. Он делает
то же, что `review_variations.py`: читает `variations-pool.json`, судит письма одним вызовом по
линзам, отбирает 100 в `variations-100.json`. Отличия: судья возвращает `verdict PASS|WARN|CRITICAL`
вместо списка `critical` (`review_select.py:23`), отбор — просто топ-100 по баллу без балансировки
по бакетам (`:54-55`), кэш в другом формате. В конвейере (`night_orchestrator.py:44`) стоит
`review_variations.py`. **`review_select.py` — мёртвый код, безопасно игнорировать.**

### 6.2 `review_search_report.py` — входов нет в репозитории

Скрипт читает `inventory/OTCHET-poisk.md`, `inventory/search-problems.csv`,
`inventory/search-results.jsonl` (`:8-13`). Каталога `inventory/` нет ни локально, ни в одном
коммите ни одной ветки — проверено полным обходом деревьев всех ревизий (`git rev-list --all` +
`git ls-tree -r`): ни одного пути с `inventory` в истории. Сырьё аудита поиска лежит на дропе
(коммит `601b5b1`: «Сырьё прогона: drop `inventory-search-v3.tar.gz`»).

Важно: **у скрипта есть более поздний преемник — `review_v3.py`**, и он существует, просто НЕ в
текущей ветке. Он лежит только в `origin/claude/hopeful-galileo-n8gg7o`, коммит `601b5b1`, который
не является предком HEAD. Посмотреть:
```bash
git show origin/claude/hopeful-galileo-n8gg7o:seo-texts/review_v3.py
```
`review_v3.py` рецензирует отчёт v3 после починки поиска, отдельно проверяя, не выдумана ли
«просадка» 1.80→1.73 при недетерминированном ИИ-оценщике.

### 6.3 `review_api.py` — читает несуществующие payload

`review_api.py:60` ищет `gen/payload-el-<slug>.json`, при отсутствии подставляет пустой dict (`:61`).
Таких файлов в `gen/` **ноль** (их пишет `build_el_payloads.py:7`, `OUT = 'gen/payload-el-{slug}.json'`;
в истории они не коммитились). Значит блок «ДАННЫЕ, НА КОТОРЫХ ПИСАЛСЯ ТЕКСТ» уйдёт модели как JSON
из одних `null`, а критерий `grounding` («все факты соответствуют payload») будет оценён вслепую.
Скрипт при этом отработает и выдаст правдоподобные цифры. Никто его не вызывает; выхода
`gen/review-result-*.json` в репозитории нет.

### 6.4 Захардкоженный scratchpad несуществующей сессии

Пять скриптов пишут пути вида
`/tmp/claude-0/-home-user-avto/bcce55cd-293a-515c-9700-ae71a77daa5a/scratchpad`:
`review_via_provider.py:14`, `eng_fix_review.py:11`, `model_compare.py:11`,
`model_extract_test.py:12`, `role_model_test.py:13`.
Этого каталога больше нет (в `/tmp/claude-0/-home-user-avto/` сейчас только текущая сессия).
Последствия:
* кэши создадутся заново (`mkdir(parents=True)`) — все вызовы будут оплачены повторно;
* `model_extract_test.py:87` читает `scratchpad/roletext_recovered.json` — файла нет,
  упадёт с `FileNotFoundError`;
* `model_compare.py` и `role_model_test.py` берут вход из argv, поэтому запустятся, но отчёты
  положат в новый пустой scratchpad.

Перед запуском любого из этих пяти путь надо править на актуальный scratchpad. Результаты уже
сохранены: `MODEL-COST-DECISION.md`, `MODEL-EXTRACT-DECISION.md`, `sender/REVIEW-FINDINGS.json`.

### 6.5 `sender/REVIEW-FINDINGS.md` затёрт откатом

Хронология проверена по git:
* `fd68e4a` (2026-07-17) — положен ручной fact-checked roadmap;
* `757e488` (2026-07-21) — `review_via_provider.py` (модель `claude-sonnet-5`) перезаписал `.md`
  своим сводом на 112 находок и создал `.json`;
* `48ef189` (2026-07-26, синхронизация панели с боевого сервера) — откатил `.md` обратно
  (−230/+77 строк).

Сейчас в HEAD: `.md` **побайтно совпадает с версией `fd68e4a`**, `.json` **побайтно совпадает с
версией `757e488`**. То есть два файла с одинаковым именем-корнем рассказывают про разные прогоны.
Машинный свод ревьюера восстанавливается так:
```bash
git show 757e488:seo-texts/sender/REVIEW-FINDINGS.md
```

### 6.6 Артефакты старой версии `engineer_sweep.py`

`eng-sweep/`, `eng-sweep-report.md`, `engineer-flagged.txt` — без тега в имени, а текущий код всегда
пишет с тегом (`:12, :74, :86`). Заголовок `eng-sweep-report.md:1` — «Инженерный sweep не-брендовых
страниц», тогда как код формирует `f'# Инженерный sweep: {_TAG}'` (`:77`). Значит эти три артефакта
сделаны версией скрипта, которая до коммита не дожила (весь seo-texts влит одним коммитом `3614fef`,
истории правок `engineer_sweep.py` нет).

Практический вывод: повторный `python3 engineer_sweep.py sweep-facets.txt` создаст
`eng-sweep-sweep_facets/`, `engineer-flagged-sweep_facets.txt`, `eng-sweep-report-sweep_facets.md` —
всё заново и за полные деньги. Чтобы переиспользовать кэш, надо либо переименовать `eng-sweep/`, либо
назвать входной файл так, чтобы тег совпал.

### 6.7 `reviews.html` — не имеет отношения к ревьюерам

В корне `seo-texts/` лежит `reviews.html` (380 КБ) — это скачанная страница
`prokompressor.ru/company/reviews/` (отзывы клиентов) из краулинга сайта. К ревью-скриптам отношения
не имеет, не путать.

---

## 7. Что не проверено

* **Ни один скрипт этой области я не запускал.** Правило сессии запрещает вызовы провайдерского API;
  на сервере владельца идёт боевое обогащение. Все выводы — из чтения кода, отчётов, логов и git.
  Проверена только синтаксическая корректность: `python3 -m py_compile` проходит на всех 18 модулях.
* **НЕ ПРОВЕРЕНО: качество самих ревью.** Я не сверял ни одну находку инженера/филолога/SEO с
  реальным текстом страницы. Возможно, часть находок — галлюцинации модели. В частности, инженерный
  свип флагнул 213 страниц из 759 (28%) — насколько эти дефекты реальны, не проверял никто, кроме
  самой модели.
* **НЕ ПРОВЕРЕНО: происхождение `defect-map.json`.** Совпадение множества ключей с объединением
  flagged-файлов доказано (213 = 213, множества равны), но КАК описания дефектов попали в значения —
  выдернуты ли они парсером из `eng-sweep-report*.md` или переписаны моделью — я не выяснил.
  Скрипта нет; ПРЕДПОЛОЖЕНИЕ: собрано сессией разово.
* **НЕ ПРОВЕРЕНО: откуда взялись 3 «разрешённых» слага** в `defect-map-resolved.json`, которых нет в
  `defect-map.json` (`podgotovka-vozdukha__filtry-magistralnye__pneumatech`,
  `...__filtry-magistralnye__remeza`, `...__separatory-tsentrobezhnye-tsiklonnye__rkz`). Логика
  «8 коротких → 3 полных» — моя реконструкция по совпадению суффиксов, не по коду.
* **НЕ ПРОВЕРЕНО: реально ли `regen_driver.py` отработал по всем 208 дефектам.** Я читал только его
  входы; `regen-state.json` и связку с очередями (`regen-queue*.json`) не разбирал — это область
  документа `docs/01-generaciya-tekstov.md`.
* **НЕ ПРОВЕРЕНО: живы ли модели** `claude-fable-5`, `claude-opus-4-8`, `claude-sonnet-5`,
  `claude-haiku-4-5` на шлюзе сегодня. Имена взяты из кода; `gen_provider.resolve_model` (`:228`)
  я не читал построчно — возможно, там есть маппинг алиасов, меняющий фактически вызываемую модель.
* **НЕ ПРОВЕРЕНО: содержимое `variations-pool.json` и `variations-100.json`** — только размеры и
  факт существования. Не сверял, что в `variations-100.json` ровно 100 записей и что у каждой есть
  поле `review`.
* **НЕ ПРОВЕРЕНО: актуальность `sender/REVIEW-FINDINGS.json`** относительно текущего кода сендера.
  Ревью прогонялось 2026-07-21 на 20 модулях; с тех пор коммит `48ef189` (2026-07-26) обновил
  21 файл и добавил 32 из боевого сервера — большая часть находок могла устареть или, наоборот,
  относиться к коду, которого в репозитории тогда не было.
* **НЕ ПРОВЕРЕНО: содержимое дропа.** Я не обращался к `drop_client.sh` (запрещено правилами сессии).
  Утверждение, что сырьё аудита поиска лежит там как `inventory-search-v3.tar.gz`, взято из текста
  коммита `601b5b1`, а не из листинга дропа.
* **ПРЕДПОЛОЖЕНИЕ (не факт): `seo-effect-report.md` и `manifest/editor-diff-review.md` написаны
  сессией без скрипта.** Основание — grep по всем `.py` в репозитории не находит кода, который бы их
  писал. Но скрипт мог существовать и быть удалён, либо жить в невыгруженной ветке — полный обход
  истории на предмет «был ли когда-то генератор seo-effect-report» я НЕ делал.
* **НЕ ПРОВЕРЕНО: работает ли `review_variations.py` в текущем виде.** `night_orchestrator.py` его
  вызывает, но был ли этот вызов успешным в последний раз — из логов не видно, `review-cache/`
  отсутствует локально (в `.gitignore`).
